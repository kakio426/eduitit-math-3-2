# 매스몬 표적 맞히기 제작 보고 (3-2-3-1)

## 2026-07-29 점수 확인 뒤 보상 효과 재생

- 점수 모달을 여는 순간 왼쪽 보상 장면 효과가 블러 뒤에서 먼저 재생되던 순서 오류를 수정했습니다.
- 모달의 점수 공개 단계에서는 변화량만 저장합니다. 학생이 `다음`을 누르면 모달을 먼저 닫고, 왼쪽 표적 장면 효과를 720ms 재생한 뒤 다음 문제 또는 결과 화면으로 넘어갑니다.
- 증가 보상은 반짝임, 감소 보상은 같은 단계에 머물러도 어두워지는 반응이 보입니다. 모션 줄이기 설정에서는 대기 없이 전환합니다.
- GPT 브라우저에서 `+8` 보상으로 `모달 공개 중 효과 없음 → 버튼 직후 모달 숨김·효과 시작 → 효과 종료 뒤 2/10 전환`을 확인했습니다.
- 자동 전체 흐름 QA는 1280×800, 1024×768와 등록된 1082×897 DPR2 회귀 화면에서 통과했습니다. 텍스트 넘침·요소 겹침·누락 이미지는 0건입니다.
- 학생에게 보이는 문구는 바꾸지 않았습니다. Humanizer 학생 문구 QA 대상 변화 없음입니다.

## 2026-07-29 문제 화면 6단계 전용 보상 장면 재제작

- 실패 원인은 1280×800 최종 결과 장면을 좁은 세로 슬롯에 `object-fit: cover`로 넣은 것이었습니다. 번개늑대몬과 표적 일부가 잘렸고, 플레이 중 진행 변화도 읽히지 않았습니다.
- `3-2-2-3-mathmon-star-pickup`처럼 플레이 상태와 최종 결과를 분리했습니다. `연습 표적 → 가장자리 → 명중 → 정중앙 → 표적왕 → 전설 명중`에 대응하는 768×1536 전용 생성 이미지 6장을 새로 만들었습니다.
- 단계가 오를수록 화살이 바깥 고리에서 안쪽·정중앙으로 모이고, 충격광·금빛 장식·번개 효과·번개늑대몬 반응이 커집니다. 모든 화살은 표적 면에 박힌 접점과 그림자가 보입니다.
- 왼쪽 슬롯은 Stage 기준 왼쪽 `2.5%`, 폭 `25.78125%`, 위 `11%`부터 아래 `6.5%`까지의 정확한 1:2 비율입니다. 이미지는 `object-fit: contain`으로 표시하며 전신·표적 잘림은 0건입니다.
- 플레이 전용 이미지가 최종 결과 이미지를 재사용하거나 fallback하지 못하도록 source QA를 추가했습니다. 6개 `playImage`는 모두 고유해야 하고, `playImage !== image`, 768×1536, `object-fit: contain`을 만족해야 합니다.
- 플레이 6장 컨택시트는 `play-target-progress-v2-contact-sheet.png`, 플레이↔최종 결과 1:1 비교표는 `play-vs-final-v2-contact-sheet.png`입니다.
- 사용자 회귀 화면 `user-reported-left-reward-character-cropped-1082x897-dpr2`에서 6단계를 각각 강제 렌더했습니다. Stage `1038.75×649.22px`, 왼쪽 보상판과 오른쪽 작업 영역 교차 `0px`, 텍스트 넘침 `0건`, 이미지 누락 `0건`입니다.
- 같은 규칙을 `AGENTS.md`, `CLAUDE.md`, `LESSON_COMMONS.md`, 차시 제작 스킬, 매스몬 자산 스킬에 추가했습니다. 최종 결과 CSS 크롭 재사용과 캐릭터 잘림은 공통 실패 조건입니다.

## 2026-07-28 최종 보상 화면 정리

- 기준 차시 `3-2-2-4-mathmon-check-lock`처럼 오른쪽 결과 상자 안을 `다음 목표 → 정답 수` 한 축으로 정리했습니다.
- 최종 화면에서 표적 점수 막대와 점수 숫자를 제거해 같은 정보를 반복하지 않게 했습니다.
- `다음은 가장자리·명중·정중앙·표적왕·전설 명중`과 `최고 단계`를 HTML 글자가 아닌 생성형 투명 PNG 6장으로 제작해 상태별로 연결했습니다.
- 정답 수 이미지까지 포함한 결과 묶음의 중심을 파란 상자 중심에 맞췄습니다.
- `다시`는 결과 장면에 포함된 승인된 공용 버튼 표면을 유지하고, 같은 경계의 공용 HTML hitbox만 클릭을 맡깁니다. 불투명 공용 WebP를 다시 겹쳐 네모 배경이 생기지 않도록 내부 중복 이미지는 숨겼습니다.
- 사용자 발견 화면을 `user-reported-final-reward-ui-broken-1082x897-dpr2` 회귀 항목으로 기록했습니다.
- 현재 1082×897 DPR2 결과 화면에서 생성형 다음 목표, 정답 수, 다시하기 버튼의 겹침과 상자 밖 넘침은 0건입니다.
- 같은 화면에서 결과 타이틀과 정답 수 중심축이 오른쪽으로 밀린 사례를 `user-reported-result-panel-axis-1082x897-dpr2`로 추가했습니다. 두 요소의 Stage 기준 중심을 모두 `64.8%`로 맞추고 세로 간격을 줄였습니다.

## 2026-07-28 공통 보상 모달 복구

- 화면 크기에 따라 위치가 무너지던 차시 전용 `target-reward-story` DOM·CSS·상태 훅을 제거했습니다.
- 기준 차시 `3-2-2-4-mathmon-check-lock`과 같은 엔진 공통 `modal-art` 구조를 사용합니다.
- 닫힌 상태는 사건 그림을 숨긴 채 가려진 표적과 `열기`만 보여 주고, 열린 상태는 사건 그림·실제 변화량·`다음`만 보여 줍니다.
- 공통 보상 표준은 `mathmon-unified-reward-v1`이며, 빈 보상은 누적값을 유지합니다.
- 사용자 발견 화면은 최종 보상 문제로 다시 확인되어 `user-reported-final-reward-ui-broken-1082x897-dpr2`로 바로잡았습니다.
- 실측 결과: 1082×897 DPR2에서 닫힌 카드 560×420px, 열린 카드 560×442.63px이며 카드·그림·문구·버튼이 모두 Stage 안에 있습니다.
- 1280×800과 1024×768에서도 카드·그림·문구·버튼이 Stage 안에 있고, 그림↔문구와 문구↔버튼 교차는 모두 0px입니다.

## 2026-07-27 문제 화면 리디자인

- 문제판·지시판·선택지가 따로 떠 보이던 화면을 하나의 황동·에나멜 표적 콘솔로 다시 설계했습니다.
- 왼쪽 대포·화살 보상 장면을 보존하고, 오른쪽 Stage 폭 65.5%에 네 선택지를 안정적인 2×2 슬롯으로 고정했습니다.
- 큰 질문은 `중심`, `반지름`, `지름` 한 단어만 남기고 바로 아래에 현재 행동 한 줄만 보이게 했습니다.
- 오답은 고른 표적 안에서 중심 위치, 부족한 끝점, 원까지 닿는지 여부를 그림으로 보여 줍니다.
- 정답은 같은 표적 안에 초록빛으로 확정되고, 다른 표적만 흐려져 화면의 크기와 중심축이 움직이지 않습니다.
- 설명 1도 새 콘솔 조작과 같은 장면으로 바꾸고, 정답 확인 뒤에는 생성형 `점수 보기` 버튼을 사용합니다.
- 사용자 발견 화면을 `1082×897, DPR 2`라는 이름 있는 회귀 viewport로 추가했습니다.

## 이번 고도화

- 기존 검은 패널 중심 화면을 생성 이미지 기반의 밝은 표적 훈련장으로 교체했습니다.
- 한 원에 후보 4개를 겹쳐 표시하던 방식을 독립된 미니 원 선택지 4개로 바꿨습니다.
- 문제 화면에서 점수 장치·등급 트랙·긴 설명을 빼고 `큰 질문 → 원 → 한 줄 지시 → 선택지`만 남겼습니다.
- 정답 뒤 선택한 점 또는 선분이 큰 원에 확정되어 보이는 확인 화면을 추가했습니다.
- 소스 엔진을 `_lessons/3-2-3-1-mathmon-target-hit/`로 분리하고 `index.html`을 빌드 산출물로 전환했습니다.

## 2026-07-12 1단원 기준 시각 흐름 보강

- 커버의 번개늑대몬을 설명·보상·결과까지 유지하고, 팩은 `diversity-reward-pack` 하나로 고정했습니다.
- 설명 1에서 `중심`, `반지름`, `지름`을 명시하고 각각 `원 한가운데 점`, `중심에서 원까지`, `중심을 지나 양쪽 끝까지`로 연결했습니다.
- 설명 2에서는 풀이법을 반복하지 않고 `10문제 → 덮인 표적 열기 → 점수 +/0/− → 표적 이름`을 실제 보상·결과 장면과 같은 순서로 보여 줍니다.
- 기존 설명 2의 크기 변화 표적과 파란 사람 실루엣은 뜻이 모호해 제거했습니다. 새 그림은 실제 닫힌 표적, 세 가지 점수 방향, `명중` 결과판을 한 흐름으로 연결합니다.
- 문제 제목 상자 좌우의 기능 없는 금색 쐐기 장식은 수학 선분으로 오해하지 않도록 제거했습니다. 상자도 610px에서 260px 중앙 이름표로 줄여 글자와 테두리만 또렷하게 남겼습니다.
- 이전 설명 2 원본·PNG·WebP는 `_archive/20260729-pre-tutorial-reward-flow/`에 보존했습니다.

### 2026-07-29 설명·문제 제목 회귀 QA

- 새 실행 이미지 `tutorial-page-2-reward-flow-v2-generated.webp`는 `1280×800`, 디코드 완료, 누락 이미지 `0건`입니다.
- 사용자 화면 회귀 항목 `user-reported-problem-title-side-spikes-1082x897-dpr2`를 추가해 설명 2와 문제 대기·오답·정답·보상·결과를 다시 캡처했습니다.
- 해당 화면에서 문제 이름표는 실제 `285.75×64px`, Stage 폭 대비 `27.51%`이며 작업 영역 중심과 같은 x축에 놓였습니다.
- 작업 영역 형제 교차 `0건`, 텍스트 넘침 `0건`, 누락 이미지 `0건`입니다.
- Humanizer 학생 문구 QA: `10문제를 풀어요.`, `맞히면 표적을 열어요.`, `끝나면 내 표적 이름을 봐요.`는 한 문장 한 행동이며 번역투·제작자 용어가 없습니다. 자연도 A, 의미 보존 6항 통과입니다.
- 랜덤 보상 6종은 일반 명중·가장자리·대량 명중·정중앙·빗나감·무지개 명중을 별도 이미지로 구분했습니다.
- 결과 6장은 모두 1280×800이며, 번개늑대몬의 위치·오른쪽 동적 정보판·고정 제목·`다시` 버튼 슬롯을 통일했습니다.
- 결과 컨택시트: `result-tiers-v2-contact-sheet.png`
- 표지 왼쪽 위는 실제 `eduitit-logo-mark.png`를 쓰며, 공용 상단 슬롯 좌표를 따릅니다.

## 수학 설계

- 10문제는 중심 4개, 반지름 3개, 지름 3개를 섞어 출제합니다.
- 각 문제는 정답 1개와 대표 오개념 3개를 갖습니다.
- 모든 오답에는 `misconceptionId`와 한 줄 피드백이 있습니다.
- 중심·반지름·지름은 색이 아니라 점과 선분의 기하 관계로 구분합니다.
- 오답 뒤에는 선택한 잘못된 관계를 큰 원에서 보여 주고, 다시 고르게 합니다.

## 이미지·동적 UI 역할

- 생성 이미지: 배경 공간, 빛, 목재, 표적 훈련 장치
- SVG: 원, 중심점, 반지름, 지름, 현, 선택·확인 상태
- HTML: 질문과 한 줄 문구, 접근성 버튼 hitbox
- 문제 배경: `problem-stage-generated.webp` 1280×800

## 보상 구조

- 중심 보상은 `표적 점수` 하나입니다.
- 일반 증감, 큰 증가, 정중앙, 0, 무지개 이벤트를 유지했습니다.
- 문제 화면에서는 숫자 점수를 숨기고, 왼쪽 생성 장면으로 현재 보상 단계만 보여 줍니다.
- 정답 확인 뒤 학생이 `점수 보기`를 누르면 공통 보상 모달이 열립니다.
- `열기`를 눌러야 사건 이미지와 실제 점수 변화가 나타납니다.

## Humanizer 학생 문구 QA

- 추상적인 `묻는 관계`를 `알맞은 점이나 선분이 그려진 원`으로 바꿨습니다.
- 리디자인에서는 문제 제목을 교과 용어 한 단어로 줄이고, `표적 네 개 중 알맞은 그림을 골라요.`, `점수 보기`, `열기`처럼 한 문장에 행동 하나만 남겼습니다. 보상에는 실제 변화량 한 덩어리만 보입니다.
- 문제 지시와 피드백은 한 문장에 행동이나 이유 하나만 담았습니다.
- `중심`, `반지름`, `지름`은 교과 수학 용어이므로 유지하고, 바로 아래에 위치 관계를 쉬운 말로 붙였습니다.
- 전수 확인 결과 S1 0건, S2 0건, 자연도 A이며 수치·교과 의미·부정 표현·격식 수준을 그대로 보존했습니다.

## 텍스트 넘침·요소 겹침 QA

- 이번 보상 수정은 1280×800, 1024×768, 1082×897 DPR2에서 닫힌·열린 공통 모달을 각각 확인했습니다.
- 세 화면 모두 카드·사건 그림·변화량·버튼이 Stage 안에 있습니다.
- 열린 상태의 그림↔변화량, 변화량↔버튼 교차는 모두 0px입니다.
- 1082×897 DPR2에서 닫힌 상태와 열린 상태를 눈으로 확인했으며 잘림·넘침·요소 겹침은 0건입니다.

## 2026-07-27 리디자인 실측

| 화면 | Stage | 작업 영역 | Stage 폭 비율 | 2×2 표적 콘솔 | 최소 선택지 |
|---|---:|---:|---:|---:|---:|
| 1280×800 | 1203.19×751.98 | 788.09×663.98 | 65.50% | 788.09×441.98 | 364.05×192.98 |
| 1024×768 | 983.06×614.41 | 643.92×526.41 | 65.50% | 643.92×304.41 | 291.95×124.20 |
| 1082×897 DPR2 | 1038.75×649.22 | 680.39×561.22 | 65.50% | 680.39×339.22 | 310.19×141.61 |

- 세 화면 모두 제목판 → 한 줄 행동판 → 표적 콘솔 사이 간격 8px 이상, 상호 교차 0px입니다.
- 왼쪽 25.78125% 보상 단계 장면과 오른쪽 65.5% 문제판은 세 화면 모두 분리되어 서로 겹치지 않습니다.
- 대기·오답·정답·완료에서 표적 콘솔의 좌우 경계와 중심축은 움직이지 않습니다.
- 설명 1의 공용 생성형 `다음` 버튼과 정답 확인의 생성형 `점수 보기` 버튼은 이미지와 hitbox 실제 rect가 1px 이내로 같습니다.
- 중심이 테두리에 있음, 반지름을 지름으로 고름, 지름이 중심을 지나지 않음 상태를 세 화면에서 각각 다시 캡처했습니다.
- 중심을 지나지 않는 현은 중심에서 35 SVGpx 떨어지게 고쳤고, 선 두께를 포함한 자동 기준 `12px 초과`를 source·브라우저 QA에 추가했습니다.
- 결과 화면은 배경에 생성된 `다시` 버튼 하나만 보이게 하고 투명 hitbox만 맞췄습니다. 정답 수·다음 목표·재시도 버튼은 세 화면에서 서로 겹치지 않습니다.

## 2026-07-23 하네스 보완

- 공용 시작 버튼과 hitbox의 중심·너비·높이 차이는 두 화면 모두 1px 이하입니다.
- 문제 HUD의 브랜드·문제 수·단원·설정 버튼은 서로 겹치지 않습니다.
- 1280×800 브라우저에서 실제 Stage는 1203.19×751.98px, 작업 영역은 758.02×659.98px입니다. Stage 폭의 63.00%, 면적의 55.29%입니다.
- 1024×768 브라우저에서 실제 Stage는 983.06×614.41px, 작업 영역은 619.34×522.41px입니다. Stage 폭의 63.00%, 면적의 53.57%입니다.
- 핵심 문제판 면적은 데스크톱 26.96%, 태블릿 24.65%로 선택지 묶음보다 큽니다.
- 최소 선택지 크기는 데스크톱 374.50×130.95px, 태블릿 305.17×101.50px입니다.
- 설정 버튼은 42×42px, 브랜드·단원 배지는 14px, 문제 수는 16.8px, 태블릿 지시문은 18.43px 이상입니다.
- 문제판·지시판·선택지 사이 실제 간격은 8px 이상이며, 문제 SVG의 바깥 표면도 문제판 안에 들어옵니다.
- 대기 문제판과 완료 패널의 좌우 경계·중심 차이는 1px 이하입니다.
- 최저 결과를 `practice / 연습 표적`으로 교체했습니다. `0점·0/10` 강제 화면에서도 새 1280×800 결과 장면과 동적 숫자판이 맞게 보입니다.
- 결과 6장 컨택시트를 갱신했고, 이전 `result-miss-*`와 이전 전체 흐름 캡처는 `_archive/20260723-pre-harness-remediation/`에 보관했습니다.
- Humanizer 전수 검사에서 목표·설명·지시·오답·확인·보상·결과·접근성 이름을 확인했습니다. 교과 용어 외 제작자 말투와 번역투 잔여는 없습니다.

## 검증 결과

- `node scripts/qa-engine-unit3-target-source.mjs` → PASS
- `node scripts/build-lesson.mjs 3-2-3-1-mathmon-target-hit` → PASS
- `node scripts/check-lesson-contract.mjs 3-2-3-1-mathmon-target-hit` → PASS
- `node scripts/check-lesson-visual-contract.mjs 3-2-3-1-mathmon-target-hit` → PASS
- `node scripts/check-stage-ratio.mjs` → PASS (24개 차시)
- `node scripts/check-ranking-disabled.mjs` → PASS
- 브라우저 보상 QA: 1280×800, 1024×768, 1082×897 DPR2에서 닫힌·열린 모달과 플레이 전용 6단계를 각각 렌더해 Stage 이탈 0, 형제 요소 교차 0, 이미지 잘림 0
- 스킬 frontmatter 검증: 시스템 검사기는 로컬 `PyYAML` 부재로 실행되지 않아 동일 계약을 Ruby YAML 파서로 검사했으며 차시·자산 스킬 모두 PASS

## 검증 자산

- 데스크톱: `screenshots/engine-flow-desktop-01-cover.png`부터 `08-result.png`
- 태블릿 가로: `screenshots/engine-flow-tablet-landscape-01-cover.png`부터 `08-result.png`
- 사용자 발견 화면: `screenshots/engine-flow-user-redesign-1082x897-dpr2-01-cover.png`부터 `08-result.png`
- 오답 상태: 각 화면군의 `05b-play-wrong.png`
- 정답 확인: 각 화면군의 `06-confirm.png`
- 닫힌 보상·열린 보상: 각 화면군의 `07-reward-closed.png`, `07b-reward-open.png`
- 플레이 보상 단계 전수: `screenshots/engine-flow-user-reported-left-reward-character-cropped-1082x897-dpr2-05p-play-tier-*.png`
- 최저 결과: 각 화면군의 `08a-result-practice-0-of-10.png`
- 기준 비교와 입력 통계: `BENCHMARK_AUDIT.md`
