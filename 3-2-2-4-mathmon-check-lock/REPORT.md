# 매스몬 검산 자물쇠 구현 보고서

- 갱신일: 2026-07-14
동행 매스몬 팩: `base-pack`

## 2026-07-11 고도화 결과

- 곱한 값과 더한 값을 4지선다가 아니라 금고 숫자판에 직접 넣도록 바꿨습니다.
- 비교와 오류 찾기는 `같아요 / 달라요`, `몫 / 나머지` 레버로 조작합니다.
- 단계마다 금고 핀이 켜지고 마지막 확인 뒤 금고가 열립니다.
- 닫힌 금고를 학생이 직접 연 뒤 랜덤 열쇠 사건과 값이 공개됩니다.
- 결과는 빈 점수판 없는 보물 금고 장면과 독립 등급명 이미지로 바꿨습니다.

## 2026-07-14 비교 감사 후 수정

- 특별 결과 `무지개 금고`가 낮은 일반 금고를 다음 목표로 가리키던 모델 경계를 수정했습니다.
- 마지막 확인에 남아 있던 빈 안내판을 없애 검산식과 `자물쇠 열기`를 한 덩어리로 묶었습니다.
- 닫힌 보상은 이미지와 `열기`만, 열린 보상은 이미지·변화량·분명한 `다음` 버튼만 보입니다.
- 곱한 값이 너무 큰 오답과 너무 작은 오답을 데스크톱·태블릿에서 각각 캡처합니다.
- 현재 실행 HTML이 참조하던 추적 밖 자산 22개와 정본 반응 자산을 Git 추적 대상으로 추가했습니다.
- 예전 DOM을 검사하던 614줄 전용 QA를 `scripts/_archive/qa-lesson2-check-lock-legacy-20260714.mjs`로 옮기고 현재 엔진 전체 흐름 QA로 교체했습니다.

## 2026-07-14 Codex 브라우저 겹침 회귀 수정

- 사용자 제보 화면을 `codex-browser-regression`으로 이름 붙이고 `1280×720`, DPR 2, 실제 Stage `1075.19×671.98`을 영구 회귀 조건에 추가했습니다.
- 원인은 SVG 계산판의 `overflow: visible`, 58px 계산 단계판을 52px 트랙에 넣은 불일치, 브라우저 폭은 1280px이지만 높이 때문에 Stage가 1075px로 줄어드는 조건을 놓친 숫자판 최소 폭이었습니다.
- 계산판과 숫자판을 각각 자기 영역 안에 가두고, 단계 트랙을 58px로 맞추며, `지우기 / 숫자 / 넣기` 열을 줄어든 Stage 안에서도 42px 터치 영역을 지키도록 다시 배치했습니다.
- QA는 이제 SVG 바깥 상자만 보지 않고 실제 `.lock-board-bg`, 지시문, 피드백, 선택지·숫자판, 버튼 hitbox의 렌더 경계를 잽니다. 계산판-지시문 6px 이상, 지시문-선택지 4px 이상, 형제 요소 겹침 0px, Stage 이탈 0건을 강제합니다.
- 곱하기·더하기·같고 다름의 대기/정답 확인, 곱한 값 과대·과소, 마지막 확인, 보상, 결과를 세 화면 크기에서 다시 캡처했습니다.
- 학생에게 보이는 문구는 바꾸지 않았습니다.

## 수학 계약

- 검산값은 `나누는 수×몫+나머지`입니다.
- 검산값과 처음 수의 같고 다름이 모델 값과 일치합니다.
- 틀린 식은 몫과 나머지 가운데 정확히 한 부분만 다릅니다.
- 한 판에는 맞는 식과 틀린 식이 모두 포함됩니다.

## Humanizer 학생 문구 QA

- `곱한 값을 숫자판에 넣어요.`
- `더한 값을 숫자판에 넣어요.`
- `같아요 또는 달라요를 눌러요.`
- `몫 또는 나머지를 눌러요.`
- `곱한 값이 너무 커요.`
- `곱한 값이 너무 작아요.`
- `열쇠 힘은 얼마나 나올까요?`

`핵심`, `메커니즘`, `게이트`, `토큰`, `오브젝트`, `부분곱` 같은 제작자 용어와 번역투를 학생 문구에서 쓰지 않았습니다. 보상 닫힘 상태의 안내 문장은 화면에서 숨겨 이미지와 `열기` 행동만 남겼습니다.

## 텍스트 넘침·요소 겹침 QA

- 1280×800, 1024×768, 사용자 제보 조건 1280×720에서 전체 흐름을 완주했습니다.
- 태블릿에서 숫자판의 `지우기 / 넣기` 버튼이 Stage 밖으로 나가던 문제를 수정했습니다.
- 최종 결과: 겹침 0건, 넘침 0건, 깨진 이미지 0건, Stage 밖 요소 0건입니다.
- 문제 장면 세트: 3장, 실행 크기 1280×800, `problem-state-contact-sheet.png`
- 결과 제목 세트: 6장, `result-title-contact-sheet.png`
- 브라우저 QA: 1280×800, 1024×768, 1280×720(DPR 2 제보 조건)
- 곱한 값 과대·과소, 마지막 확인, 닫힌·열린 보상, 결과를 각 화면 크기에서 별도 캡처했습니다.
- Codex 회귀 증거: `screenshots/engine-flow-codex-browser-regression-05-lock-*-waiting.png`, `05-lock-*-confirm.png`, `05b-play-product-too-high.png`, `05b2-play-product-too-low.png`
- 마지막 확인의 빈 패널 0건, 보상 중복 행동어 0건을 실제 PNG로 확인했습니다.
- 실행 HTML 참조 자산의 Git 추적 누락은 0개입니다.

## 통과한 검증

- `node scripts/qa-lesson-model.mjs 3-2-2-4-mathmon-check-lock` → `QA_LESSON_MODEL: PASS`
- `node scripts/qa-lesson2-check-lock.mjs 20260714` → `QA_LESSON2_CHECK_LOCK: PASS` (세 화면 크기 전체 흐름)
- `node scripts/check-stage-ratio.mjs` → 21개 차시 Stage 계약 통과
- `node scripts/check-rule-consistency.mjs` → 13개 문서·21개 차시 규칙 일치
- `node scripts/check-lesson-contract.mjs` → 17개 엔진 차시 통과
- `node scripts/check-lesson-visual-contract.mjs` → 12개 차시 시각 계약 통과
- `./.venv/bin/python manage.py test mathmon_scoreboard --keepdb` → 2단원 네 차시 서버 점수 재계산·변조 거부 검사 통과

현재 캡처는 이번 코드에서 다시 만든 `screenshots/engine-flow-*.png`입니다. 자동 종료 코드와 실제 화면 확인을 별도 증거로 남겼습니다.

## 전국 순위 활성화 QA

- 결과 화면에서 `전국 열쇠 순위`를 열고 `내 열쇠 힘`으로 1~10위를 비교합니다.
- 서버의 `check_lock` 검증기는 10문제의 곱하기·더하기·같고 다름·틀린 곳 찾기와 보상 사건을 다시 계산하고 변조 점수를 거부합니다.
- 생성형 `순위 보기` 면과 투명 hitbox를 정확히 맞추고 `다시`와 겹침 0px를 확인했습니다.
- 오프라인·연결 중·오류·빈 목록·긴 이름 10개·상하 스크롤·42×42px 이상 버튼을 세 화면 크기에서 통과했습니다.
- 현재 증거: `screenshots/engine-flow-*-08-result.png`, `09-scoreboard-offline.png`, `09b-scoreboard-10rows-start.png`, `09c-scoreboard-10rows-end.png`.

## 남은 제품 선택

- 문제 중 `현재 금고 → 다음 금고 → 마지막 금고` 표시는 시리즈 통일용 P2 항목입니다. 현재 문제 수와 최종 6단계 결과로 필수 진행 흐름은 통과합니다.
