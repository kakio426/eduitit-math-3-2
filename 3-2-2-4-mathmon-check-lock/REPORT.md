# 매스몬 검산 자물쇠 구현 보고서

- 갱신일: 2026-07-27
동행 매스몬 팩: `base-pack`

## 2026-07-27 3-2-2-3 기준 전면 재구성

- 기존 화면을 조금씩 고치는 방식을 중단하고, 3-2-2-3의 실제 문제 화면 골격을 기준으로 문제 화면을 처음부터 다시 배치했습니다.
- 왼쪽 세로 보상 세계는 Stage 기준 `23.4375%×82%`, 오른쪽 학습 열은 약 `70%`입니다. 큰 문제, 검산 자물쇠, 한 줄 안내, 선택지를 한 열로 맞췄습니다.
- 왼쪽은 여러 카드로 쪼개지 않고 킹드래곤몬·현재 금고·금고실이 이어지는 장면 하나로 만들었습니다. `지금 / 열쇠 힘 / 다음`은 위아래 그라데이션 위에만 표시합니다.
- 전용 생성 장면 6장은 모두 `600×1312`입니다. `play-vault-world-contact-sheet.png`에서 킹드래곤몬 전신, 금고, 빛길, 상하 잘림, 단계별 밝기 증가를 전수 확인했습니다.
- 검산 자물쇠는 바깥 사각 표면 1개와 원형 다이얼·핀만 씁니다. 곱셈 대기식과 선택 뒤 관계 묶음은 표면 중앙축 오차 5px 이하로 고정했습니다.
- 정답에는 핀 점등과 다이얼 회전, 오답에는 짧은 흔들림, 문제 완료에는 계산판에서 왼쪽 금고로 이동하는 열쇠빛을 넣었습니다. 열쇠 힘이나 금고 단계가 바뀌면 세로 장면의 핀 빛과 스파크가 이어집니다.
- 자동 흐름 QA는 여섯 화면 크기, 대표 오답, 모든 단계 확인, 닫힌·열린 보상, 결과를 완주했습니다. 겹침·넘침·깨진 이미지·Stage 이탈은 모두 0건입니다.
- 사용자 제보 `1082×897 DPR 2`에서 왼쪽 보상은 Stage 폭 `23.44%`, 오른쪽 학습 열 `70.11%`, 실제 검산 표면 `69.79%`, 곱셈 중앙축 오차 약 `3.2px`입니다.

## 2026-07-27 보상 장면·중앙 정렬·중첩 상자 수정

- 계산판을 화면 가운데 80%로 넓힌 재구성안이 왼쪽 보상 목표를 없앴습니다. 이어 배경 금고와 숫자형 `열쇠 힘`만 남긴 수정도 독립 보상판을 복구한 것이 아니었습니다.
- 3-2-2-1의 현재 보상 그림·이름과 3-2-2-2의 `지금 → 다음` 목표 연결을 다시 대조했습니다. 문제 화면 왼쪽에 현재 금고 그림·이름, `열쇠 힘`, 다음 금고 이름이 함께 보이는 독립 보상판을 만들고, 오른쪽 70%에는 계산 작업만 남겼습니다.
- 이 단계에서 쓰던 560×560 여섯 장은 이후 전면 재구성에서 `600×1312` 전용 세로 장면으로 교체했습니다.
- 곱셈식 위에 비어 있는 역할 라벨 자리를 남겨 둔 탓에 대기 상태 `?×?`가 애매한 높이에 놓였습니다. 문제 줄 아래 작업 구역을 따로 잡아 대기 식과 선택 뒤 관계 묶음을 그 중심에 고정했습니다.
- 곱하기·더하기 계산판의 문제 칩, 안쪽 초점 패널, 답 상자, 완료 칩을 없앴습니다. 현재 두 단계는 바깥 계산판 상자 1개만 사용하고, 다른 곳 찾기에서만 몫·나머지의 실제 비교 자리 2개를 남겼습니다.
- 이 단계에서는 한 줄 지시문의 크림색 상자를 없앴습니다. 최종 전면 재구성에서는 3-2-2-3과 같은 단일 안내판 하나로 다시 통합했고, 선택지는 학생이 실제로 눌러야 하므로 카드 형태를 유지했습니다.
- 여섯 회귀 화면에서 왼쪽 보상판은 Stage 폭의 20.40~21.80%, 높이의 66.23~77.38%입니다. 계산 영역과의 간격은 33.56~48px, 곱셈 관계의 작업 구역 중심 오차는 최대 0.46px입니다. 핵심 계산판은 Stage 폭의 67.48%, 전체 작업 열은 70%입니다.
- Humanizer 재점검 결과 `지금`, `다음`, `모든 금고를 열었어요`는 짧고 화면의 관계를 그대로 말합니다. 기존 `열쇠 힘`, `먼저 무엇과 무엇을 곱할까요?`와 함께 초3 학생이 바로 이해할 수 있는 말로 확인했습니다.

## 2026-07-27 문제 첫 화면 재구성

- 사용자가 제보한 물리 캡처 `2048×1280`을 CSS viewport `1024×640`, DPR 2로 추정하고 `user-reported-overload`라는 영구 회귀 조건으로 추가했습니다.
- 첫 재구성안은 `6×5`의 답부터 고르게 해 검산 방법보다 곱셈 능력을 확인한다는 사용자 지적을 받았습니다.
- Claude Code와 다시 교차 검토했습니다. 검산에서 먼저 알아야 할 것은 계산값이 아니라 `나누는 수×몫+나머지=처음 수`라는 관계라는 데 의견이 모였습니다.
- 첫 단계는 `무엇과 무엇을 곱할지`, 둘째 단계는 `무엇을 더할지`를 큰 보기 4개에서 고릅니다. 선택 뒤 곱과 합은 시스템이 계산하므로 이미 정해진 답을 다시 입력하지 않습니다.
- 곱하기 단계에는 곱하기만, 더하기 단계에는 완료된 곱셈 칩과 더하기만, 다른 곳 찾기 단계에는 완성된 검산 관계만 보여 줍니다. 아직 하지 않을 계산은 미리 펼치지 않습니다.
- 설명 1장도 같은 관계 선택 흐름으로 새로 만들었습니다. 생성 원본은 `tutorial-page-1-v6-source.png` 1586×992, 배포본은 `tutorial-page-1-generated.png/.webp` 1280×800입니다.
- 정답을 고르면 다음 단계보다 먼저 현재 계산판의 `?×?`가 `나누는 수×몫`으로 바뀌고 자동 계산식이 보입니다.
- 오답·정답 반응 캐릭터가 첫 번째 보기를 가리던 문제를 없앴습니다. 반응 자산은 보관하되 현재 문제 화면 오버레이는 사용하지 않습니다.
- 결과 화면의 오래된 선택자 이름을 현재 DOM과 맞추고 `정답 수` 이미지와 `다시 하기` hitbox의 태블릿 겹침 9px를 0px로 고쳤습니다.
- 자동 QA의 기존 중첩 템플릿 문자열 구문 오류 한 곳을 고쳐 현재 전체 흐름 검사가 실행되도록 했습니다.

설명 1 이미지는 built-in `imagegen` 편집 모드로 만들었습니다. 최종 프롬프트의 핵심은 다음과 같습니다.

> 기존 금고 튜토리얼의 금색 프레임·남색 배경·3패널 구도·다음 버튼을 유지한다. 1패널은 `32÷6=5…2`에서 `나누는 수 6×몫 5`를 관계 보기 중 강조한다. 2패널은 계산된 `6×5=30` 뒤 `나머지 2`를 더할 수 보기 중 강조한다. 3패널은 `6×5+2=32`, `32=처음 수 32`를 보여 준다. 숫자판·값 정답 보기·입력창은 넣지 않고 한글과 수식을 정확히 표시한다.

## 2026-07-11 고도화 결과

- 당시에는 곱한 값과 더한 값을 금고 숫자판에 직접 넣도록 바꿨습니다. 2026-07-27 정보 과밀 재구성에서 이 조작은 큰 보기 선택으로 교체했습니다.
- 틀린 식의 오류 찾기는 `몫 / 나머지` 두 선택지로 조작합니다.
- 단계마다 금고 핀이 켜지고 마지막 확인 뒤 금고가 열립니다.
- 닫힌 금고를 학생이 직접 연 뒤 랜덤 열쇠 사건과 값이 공개됩니다.
- 결과는 빈 점수판 없는 보물 금고 장면과 독립 등급명 이미지로 바꿨습니다.

## 2026-07-14 비교 감사 후 수정

- 특별 결과 `무지개 금고`가 낮은 일반 금고를 다음 목표로 가리키던 모델 경계를 수정했습니다.
- 마지막 확인에 남아 있던 빈 안내판을 없애 검산식과 `자물쇠 열기`를 한 덩어리로 묶었습니다.
- 닫힌 보상은 이미지와 `열기`만, 열린 보상은 이미지·변화량·분명한 `다음` 버튼만 보입니다.
- `처음 수×나누는 수`와 `나누는 수×나머지` 오답을 데스크톱·태블릿에서 각각 캡처합니다.
- 현재 실행 HTML이 참조하던 추적 밖 자산 22개와 정본 반응 자산을 Git 추적 대상으로 추가했습니다.
- 예전 DOM을 검사하던 614줄 전용 QA를 `scripts/_archive/qa-lesson2-check-lock-legacy-20260714.mjs`로 옮기고 현재 엔진 전체 흐름 QA로 교체했습니다.

## 2026-07-14 Codex 브라우저 겹침 회귀 수정

- 사용자 제보 화면을 `codex-browser-regression`으로 이름 붙이고 `1280×720`, DPR 2, 실제 Stage `1075.19×671.98`을 영구 회귀 조건에 추가했습니다.
- 원인은 SVG 계산판의 `overflow: visible`, 58px 계산 단계판을 52px 트랙에 넣은 불일치, 브라우저 폭은 1280px이지만 높이 때문에 Stage가 1075px로 줄어드는 조건을 놓친 숫자판 최소 폭이었습니다.
- 계산판과 숫자판을 각각 자기 영역 안에 가두고, 단계 트랙을 58px로 맞추며, `지우기 / 숫자 / 넣기` 열을 줄어든 Stage 안에서도 42px 터치 영역을 지키도록 다시 배치했습니다.
- QA는 이제 SVG 바깥 상자만 보지 않고 실제 `.lock-board-bg`, 지시문, 피드백, 선택지·숫자판, 버튼 hitbox의 렌더 경계를 잽니다. 계산판-지시문 6px 이상, 지시문-선택지 4px 이상, 형제 요소 겹침 0px, Stage 이탈 0건을 강제합니다.
- 곱하기·더하기·자동 비교·틀린 곳 찾기의 대기/정답 확인, 곱한 값 과대·과소, 마지막 확인, 보상, 결과를 네 화면 크기에서 다시 캡처했습니다.
- 학생에게 보이는 문구는 바꾸지 않았습니다.

## 2026-07-14 자동 비교·시각 우선순위 수정

- 곱한 값과 더한 값을 입력하면 같고 다름은 수학적으로 이미 결정됩니다. 새 판단이 없던 `같아요 / 달라요` 입력을 삭제하고 계산판이 `=` 또는 `≠`를 자동으로 보여 주도록 바꿨습니다.
- 맞는 식은 곱하기·더하기 두 판단 뒤 완성식으로 이동합니다. 틀린 식은 자동 `≠` 확인 뒤에만 `몫 / 나머지` 가운데 다른 곳을 찾습니다.
- 마지막 `자물쇠 열기`는 무작위 열쇠 힘을 공개하므로 누르기 전후 정보가 달라집니다. 이 버튼은 계산 버튼이 아니라 계산과 보상을 잇는 선택으로 유지했습니다.
- 좁던 검산 계산판을 가로형으로 다시 짜고, 문제 작업 영역을 Stage 너비의 76~84%, 핵심 계산판을 73.27~80.98%로 키웠습니다.
- 완료 상태에서도 계산판과 `완성식 + 자물쇠 열기` 상자를 같은 1열·같은 중앙 축으로 고정했습니다. 현재 Codex 패널에서 중심 차이 0px, 겹침 0px입니다.
- 현재 Codex 패널 `931×897`, DPR 2를 `codex-live-panel-regression`으로 추가했습니다. 실제 Stage는 `893.78×558.61`이며 핵심 계산판 너비는 Stage의 80.78%입니다.
- 설명 1 이미지는 `곱한 값을 넣어요 → 나머지를 더해요 → 처음 수와 같은지 봐요` 흐름으로 다시 만들었습니다. 세 번째 장면은 학생이 누르는 레버 없이 자동 결과만 보여 줍니다.

## 수학 계약

- 검산값은 `나누는 수×몫+나머지`입니다.
- 곱하고 더한 값과 처음 수의 같고 다름이 모델 값과 일치하며, 학생 입력 없이 자동으로 표시됩니다.
- 틀린 식은 몫과 나머지 가운데 정확히 한 부분만 다릅니다.
- 한 판에는 맞는 식과 틀린 식이 모두 포함됩니다.

## Humanizer 학생 문구 QA

- `먼저 무엇과 무엇을 곱할까요?`
- `그다음 무엇을 더할까요?`
- `처음 수는 마지막에 비교해요.`
- `몫은 나누는 수와 곱해요.`
- `나머지는 곱하지 않아요.`
- `몫 말고 나머지를 더해요.`
- `어느 수가 다를까요?`
- `몫 9`
- `나머지 3`
- `10문제를 풀면 금고를 열어요.`
- `열쇠 힘은 얼마나 나올까요?`
- `지금`
- `다음`
- `모든 금고를 열었어요`

각 문구는 한 문장에 행동 하나만 담고, 초3 학생이 소리 내어 바로 이해되는 말로 확인했습니다. `검산값` 대신 화면에 보이는 수와 행동을 쓰고, `핵심`, `메커니즘`, `게이트`, `토큰`, `오브젝트`, `부분곱` 같은 제작자 용어와 번역투를 학생 문구에서 쓰지 않았습니다. 짧은 문구라 패턴 분석은 제한적이지만 S1·S2 AI 문체 표지는 발견되지 않았고 자연도 A로 판정했습니다. 의미·수치·부정·격식 검증 6항도 통과했습니다.

## 텍스트 넘침·요소 겹침 QA

- 1280×800, 1024×768, 이번 정보 과밀 제보 조건 1024×640(DPR 2), 기존 제보 조건 1280×720(DPR 2), 현재 Codex 패널 931×897(DPR 2), 왼쪽 보상판 누락 제보 조건 1082×897(DPR 2)에서 전체 흐름을 완주했습니다.
- 문제 대기, 곱하기 관계 오답 2종, 곱하기 관계 정답 확인, 더할 수 대기·정답 확인, 다른 곳 찾기 대기·정답 확인, 맞는 식 자동 확인, 마지막 확인, 닫힌·열린 보상, 결과를 각 화면 크기에서 별도 캡처했습니다.
- 최종 결과: 겹침 0건, 넘침 0건, 깨진 이미지 0건, Stage 밖 요소 0건입니다.
- 문제 장면 세트: 3장, 실행 크기 1280×800, `problem-state-contact-sheet.png`
- 문제 중 보상 세트: 6장, 실행 크기 `600×1312`, `play-vault-world-contact-sheet.png`
- 결과 제목 세트: 6장, `result-title-contact-sheet.png`
- 브라우저 QA: 1280×800, 1024×768, 1024×640(DPR 2 정보 과밀 제보), 1280×720(DPR 2 기존 제보), 931×897(DPR 2 현재 패널), 1082×897(DPR 2 보상판 누락 제보)
- 곱하기 관계 오답 2종, 마지막 확인, 닫힌·열린 보상, 결과를 각 화면 크기에서 별도 캡처했습니다.
- Codex 회귀 증거: `screenshots/engine-flow-codex-browser-regression-05-lock-*-waiting.png`, `05-lock-*-confirm.png`, `05b-play-dividend-times-divisor.png`, `05b2-play-divisor-times-remainder.png`
- 마지막 확인의 빈 패널 0건, 보상 중복 행동어 0건을 실제 PNG로 확인했습니다.
- 실행 HTML 참조 자산의 Git 추적 누락은 0개입니다.

### 학습 우선순위 실제 렌더 측정

문제 첫 단계의 1순위는 현재 계산판, 2순위는 선택지, 3순위는 한 줄 지시문입니다. Stage 전체 배경은 면적 비교에서 제외했습니다.

| 화면 | 영역 | 우선순위 | 정보 묶음 수 | width×height | Stage 폭 비율 | Stage 면적 비율 | 판정 |
|---|---|---:|---:|---:|---:|---:|---:|
| 1280×800 | 현재 계산판 | 1 | 2 | 842.71×332.85 | 70.04% | 31.0% | 오른쪽에서 가장 큰 단일 학습 영역 |
| 1280×800 | 선택지 | 2 | 4 | 846.56×108.00 | 70.36% | 10.1% | 4개가 한 줄로 분리됨 |
| 1280×800 | 한 줄 지시문 | 3 | 1 | 846.56×52.00 | 70.36% | 4.9% | 현재 행동만 보이는 단일 판 |
| 1024×640 DPR 2 | 현재 계산판 | 1 | 2 | 656.77×259.41 | 69.34% | 30.4% | 왼쪽 금고를 가리지 않고 기준 통과 |
| 1024×640 DPR 2 | 선택지 | 2 | 4 | 663.30×108.00 | 70.03% | 12.8% | 최소 버튼 158.31×102.00 |
| 1024×640 DPR 2 | 한 줄 지시문 | 3 | 1 | 663.30×52.00 | 70.03% | 6.2% | 계산판·선택지와 겹침 0px |

## 통과한 검증

- `node scripts/qa-lesson-model.mjs 3-2-2-4-mathmon-check-lock` → `QA_LESSON_MODEL: PASS`
- `node scripts/qa-lesson2-check-lock.mjs 20260727` → `QA_LESSON2_CHECK_LOCK: PASS` (여섯 화면 크기 전체 흐름)
- `node scripts/check-stage-ratio.mjs` → 24개 차시 Stage 계약 통과
- `node scripts/check-rule-consistency.mjs` → 13개 문서·24개 차시 규칙 일치
- `node scripts/check-lesson-contract.mjs` → 이 차시 오류는 없었으나, 별도 `3-2-3-1`의 선언 자산 2개 누락과 `_shared` 소스 탐색 문제로 전체 명령 종료 코드 1
- `node scripts/check-lesson-visual-contract.mjs` → 이 차시 오류는 없었으나, 별도 `3-2-2-1` 문서의 매스몬 팩 ID 누락으로 전체 명령 종료 코드 1
- `./.venv/bin/python manage.py test mathmon_scoreboard --keepdb` → 2단원 네 차시 서버 점수 재계산·변조 거부 검사 통과
- `./.venv/bin/python manage.py check` → Django 시스템 오류 0건

현재 캡처는 이번 코드에서 다시 만든 `screenshots/engine-flow-*.png`입니다. 자동 종료 코드와 실제 화면 확인을 별도 증거로 남겼습니다.

## 전국 순위 비활성화

- 현재 제품 정책에 따라 `scoreboard.enabled`는 `false`입니다.
- 학생 흐름에 랭킹 문구·진입 버튼·순위 화면이 없고 점수 제출·조회 요청을 하지 않습니다.
- 예전 `09-scoreboard-*.png` 파일은 현재 실행 증거로 쓰지 않습니다.

## 남은 제품 선택

- P0·P1 남은 문제는 없습니다.
- 문제 중 `현재 금고 → 다음 금고`가 왼쪽 보상판에 연결되어 있고, 무지개 금고에서는 `모든 금고를 열었어요`로 끝납니다.
