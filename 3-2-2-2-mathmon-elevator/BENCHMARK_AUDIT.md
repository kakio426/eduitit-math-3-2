# 매스몬 엘리베이터 기준 차시 비교 감사

- 감사일: 2026-07-14
- 수정 확인: 2026-07-14
- 대상: `3-2-2-2-mathmon-elevator`
- 기본 비교군: `3-2-1-1-mathmon-box-run`, `3-2-1-2-mathmon-rocket-charge`, `3-2-1-3-mathmon-jump-islands`, `3-2-1-4-mathmon-fusion`
- 비교 기준: `AGENTS.md`, `CLAUDE.md`, `LESSON_COMMONS.md`, `SERIES_CONTRACT.md`, `.claude/skills/eduitit-mathmon-lesson/references/verification.md`

## 판정 표시

- `✅ 통과`: 현재 코드와 현재 화면 증거가 모두 맞음
- `🟡 일부 통과`: 기능은 있으나 비교 기준보다 약하거나 상태별 증거가 부족함
- `❌ 부족`: 강제 계약 위반
- `⏸ 선택`: 학습 핵심과 별개이고 제품 결정을 먼저 해야 함

우선순위는 `P0` 수학 오류·배움 부재, `P1` 학습 피드백·보상 원칙·증거 신뢰도 저하, `P2` 완성도 향상 순입니다.

## 비교군에서 가져온 강점

| 차시 | 기준으로 삼은 강점 | 엘리베이터 반영 |
|---|---|---|
| 1차시 상자런 | 정답/오답이 보상 손익으로 이어지고 한 문제 뒤 보상을 여는 구조 | 정답은 랜덤 힘, 오답은 정전 손해로 연결됨 `✅` |
| 2차시 로켓 | 자리값 계산을 단계로 나누고 하나의 연료 보상으로 목표까지 가는 흐름 | 십의 자리 → 내리기 → 일의 자리, 하나의 `올라갈 힘`으로 구성 `✅` |
| 3차시 점프섬 | 현재 위치와 다음·마지막 목표가 보이는 거리감 | 상단에 `지금 → 다음 → 끝` 층 진행표를 두고 힘·정답 수에 따라 갱신 `✅` |
| 4차시 로봇 합체 | 단계 정답 확인, 마지막 완성값, 상태별 캡처, 생성 이미지 세트 QA | 1.8초/1.6초 확인, 마지막 완성식, 3개 화면 크기 캡처로 반영 `✅` |

비교군을 그대로 복제하지 않았습니다. 예를 들어 1차시 문제 화면은 브랜드·단원 표기가 없고, 2차시는 문제 화면 단원 배지가 없으며, 3차시는 문제 화면 왼쪽 배지가 `에듀잇티 수학 게임`이 아닙니다. 엘리베이터의 현재 상단 구조가 이 항목에서는 더 강합니다.

## 1. 시리즈 골격·브랜드 체크

| 체크 항목 | 1-1 | 1-2 | 1-3 | 1-4 | 엘리베이터 판정 | 현재 증거 |
|---|---|---|---|---|---|---|
| 16:10, 1280×800 Stage | ✅ | ✅ | ✅ | ✅ | ✅ | `data-stage-ratio="16:10"`, `data-stage-size="1280x800"` |
| 생성형 커버 배경 | ✅ | ✅ | ✅ | ✅ | ✅ | `cover-generated.webp` |
| 생성형 제목 아트 | ✅ | ✅ | ✅ | ✅ | ✅ | `title-logo-generated.webp` |
| 생성형 시작 버튼 | ✅ | ✅ | ✅ | ✅ | ✅ | `data-cover-start-standard="generated-button-art"` |
| 설정 모달 표준 | ✅ | ✅ | ✅ | ✅ | ✅ | `data-settings-standard="modal-controls"` |
| 풀이·목표 설명 분리 | ✅ 2장 | ✅ 2장 | ✅ 카드 | ✅ 2장 | ✅ 2장 | `tutorial-page-1/2-generated.webp` |
| 문제 화면 왼쪽 브랜드 | ❌ 없음 | ✅ | 🟡 게임명 | ✅ | ✅ | `에듀잇티 수학 게임` |
| 문제 화면 단원 배지 | ❌ | ❌ | 🟡 차시 | 🟡 테마 | ✅ | `2단원 나눗셈` |
| 설정 버튼 예약 공간 | 🟡 구형 | ✅ | ✅ | ✅ | ✅ | 공통 상단 슬롯, 화면별 transform 없음 |
| 문제 수 진행 | ✅ | ✅ | ✅ | ✅ | ✅ | `1/10` |

## 2. 수학 순서·표현 체크

- [x] `✅ P0` 긴 나눗셈 순서가 `십의 자리 몫 → 곱한 값 → 빼기 → 남은 십 → 일의 자리 내려오기 → 다시 나누기`로 이어집니다.
- [x] `✅ P0` 몫 칸은 교과서처럼 피제수 위의 해당 자리 열에 놓입니다.
- [x] `✅ P0` `나누는 수×십의 자리 몫`과 빼는 선, 남은 수가 십의 자리 중심선에 맞습니다.
- [x] `✅ P0` 일의 자리 수는 같은 열에서 아래로 내려오고, 남은 수와 합쳐 내린 수를 만듭니다.
- [x] `✅ P0` 내림 단계의 답 칸은 일의 자리 한 칸이 아니라 십·일 두 열을 함께 감싸는 `합친 수` 상자입니다. 학생은 `2`가 아니라 `12`처럼 합친 수 전체를 고릅니다.
- [x] `✅ P0` 계산판의 자리 숫자와 학생이 고르는 실제 값을 구별합니다. 계산판은 `1`, 선택지는 `몫 10`입니다.
- [x] `✅ P0` 첫 단계 지시문은 `30에서`, `70에서`처럼 실제 수를 씁니다. `3십`, `7십`으로 말하지 않습니다.
- [x] `✅ P0` 선택지와 정답 확인도 `남은 수 10`, `몫 20`처럼 실제 값을 보여 줍니다.
- [x] `✅ P0` 동적 학생 문구에서 숫자+`십` 표기가 남아 있지 않습니다. 보고서의 과거 수정 설명만 검색됩니다.
- [x] `✅ P0` 모든 문제 후보는 남은 십이 1 이상이고 내린 수가 나누어떨어지는 수학 계약을 따릅니다.
- [x] `✅ P0` 마지막 몫은 `12 ÷ 2의 몫`처럼 내린 수와 나누는 수를 함께 보여 줍니다.
- [x] `✅ P0` 대표 오답은 남은 수 생략, 몫 과대·과소, 일의 자리만 사용, 너무 일찍 더하기/빼기를 포함합니다.
- [x] `✅ P1` 몫 과대와 몫 과소를 각각 결정적으로 재현해 세 화면 크기로 캡처했습니다. 과대는 `05b-play-wrong`, 과소는 `05b2-play-quotient-too-low`입니다.

## 3. 배움이 보이는 단계 흐름 체크

- [x] `✅ P0` 각 화면에서 학생 행동은 하나입니다: 두 값을 고르기, 내린 수 고르기, 마지막 몫 고르기.
- [x] `✅ P0` 반복 클릭만으로 끝나지 않고 수·자리·관계를 판단해야 합니다.
- [x] `✅ P0` 1단계 정답 뒤 계산판에 몫, 부분곱, 빼는 선, 남은 수가 실제로 나타납니다.
- [x] `✅ P0` 2단계 정답 뒤 내려온 일의 자리와 내린 수가 계산판에 나타납니다.
- [x] `✅ P0` 2단계 오답 뒤에도 학생이 고른 수 전체가 `합친 수` 상자에 들어가 `남은 십+일의 자리`와 바로 비교됩니다.
- [x] `✅ P0` 1단계 확인은 1.8초, 내림 확인은 1.6초 유지됩니다.
- [x] `✅ P0` 지시문과 피드백이 같은 슬롯을 번갈아 사용하며 동시에 겹치지 않습니다.
- [x] `✅ P0` `?` 몫 칸, 내려오는 화살표, 내린 수 칸이 피드백 패널에 가려지지 않습니다.
- [x] `✅ P0` 마지막 단계 뒤 `32 ÷ 2 = 16` 완성식을 먼저 보여 줍니다.
- [x] `✅ P0` 학생이 `엘리베이터 움직이기`를 눌러야 보상으로 넘어갑니다.
- [x] `✅ P1` 오답 뒤 선택한 카드가 붉게 바뀌고, 고른 몫은 실제 몫 칸에 들어갑니다.
- [x] `✅ P1` 몫 과대·과소는 곱한 값과 남는 수, 내림 오답은 고른 수와 `남은 십+일의 자리`, 마지막 몫 오답은 `나누는 수×고른 몫`을 계산판에서 보여 줍니다. 문구만으로 끝나는 대표 오답은 없습니다.

## 4. 글씨·정렬·겹침 체크

- [x] `✅ P0` 큰 문제의 숫자와 `÷` 사이에 고정 간격이 있고 흰 그림자가 없습니다.
- [x] `✅ P0` 나눗셈 가로선은 몫 칸 테두리와 떨어져 있습니다.
- [x] `✅ P0` 부분곱과 남은 수는 십의 자리, 내려온 수는 일의 자리 중심선에 맞습니다.
- [x] `✅ P0` 계산판은 이전보다 세로로 넓고, `합친 수` 상자는 두 자리 중심선을 모두 포함하면서 아래 지시문과 4px 이상 떨어져 있습니다.
- [x] `✅ P0` 선택지의 라벨과 값은 각 반쪽 카드 중앙에 한 묶음으로 정렬됩니다.
- [x] `✅ P1` 숫자는 tabular 숫자로 고정되어 값이 바뀌어도 폭이 크게 흔들리지 않습니다.
- [x] `✅ P1` 실제 렌더 검사 기준 큰 문제 40px, 계산판 숫자 32px, 지시문 14px, 선택지 값 21px 이상을 통과했습니다.
- [x] `✅ P1` 선택 카드와 아래 칸의 터치 영역은 42×42px보다 큽니다.
- [x] `✅ P0` 1280×800, 1024×768, 931×897에서 텍스트 넘침·요소 겹침·Stage 밖 잘림이 0건입니다.
- [x] `✅ P0` 931×897에서 문제 대기, 오답, 1단계 확인, 2단계, 3단계, 마지막 확인이 각각 캡처되어 있습니다.

핵심 화면 증거:

- `screenshots/engine-flow-codex-browser-05-play-step1.png`
- `screenshots/engine-flow-codex-browser-05b-play-wrong.png`
- `screenshots/engine-flow-codex-browser-05b2-play-quotient-too-low.png`
- `screenshots/engine-flow-codex-browser-05c-play-step1-confirm.png`
- `screenshots/engine-flow-codex-browser-05d-play-step2.png`
- `screenshots/engine-flow-codex-browser-05d2-play-down-wrong.png`
- `screenshots/engine-flow-codex-browser-05e-play-step3.png`
- `screenshots/engine-flow-codex-browser-05e2-play-ones-too-high.png`
- `screenshots/engine-flow-codex-browser-05e3-play-ones-too-low.png`
- `screenshots/engine-flow-codex-browser-06-confirm.png`

## 5. 진행감·보상·결과 체크

- [x] `✅ P0` 중심 보상은 `올라갈 힘` 하나입니다.
- [x] `✅ P0` 정답은 랜덤 보상을 열 기회를 주고, 오답은 정전 손해로 이어집니다.
- [x] `✅ P0` 증가, 큰 증가, 감소, 0, 급행, 무지개 특별 보상이 모두 모델에 있습니다.
- [x] `✅ P0` 최고 결과는 높은 힘·높은 정답 수 또는 매우 희귀한 특별 보상이 필요합니다.
- [x] `✅ P1` 닫힌 문을 학생이 직접 열어야 결과가 공개됩니다.
- [x] `✅ P1` 닫힌 보상 모달은 `어떤 힘이 나올까요?`와 버튼 `열기`로 역할을 나눠 같은 행동을 되풀이하지 않습니다.
- [x] `✅ P1` 보상 공개는 이미지와 `올라갈 힘 -9` 같은 변화량 하나로 전달됩니다.
- [x] `✅ P1` 결과는 생성 장면·생성형 결과 타이틀·생성형 다시 버튼과 동적 정답 수/힘으로 구성됩니다.
- [x] `✅ P1` 최저 결과는 `지하 정비층`이며 `0/10`, 힘 0 실제 결과 화면을 세 화면 크기로 캡처해 빈손 금지를 확인했습니다.
- [x] `✅ P2` 문제 상단에 `지금 지하 → 다음 1층 → 끝 꼭대기`가 보이고, 현재 힘·정답 수에 맞춰 현재와 다음 층이 갱신됩니다.
- [ ] `⏸ 선택` 전국 순위는 1단원 1~4차시에 있으나 엘리베이터는 `data-scoreboard-enabled="false"`입니다. 핵심 나눗셈 배움과 무관하고 백엔드 계약이 필요하므로 임의로 켜지 않습니다.

## 6. 화면 증거·문서 체크

- [x] `✅ P1` 엘리베이터 `index.html` 수정 시각은 2026-07-13 12:23, 931×897 상태별 캡처는 12:24 이후여서 현재 구현과 순서가 맞습니다.
- [x] `✅ P1` 표지, 설정, 설명 1·2, 문제, 오답, 단계 확인, 닫힌/열린 보상, 결과 캡처가 있습니다.
- [x] `✅ P1` `REPORT.md`에 1280×800, 1024×768, 931×897와 상태별 검증이 적혀 있습니다.
- [x] `✅ P1` 문제 상태 3장과 현재 결과 제목 6장의 컨택시트 경로가 보고서에 있습니다.
- [x] `✅ P0` `QA_LESSON_MODEL`, `QA_LESSON_FLOW`, 차시 계약, 시각 계약, Stage 비율 통과 기록이 있습니다.
- [x] `✅ P1` 차시 전용 `scripts/qa-lesson2-elevator.mjs`를 현재 source-driven 엔진의 모델·흐름 QA 진입점으로 교체했고 `LESSON2_ELEVATOR_QA: PASS`를 확인했습니다.
- [ ] `참고 제한` 1단원 비교군 중 1-1, 1-2, 1-3의 대표 문제 캡처는 현재 `index.html`보다 오래되었습니다. 구조 비교 자료로만 사용했으며 엘리베이터 UI 통과 증거로 계산하지 않았습니다.
- [x] `✅ P1` 몫 과대·과소, 2단계 오답, 3단계 몫 과대·과소, `0/10` 최저 결과를 1280×800·1024×768·931×897에서 결정적으로 재현했습니다.

## 최종 판정

- P0: `0건`
- P1: `0건` — 감사에서 찾은 5건 모두 수정·현재 캡처 확인
- P2: `0건` — 다음 목표·마지막 목표를 상단 층 진행표로 보강
- 선택: `1건` — 전국 순위 활성화 여부

현재 엘리베이터는 사용자가 지적한 긴 나눗셈 자리 배치, `÷` 기호 겹침, `7십` 표현, `몫 2/1` 혼동, 브랜드 누락, 정답 확인 전환 겹침과 이번 감사의 P1·P2 항목을 모두 해결했습니다. 남은 전국 순위는 학습 핵심과 별개이고 백엔드 계약이 필요한 제품 선택이므로 임의로 켜지 않습니다.
