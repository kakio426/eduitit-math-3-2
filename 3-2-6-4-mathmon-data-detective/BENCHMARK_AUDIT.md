# 3-2-6-4 기준 차시 비교 감사

검사일: 2026-08-01
최종 판정: **통과**

## 강점별 비교

| 기준 차시 | 가져온 강점 | 현재 증거 |
| --- | --- | --- |
| 1-1 상자런 | 풀이 뒤 직접 여는 보상 | 단서 관계 확인 뒤 단서 상자 열기 |
| 1-2 로켓발사 | 중심 보상 하나 | `탐정 힘`만 누적 |
| 1-3 점프섬 | 현재 행동만 크게 | 큰 값·작은 값·차이 중 하나만 질문 |
| 1-4 로봇 합체 | 여러 값을 마지막 관계로 연결 | 네 줄의 실제 값과 비교·차이 식 완성 |

## 강제 계약

한 화면 한 행동, 실제 값 판단, 큰 값 3·작은 값 3·차이 4, 대표 오개념, 정답 확인, 겹침 0, Stage-Reveal, 랭킹 비활성 모두 통과했습니다.

## 영역·입력 측정

| 화면 | 1순위 문제판 | 중심 작업판 | 선택지 | 판정 |
| --- | ---: | ---: | ---: | --- |
| 1280×800 | 896.39×465.98 | 858.39×384.98, Stage 폭 71.34% | 최소 210.09×56 | 통과 |
| 1024×768 | 742.22×353.94 | 716.22×291.69, Stage 폭 72.86% | 최소 172.55×52 | 통과 |

- 정보 묶음: 단위 1, 네 줄 자료 1, 현재 단서 1, 선택지 4
- 대기↔완료 경계·중심 차이 0px
- 정답 경로의 문제당 수학 판단 수 min/median/avg/max: `1/1/1/1`
- 정답 경로의 문제당 물리 입력 수 min/median/avg/max: `4/4/4/4`
- 표지·설명 이동을 포함한 한 판 입력: 43회

왼쪽 진행 보상은 Stage 기준 `left 1.65% / top 11% / width 19.2% / height 84%`의 고정 칸을 쓰며, 학습 영역과의 교차는 `0px`입니다. 최종 보상 6등급을 먼저 확정한 뒤 같은 여섯 세계를 잇는 전용 진행 이미지 6장을 만들었고, 결과 이미지 크롭 재사용은 없습니다.

## 오개념 증거

그림 수 차이만 계산하기, 큰 값 자체 고르기, 단위 하나 적게 계산하기, 단위 하나 많게 계산하기를 모델 검사로 확인했습니다. 작은·큰 차이 오답은 작업판의 실제 값과 고른 차이를 함께 보여 줍니다. 큰 값·작은 값 오답은 고른 줄과 실제 값을 남깁니다.

## 결론

6-1의 세기 활동과 겹치지 않고, 그림의 단위를 읽은 뒤 현재 단서 하나를 비교하는 단원 정점 역할을 합니다. 필수 계약 미달과 남은 P0/P1은 없습니다.
