# AI Mart 작업 메모

## 하네스/스킬 기준

- `AGENTS.md`는 Codex용 프로젝트 진입점이고, `CLAUDE.md`는 Claude용 프로젝트 진입점입니다. 두 파일의 프로젝트 규칙과 로드맵은 같은 내용으로 유지합니다.
- 차시 제작·계획·대형 화면 변경의 공정 기준은 프로젝트 전용 스킬 `.claude/skills/eduitit-mathmon-lesson/SKILL.md`입니다.
- 매스몬 캐릭터 생성·교체·팩 관리·카탈로그·결과 카드 이미지 연결의 공정 기준은 프로젝트 전용 스킬 `.claude/skills/eduitit-mathmon-assets/SKILL.md`와 계약 문서 `_shared/mathmon/MATHMON_ASSET_CONTRACT.md`입니다.
- 학생에게 보이는 한국어 문구를 만들거나 크게 바꾼 뒤에는 `humanizer` 스킬(`/Users/yubyeongju/.agents/skills/humanizer/SKILL.md`) 기준으로 AI 문체·번역투·어려운 한자어를 점검하고, 초3 학생이 소리 내어 읽어도 바로 이해되는 말로 고칩니다.
- Claude에서는 이 스킬이 프로젝트 스킬로 자동 인식될 수 있고, Codex에서는 현재 세션의 스킬 목록에 보이지 않아도 위 경로의 `SKILL.md`와 필요한 `references/` 문서를 직접 읽어 적용합니다.
- 루트 하네스 문서(`AGENTS.md`/`CLAUDE.md`)는 "왜·무엇을"의 기준입니다. 스킬은 "어떻게 만들고 검증할지"의 기준입니다.
- 반복되는 1-2-3차시 화면·보상·검증 공통 체크는 루트 `LESSON_COMMONS.md`를 보고 적용합니다.
- 스킬, `AGENTS.md`, `CLAUDE.md` 중 하나를 바꿀 때는 나머지 두 곳의 용어·배포 원칙·보상 원칙이 어긋나지 않는지 함께 확인합니다.

## 이 폴더의 의미

`/Users/yubyeongju/ai mart`는 에듀잇티 수학 게임 시리즈의 로컬 원본 작업실입니다. Codex와 Claude는 이 구조를 기준으로 차시별 게임을 만들고 정리합니다.

학생에게 보이는 화면에는 `AI Mart`라는 이름을 쓰지 않습니다. 내부 작업실 이름으로만 사용합니다.

## 기본 원칙

- 차시별 게임은 독립 실행 가능한 단일 HTML 패키지를 우선합니다.
- 학생 대상은 태블릿 가로와 컴퓨터 화면입니다.
- 스마트폰 세로 화면은 기본 지원 범위가 아닙니다.
- 첫 화면은 제목, 한 줄 목표, 시작 버튼이 가장 먼저 보여야 합니다.
- 첫 화면과 마지막 결과 화면은 이미지 생성 기반 RasterStage로 제작합니다. 결과 등급처럼 유한하게 바뀌는 라벨·칭찬·버튼 장식은 단계별 생성 이미지/타이틀 자산으로 처리하고, 정답 수 `0/10`~`10/10`처럼 유한한 동적 값도 공용 생성 이미지 세트로 처리합니다. HTML/SVG 오버레이는 점수·연료·힘처럼 값 범위가 넓은 정보와 접근성용 hitbox만 맡깁니다.
- 모든 차시는 `16:10` Stage, 기준 제작 크기 `1280×800`을 지킵니다. `index.html`의 `<main class="game">`에는 `data-stage-ratio="16:10"`과 `data-stage-size="1280x800"`을 두고, `.stage-shell`이 contain 폭과 비율을 담당하게 합니다.
- 첫 화면 커버 표준은 `data-cover-standard="generated-title-overlay"`입니다. `cover-generated.webp`는 글자 없는 대표 장면 배경으로 `.raster-bg`에 `object-fit: cover`로 깔고, 게임명은 생성형 이미지 제목 자산(`title-*-generated.webp`)을 `.hero-title-art`로 얹으며, 한 줄 목표는 보이는 HTML 오버레이로 둡니다. 새 차시와 생성형 시작 버튼으로 이관한 차시는 `data-cover-start-standard="generated-button-art" data-cover-start-asset="shared-canonical-v1"`를 붙이고, 공용 생성형 버튼 자산(`../_shared/mathmon/cover-start-button/start-button-generated.webp`)만 둡니다.
- 첫 화면에 매스몬이 필요하면 `cover-generated.webp` 생성 프롬프트 단계에서 배경·소품·조명과 같은 장면 안에 함께 포함합니다. 기존 매스몬 PNG/WebP를 `.cover-mathmon` 같은 별도 `<img>`로 얹거나 배경 생성 뒤 로컬로 붙이는 방식은 새 차시와 리마스터에서 금지합니다. 첫 화면에서 별도 오버레이로 허용되는 것은 제목 아트, 한 줄 목표, 생성형 시작 버튼 아트, 접근성용 hitbox뿐입니다.
- 기존 `generated-title-overlay` 차시 중 `.primary-button`으로 시작하는 커버는 개별 이관 전까지 `compatibility-primary-button` 상태로 보존합니다. 이 호환 상태는 기존 차시 실행을 위한 예외이며 새 차시 복제 기준이 아닙니다.
- `cover-generated.webp` 한 장 안에 제목·목표·시작 버튼을 baked-in 하거나, 커버 전체 투명 `cover-start-hitbox`로 시작시키는 방식은 새 차시에 쓰지 않습니다. 기존 1차시처럼 아직 이전 방식인 화면만 `data-cover-standard="legacy-raster-poster"`로 명시하고, 복제 기준으로 삼지 않습니다.
- 설정/소리 같은 전역 조작 버튼은 Stage 밖 viewport에 fixed로 띄우지 않고, `.stage-shell` 안의 상단 오른쪽 보조 슬롯에 작게 둡니다. `.stage-shell`의 `--sound-button-size`, `--sound-gap`, `--sound-reserve`로 위치와 예비 공간을 고정하고, `.settings-toggle`/레거시 `.sound-toggle`에는 화면별 `transform`/active-screen별 위치 보정을 쓰지 않습니다. `top-row`/`hud`는 같은 `--sound-reserve`만큼 비워 버튼이 배지·문제·선택지를 가리지 않게 합니다.
- 새 차시와 큰 수정 차시는 `<main class="game" data-settings-standard="modal-controls">`를 선언하고, 오른쪽 위 버튼은 보이는 텍스트 없는 원형 SVG 톱니바퀴 `.settings-toggle`로 둡니다. 설정 모달에는 `배경 소리`, `효과 소리`, `방법 다시 보기`, `처음부터`, `닫기`를 짧게 배치하고, BGM/SFX localStorage 키는 `mathmon-audio-bgm-enabled`, `mathmon-audio-sfx-enabled`를 씁니다.
- 상단 배지·단원 pill·설정/소리 버튼은 같은 기준선에 놓습니다. `.stage-shell`의 `--top-control-y`를 단일 세로 좌표로 두고 `.settings-toggle`, `.top-row`, 문제 화면 `.hud`가 같은 값을 씁니다. `.top-row`는 `top/left/right`와 `height: var(--sound-button-size)`, `gap: var(--sound-gap)`을 명시하고 `inset` 축약을 쓰지 않습니다. 문제 화면 `.hud`는 `align-items: start`와 `min-height: var(--sound-button-size)`를 둡니다. 새 차시와 큰 수정 차시는 `lesson.json > qa.topControlsAudit`에 단원 배지·설정 버튼 선택자와 위·아래·중심·높이 허용 오차 `1px`, 최소 가로 간격 `8px`을 선언하고 공용 브라우저 하네스로 실제 rect를 검사합니다.
- 브랜드/단원/상태 배지는 내용보다 오른쪽으로 늘어나면 실패입니다. `flex: 0 0 auto`, `width: fit-content`, `max-width: max-content`, 작은 좌우 패딩 변수(`--top-control-pad-x`)와 gap 변수(`--top-control-icon-gap`)를 쓰고, 안정 슬롯이 꼭 필요하지 않으면 `min-width`로 빈 공간을 만들지 않습니다.
- 차시를 만들거나 화면을 크게 바꾼 뒤에는 루트에서 `node scripts/check-stage-ratio.mjs`를 실행해 Stage 비율 계약을 통과해야 합니다.
- 문제와 선택지는 장식보다 항상 우선합니다.
- 보상은 하나의 중심 구조로 유지합니다. 여러 보상 체계를 동시에 전면 배치하지 않습니다.
- 새 차시는 `reward.standard="mathmon-unified-reward-v1"`을 쓰고 내부 누적값을 `0~100`으로 통일합니다. 정답 사건은 `64%/+6~10`, `15%/-5~-2`, `12%/+14~22`, `5%/+30`, `3.8%/0(누적 유지)`, `0.2%/100(특별)`이며, 오답은 문제당 최초 1회 `-6~-3`입니다. 일반 결과 기준은 `(점수/최소 정답 수) 0/0, 15/2, 35/4, 55/6, 78/8`, 특별 결과는 특별 사건이 나온 `100/1`입니다. 기존 차시는 사용자가 이관을 요청할 때만 바꿉니다.
- 새 차시와 보상 화면을 크게 고치는 차시는 작은 중앙 모달보다 `마지막 정답 확인 → 닫힌 보상 → 사건 공개 → 현재 단계·다음 목표 → 다음 문제/결과`의 `Stage-Reveal` 흐름을 기본으로 합니다. `empty`는 누적값을 비우지 않고 이번 변화만 0으로 처리하며, 상세 기준은 `.claude/skills/eduitit-mathmon-lesson/SKILL.md`의 「기본 Stage-Reveal 보상 계약」을 따릅니다.
- 사용자가 기존 단원과 같은 중앙 모달을 명시하면 `reward.mode="modal-art"` 예외를 허용합니다. 이때 문제 화면을 유지한 `rewardPop → reward-card → reward-visual → 열기/다음` 구조를 쓰고, `unit3-modal-art-v1` 카드 `560×480px(7:6)`, 이미지 슬롯 `250×250px`, 카드 최대 폭 Stage `88%`를 고정합니다. 카드↔Stage 중심·카드 크기·이미지 크기 오차를 각각 1px 이하로 재며 글자 넘침·요소 교차 0과 상태별 행동 버튼 1개를 하네스로 검사합니다. 표시 방식만 바꾸는 기존 차시는 보상 확률·증감·결과 기준까지 함께 이관하지 않습니다.
- 모달 확인 뒤 진행 장면이 바뀌는 차시는 `modal-dismiss-world-impact-v2`를 씁니다. 모달이 열린 동안 뒤 이미지·단계·효과를 바꾸지 않고, 모달을 완전히 숨긴 뒤 `250~450ms`의 시선 이동 여백을 둡니다. 이후 단계 이미지 교체와 Stage 폭 `32%` 이상으로 번지는 강한 빛·충격파를 최소 `1200ms` 보여 준 다음에만 다음 문제나 결과로 이동합니다. 하네스는 고정 단계 상승 fixture로 모달 선행 닫힘, 실제 지연 시간, 전후 이미지·단계 변경, 효과 면적, 전용 효과 클래스, 최소 표시 시간 동안 문제 번호 고정을 검사합니다.
- 문제 화면 왼쪽 진행 보상은 `stage-left-play-progress-v1`로 Stage 기준 `left/top/width/height` 비율을 선언하고 지원 viewport마다 같은 좌표를 씁니다. 가로폭은 `stage-left-progress-width-v1`에 따라 Stage의 `23.4375~25.2%`, 새 차시·리마스터 권장값 `24.5%`, 학습 영역과의 최소 간격 `1.5625%`를 씁니다. `height:auto`나 `max-height`만으로 세로 길이가 줄어들게 두지 않습니다. 6단계 결과는 전용 플레이 이미지 6장과 1:1로 연결하며, 여섯 장 모두 같은 카메라·매스몬 중심·크기·발 기준선, 전신 잘림 0건을 유지합니다. 실제 패널 네 변 오차 1px 이하와 학습 영역 교차 0px를 브라우저 하네스로 검사합니다.
- 단계형 문제에서 정답을 고르면 다음 단계나 보상 모달로 바로 넘기지 않습니다. 클릭한 선택지 색만 바뀌는 것은 부족합니다. 현재 계산판·칸·블록이 정답값으로 바뀌고, 짧은 한 줄 확인 문구가 보인 뒤 넘어가야 합니다. 마지막 단계도 보상 모달 전에 완성식이나 완성값을 충분히 보여 줍니다. 가능하면 학생이 확인 뒤 `바람 보기`, `상자 열기`처럼 차시 소재에 맞는 버튼을 눌러 보상으로 넘어가게 합니다. 모달이 정답 확인 화면을 가리면 실패입니다.
- 문구는 초등학생이 소리 내어 읽어도 자연스럽게 씁니다.
- 보상/피드백 모달은 버튼을 제외하고 보이는 텍스트를 한 덩어리로 줄입니다. 기본은 큰 결과 라벨 또는 변화량 1개만 보이고, 설명 문장은 숨기거나 제거합니다.
- 같은 뜻의 말을 한 모달 안에서 반복하지 않습니다. 예: `출하!`와 `출하 보기`, `결과`와 `결과 보기`, 제목·본문·버튼의 같은 단어 반복은 실패로 봅니다.
- 화면 구현이 끝난 뒤에는 **Humanizer 학생 문구 QA**를 반드시 한 번 거칩니다. 대상은 첫 화면 목표 문장, 설명 화면 단계, 문제 지시문, 선택지, 힌트, 피드백, 보상 모달, 결과 칭찬, 다운로드 카드 문구입니다.
- Humanizer QA에서는 `핵심`, `생산량`, `출하`, `측정`, `등급`, `적용`, `이벤트`, `토큰`, `오브젝트`, `게이트`처럼 제작자나 어른에게는 편하지만 학생에게는 멀게 느껴지는 말을 먼저 의심합니다. 화면 안 물건과 행동으로 바꿀 수 있으면 `0 뗀 곱`, `공장 힘`, `상자 출발`, `어디까지 갔는지 보기`처럼 바로 보이는 말로 바꿉니다.
- 문구는 짧아야 합니다. 한 문장에는 행동 하나만 넣고, 학생이 소리 내어 읽었을 때 숨이 차거나 교사용 설명처럼 들리면 실패입니다.
- 첫 화면 제목은 단순 큰 글자가 아니라 **타이포그래픽 포스터/타이틀 아트**로 다룹니다. 사용자가 `그림으로`, `GPT Image`, `제목 이미지`를 요구하면 기존 첫 화면 배경은 유지하고, 제목 부분만 독립 래스터 오버레이(`title-logo-generated.webp` 또는 `title-poster-generated.webp` 같은 배포 자산)로 생성해 얹습니다. 전체 커버 이미지를 갈아엎거나 HTML/CSS/SVG로 어설프게 흉내 내지 않습니다. 접근성용 실제 제목은 `visually-hidden`으로 남깁니다.
- 첫 화면 제목 자산은 반드시 GPT Image/imagegen 같은 생성형 이미지 산출물이어야 합니다. 로컬 폰트, Pillow, canvas, SVG, CSS text-shadow로 만든 "그림처럼 보이는 텍스트"는 실패입니다. 생성 원본(`title-*-source.png` 또는 `title-*-chromakey.png`)과 배경 제거 PNG/WebP를 함께 보관합니다.
- 시작 버튼은 공용 생성 자산 `mathmon-cover-start-button-v1`만 반복 사용합니다. 원본·PNG·WebP는 `_shared/mathmon/cover-start-button/`에 한 세트로 보관하며, 새 차시·큰 커버 수정에서 차시별 새 생성, 복제, 색 변경, 재가공은 금지합니다. 브랜드 변경처럼 공용 버튼 자체를 바꿀 때만 사용자의 명시적 승인 뒤 공용 세트를 교체합니다. 실제 클릭은 같은 크기의 `<button class="cover-start-button" id="startButton" aria-label="시작">`이 맡고, 표시 규격은 1280×800 Stage에서 `360×152px`, 작은 화면 최소 `300×127px`, `aspect-ratio: 1611 / 680`으로 고정합니다. 목표 아래 `14-24px` 간격을 기본으로 하며, 브라우저에서 버튼 아트와 hitbox 경계가 반드시 일치해야 합니다.

## UI 텍스트 넘침·요소 겹침 금지 (강제)

앞으로 모든 차시의 화면은 "글자가 자기 상자 안에 들어가 있는가?", "요소끼리 서로 덮지 않는가?"를 통과해야 합니다. 통과하지 못하면 완성 화면이 아니라 실패 화면으로 보고 레이아웃이나 문구를 다시 줄입니다.

- 버튼, 카드, 배지, 선택지, 보상 모달, 결과 카드 안의 한글·숫자·기호는 상자 밖으로 튀어나가면 실패입니다. 잘림, 삐져나옴, 줄 사이 겹침도 실패입니다.
- 문제, 선택지, 설정/소리 버튼, 브랜드/단원 배지, 진행 상태, 보상 버튼, 다음 단계 조작부가 서로 덮이면 실패입니다. 의도한 오버레이가 아니라 읽기나 클릭을 방해하면 무조건 다시 배치합니다.
- 고정 포맷 UI(지도, 보드, 선택지 그리드, 배지 줄, 버튼 줄, 결과 카드)는 `aspect-ratio`, 명시적인 grid/flex track, `min/max-width`, `min/max-height`, 예약 공간(`--sound-reserve` 등)으로 크기를 안정시킵니다. hover, 정답/오답, disabled, 긴 숫자, 긴 문구 때문에 카드나 버튼 크기가 흔들리면 실패입니다.
- 문제 대기에서 정답 확인·완료로 상태가 바뀌어도 핵심 계산판이 다른 열로 밀리거나 폭이 갑자기 줄면 실패입니다. 완료식·다음 행동 상자는 핵심 계산판과 같은 중앙 축과 작업 영역 폭을 유지하고, 전용 QA에서 좌우 경계 차이와 중심 차이를 `1px` 이하로 확인합니다.
- 긴 문구는 CSS로 억지로 밀어 넣지 말고 먼저 학생 말로 짧게 줄입니다. 그래도 길면 상자 안에서 자연스럽게 줄바꿈되게 하고, `overflow-wrap`, 적절한 `line-height`, `max-width`를 둡니다.
- 글자를 맞추려고 viewport 폭 기준으로 글자 크기를 계속 줄이는 방식은 쓰지 않습니다. 글자 크기는 화면 목적에 맞게 정하고, 공간이 부족하면 레이아웃·문구·정보량을 줄입니다.
- 스크린샷 QA는 컴퓨터 화면과 태블릿 가로 화면을 모두 봅니다. 첫 화면, 설명, 문제 1단계, 문제 2단계, 보상 모달, 결과 화면에서 텍스트 넘침·요소 겹침·클릭 영역 충돌이 0개여야 합니다.
- 사용자가 겹침·넘침·잘림을 발견한 화면은 일회성 참고가 아니라 영구 회귀 테스트입니다. 실제 `innerWidth×innerHeight`, DPR, Stage rect를 기록하고 해당 차시 `lesson.json > qa.viewports`와 전용 QA에 이름을 붙여 추가합니다. 비슷한 크기로 대신하거나 기본 화면만 다시 보고 PASS 처리하지 않습니다.
- 자동 겹침 검사는 글자·버튼 rect만 보지 않습니다. SVG/Canvas 계산판의 바깥 표면, 문제 카드, 지시문, 피드백, 선택지·숫자판, 모달과 hitbox의 실제 rect를 재고 형제 영역 교차 0px를 확인합니다. SVG 요소 상자는 맞아도 내부 배경 도형이나 글자가 밖으로 나가면 실패입니다.
- 문제 대기·오답 양방향·각 단계 정답 확인·다음 단계 대기·마지막 확인·닫힌/열린 보상·결과를 실패 viewport에서 각각 다시 검사합니다. z-index로 겹친 부분을 가리는 것은 수정이 아닙니다.
- `REPORT.md`에는 "텍스트 넘침·요소 겹침 QA"를 남기고, 어떤 화면 크기와 어떤 화면 상태를 확인했는지 적습니다.

## 상자 안 숫자·수식·기호 중심 정렬 (강제)

- 카드·칸·슬롯 안의 숫자, 분수, 수식, 비교·연산 기호는 실제로 보이는 내용의 가로·세로 중심이 상자 중심과 각각 `1px` 이내여야 합니다. 작성 좌표, CSS line box, SVG `<g>` 또는 `foreignObject` 외곽만 맞으면 실패입니다.
- 상자 rect를 `B`, 보이는 내용 rect를 `V`라 할 때 `dx = abs((V.left + V.right) / 2 - (B.left + B.right) / 2)`, `dy = abs((V.top + V.bottom) / 2 - (B.top + B.bottom) / 2)`를 실제 브라우저에서 기록합니다. 둘 중 하나라도 `1px`을 넘으면 다시 배치합니다.
- MathLive·MathCanvas 수식은 `math-field` 호스트가 아니라 열린 Shadow DOM 안의 `.ML__mfrac`, `.ML__base`, `.ML__latex`처럼 실제 글리프를 감싼 가장 안쪽의 보이는 rect를 우선 잽니다. 내부에 접근할 수 없을 때만 `math-field` 내용 rect를 차선 증거로 쓰고 제한을 기록합니다. 분자는 분수선·분모와 같은 세로축에 놓여야 하며, `<`, `>`, `=`, `+`, `−`, `×`, `÷`도 같은 기준을 적용합니다.
- 한 자리·두 자리 수와 문제 은행의 최솟값·최댓값, 기본·hover·focus·정답·오답·잠금 상태에서 중심이 흔들리거나 내용이 상자 밖으로 나가면 실패입니다. 자유 드래그 도중은 제외하되 시작 위치와 시스템이 확정한 배치 위치는 검사합니다.
- 내부 여백이나 baseline 때문에 치우치면 수식 객체의 실제 폭·높이와 시작 좌표를 고칩니다. 임의 margin, viewport별 눈대중 보정, overflow로 가리는 처리는 인정하지 않습니다.
- `REPORT.md`에는 확인한 viewport·상태별 `dx`·`dy` 최댓값과 잘림 `0건`을 남깁니다.

## 문구 길이·역할별 정렬 (강제)

- 숫자·분수·수식·비교·연산 기호는 항상 위의 중심 정렬 기준을 적용합니다.
- 이름표·상태칩·짧은 선택지·짧은 버튼처럼 `2어절 이하`이면서 공백을 뺀 글자가 `10자 이하`인 짧은 문구는 가운데 정렬합니다. 한 줄 라벨은 가로·세로 모두 실제 보이는 글자 중심과 상자 중심 오차가 각각 `1px` 이하여야 합니다.
- 지시문·피드백·도움말처럼 `3어절 이상`, 공백을 뺀 글자가 `11자 이상`, 또는 문장형 종결 표현을 가진 문구는 왼쪽 정렬합니다. 모든 줄은 정해진 왼쪽 안쪽 여백에서 시작하고 상자 밖 넘침·잘림이 없어야 합니다.
- 짧아도 행동을 설명하는 문장은 왼쪽, 길어도 고유 이름 하나인 제목 아트는 해당 컴포넌트 계약을 따릅니다. 길이와 의미가 충돌하면 의미를 우선하고 `data-text-align-role="label|sentence"`로 역할을 선언합니다.
- 이 기준은 가로 정렬 규칙입니다. 한 줄 라벨은 세로 가운데, 여러 줄 문장은 정한 위쪽 안쪽 여백과 줄 간격을 유지합니다.

## 맞추기 어려운 요소의 하네스 승격 (강제)

- 폰트 bearing·baseline, MathLive Shadow DOM, SVG `transform`, `foreignObject`, `preserveAspectRatio`, DPR·Stage 축소, 두 자리 이상 동적 값, 드래그 스냅 전후, 투명 hitbox처럼 눈대중으로 맞추기 어려운 항목은 일회성 보정으로 끝내지 않습니다.
- 같은 대상을 두 번 이상 좌표 보정했거나 viewport별 예외값이 필요해지면 `정렬 계약 → 실제 rect 측정 함수 → 실패 fixture → 자동 회귀 테스트 → 브라우저 캡처`를 스킬과 차시 QA에 추가합니다.
- 상자·보이는 글리프·클릭 hitbox가 함께 있는 요소는 세 rect를 따로 재고, 기본·hover·focus·선택·드래그 완료·정답·오답·잠금 상태와 한 자리·두 자리·최솟값·최댓값을 전수 확인합니다.

## 교과서식 수학 표현·전환 상태 QA (강제)

학생이 계산 순서를 잘못 배우거나, 정답 확인이 화면 전환에 가려지는 문제를 막기 위한 강제 기준입니다. 코드와 문서가 맞아도 실제 화면이 다르면 실패로 판정합니다.

- 세로셈·나눗셈·분수·단위 변환처럼 교과서에 익숙한 공간 규칙이 있는 계산은 학교에서 쓰는 순서와 자리 정렬을 따릅니다. 몫·곱한 값·빼는 선·남은 수·내려오는 수가 어느 열에 놓이는지 실제 교과서 풀이와 대조합니다.
- 계산판 안의 **자리 숫자**와 학생이 고르는 **실제 값**을 구별합니다. 예를 들어 십의 자리 몫 칸에는 `1`을 놓을 수 있지만, 선택지·지시문·확인 문구에는 `몫 10`처럼 실제 값을 보여 줍니다. `7십`, `2십`처럼 평소 학생이 쓰지 않는 표기는 `70`, `20`으로 씁니다.
- 수 표현과 학생 문구는 한 문제만 보지 않고 문제 은행 전체를 검사합니다. 동적으로 만들어지는 지시문·선택지·오답 피드백·정답 확인에서도 같은 규칙이 지켜져야 합니다.
- 문제 화면 왼쪽 위에는 `에듀잇티 수학 게임`, 같은 상단 줄에는 단원 배지를 둡니다. 표지에만 브랜드를 넣고 문제 화면에서 빠뜨리면 실패입니다.
- 오답, 정답 확인, 다음 단계 전환, 마지막 완성값처럼 잠깐 보이는 상태도 독립 화면으로 봅니다. 각 상태에서 지시문·피드백·`?` 칸·숫자·화살표가 서로 가리지 않아야 하며, 학생이 변화를 읽을 시간을 둡니다.
- 정답 확인에서는 고른 답이 계산판·칸·물건에 들어간 결과를 먼저 보여 줍니다. 다음 단계나 보상 모달이 이 확인 상태를 즉시 덮으면 실패입니다.
- 글씨 크기는 디자인 토큰만 보지 않고 실제 렌더에서 잽니다. 문제·계산판의 핵심 숫자, 지시문, 선택지 값, 작은 라벨의 위계를 확인하고, 터치 영역은 최소 `42×42px`을 확보합니다. 공간이 부족하면 글자부터 줄이지 말고 문구·패널·장식을 줄입니다.
- 숫자는 같은 자리에서 흔들리지 않게 tabular 숫자 또는 고정 슬롯을 쓰고, `÷`, `×`, `−`, 나눗셈 괄호선과 숫자의 간격·기준선을 스크린샷으로 확인합니다.
- QA는 `1280×800`, `1024×768`뿐 아니라 사용자가 문제를 발견한 브라우저 크기와 비슷한 화면에서도 진행합니다. 표지·설명·문제 대기·대표 오답·각 단계 정답 확인·다음 단계·마지막 확인·닫힌/열린 보상·결과를 현재 실행본에서 따로 캡처합니다.
- 저장된 스크린샷이나 `REPORT.md`가 현재 코드보다 오래되었으면 현재 증거로 인정하지 않습니다. 실제 실행 화면과 현재 코드가 단일 기준이며, `REPORT.md`에는 캡처한 상태·화면 크기·남은 문제를 적습니다.

## 유한 상태 생성 이미지 세트 QA (강제)

상단 목표 지도, 결과 등급, 보상 점수, 정답 수, 시작 버튼 변형처럼 정해진 개수의 생성 이미지가 한 UI 슬롯에서 바뀌는 경우는 **상태 이미지 세트**로 취급합니다. 이 세트는 한 장만 예쁘면 통과가 아니라, 모든 장이 같은 계약을 만족해야 통과입니다.

- 작업 전에 세트 계약을 먼저 적습니다. 예: `6장`, `승인된 캔버스/표시 슬롯 치수`, `각 장 로봇 슬롯 6개`, `현재 로봇 1개만 컬러`, `나머지 5개는 실루엣`, `상하 잘림 없음`, `가로 배치 유지`.
- 사용자가 이미지 크기나 배너 높이를 말하면 그 수치를 우선합니다. 중간에 높이나 슬롯을 바꿔야 한다고 판단되면 먼저 이유를 말하고, 사용자가 마지막으로 확정한 숫자로 세트 계약을 갱신한 뒤 생성합니다. 임의로 240px처럼 키우거나, CSS로 이미지를 눌러 맞추지 않습니다.
- 생성 뒤에는 파일 치수만 보지 않습니다. 모든 상태 이미지를 실제 표시 크기와 같은 비율의 컨택시트로 묶어 눈으로 확인합니다. 컨택시트에는 파일명, 크기, 상태명이 보여야 합니다.
- N장 세트는 N장 모두에서 필수 개수가 보여야 합니다. 예: 로봇 6단계 배너라면 6장 모두 로봇 슬롯 6개가 보여야 하며, 한 장이라도 5개만 보이면 세트 전체 실패입니다.
- 최종 결과 단계 세트는 `result-tier-fullscene-native-v1`로 선언하고 각 장에 `visualRank: 0..N-1`과 서로 다른 1280×800 완성 장면을 1:1로 연결합니다. 배경·매스몬·보상물·마법 효과·환경 조명·고정 제목·빈 동적 판·버튼 표면은 한 생성 이미지 안에 있어야 합니다. 기존 배경에 별도 효과 이미지나 DOM 레이어를 얹거나 `mix-blend-mode`, 단계별 CSS `filter`·`opacity`·`hue`로 차이를 만들면 실패입니다. `forbidEffectOverlay`, `forbidBlendMode`, `forbidTierCssFilter`를 계약과 브라우저 하네스에서 강제합니다. 하네스는 일반 DOM뿐 아니라 결과 루트 `::before`·`::after`의 computed `content`, 배경, opacity, filter, blend mode도 따로 검사합니다. 인접 단계는 효과 크기·입자 밀도·문양 복잡도·색 계열·매스몬 반응·환경 변화 중 두 가지 이상이 장면 자체에서 달라야 하며, 최상위 두 단계는 금빛·무지개처럼 색 계열 자체가 달라야 합니다.
- 결과판의 동적 값·진행 막대·정답 수·다음 목표·다시하기는 1280×800 Stage 기준 슬롯 rect와 공통 축을 `lesson.json`에 선언합니다. 실제 글리프·이미지·hitbox rect는 슬롯 안에 있어야 하고 선언 좌표와 네 변 오차 각각 `1px` 이하, 형제 교차 `0px`를 모든 결과 단계와 모든 `qa.viewports`에서 통과해야 합니다.
- 생성 이미지마다 빈 결과판 위치가 달라질 수 있는 완성 장면 세트는 JSON 좌표끼리만 비교해 통과시키지 않습니다. 브라우저 하네스가 실제 `resultBg` 픽셀에서 결과판 경계와 중심을 검출하고, 상태별 선언 축·동적 글리프·막대·정답 이미지 중심이 검출 중심과 `3px` 이내인지 전 장에서 확인합니다. 위치가 다른 상태는 `axisXByTier`처럼 상태별 축을 명시하며, 전역 축 하나로 억지로 맞추거나 viewport별 CSS 보정값을 두면 실패입니다.
- 결과 단계 세트는 자산 컨택시트와 실제 브라우저 결과 컨택시트를 따로 만들고, 데스크톱·태블릿 가로에서 모든 단계를 전수 확인합니다. 대표 한 장만 캡처하면 완료가 아닙니다.
- 같은 슬롯의 상태 이미지는 가로 위치, 세로 기준선, 주인공 크기, 여백, 조명 방향이 서로 크게 흔들리면 실패입니다. 한 장만 다른 구도이거나 좌우가 줄어들어 찌그러져 보이면 다시 만듭니다.
- 문제 화면의 현재 보상·진행 장면은 최종 결과 이미지를 `object-fit: cover`나 `object-position`으로 잘라 재사용하지 않습니다. 최종 결과는 결산 장면이고 플레이 상태는 진행 증거이므로 N단계면 N장의 전용 생성 이미지 세트를 따로 만듭니다. 모든 장은 같은 슬롯·카메라·주인공 크기·기준선을 쓰고, 화살 명중 위치·완성물·불빛·캐릭터 반응처럼 단계가 오른 증거가 극명해야 합니다. 각 플레이 상태를 대응 최종 결과와 1:1로 비교하고, 주인공 전신과 핵심 보상물 잘림 0건, `object-fit: contain`, 컨택시트 전수 확인을 통과해야 합니다. CSS 크롭으로 캐릭터 일부를 숨기면 실패입니다.
- 세로가 부족하면 이미지 폭을 줄이거나 `object-fit: fill`로 누르지 말고, 배너 슬롯 높이와 아래 레이아웃 높이를 다시 배분합니다. 위 검정 빈칸, 아래 흰 줄, 회색 여백처럼 이미지 가장자리 결함이 보이면 실패입니다.
- 이미지 세트를 runtime에 연결하기 전 `naturalWidth/naturalHeight`, 실제 렌더 크기, `object-fit`, `aspect-ratio`, 캐시 버전을 브라우저에서 확인합니다. 파일이 맞아도 브라우저에서 잘리거나 눌리면 실패입니다.
- 문제 화면에 들어가는 상태 세트는 실제 시작 흐름으로 문제 화면까지 진입해 컴퓨터 화면과 태블릿 가로 화면을 캡처합니다. 보기, 버튼, 상단바, 좌우 패널이 함께 살아 있는 상태에서 확인합니다.
- `README.md`와 `REPORT.md`에는 현재 runtime 기준 크기, 세트 개수, 컨택시트 경로, 브라우저 QA 화면 크기를 적습니다. 이전 실패 자산 크기와 현재 기준 크기를 섞어 쓰지 않습니다.

## 정밀 동적 UI 화면 패턴 (전국 순위판 등)

> 현재 제품 정책: 사용자가 계획 변경을 명시하기 전까지 3학년 2학기 1~6단원에는 랭킹·전국 순위 문구, 진입 버튼, 순위 화면, 점수 제출·조회 API 연동을 만들거나 노출하지 않습니다. 기존 코드가 남아 있어도 기본값은 비활성화하고 학생 흐름과 네트워크 요청에서 차단합니다. 새 차시·리마스터·복제에서도 랭킹을 다시 붙이지 않습니다.

전국 순위판, 대시보드, 표, 여러 행 목록처럼 매 판 달라지는 값이 많고 정확한 정렬이 필요한 화면은 생성 이미지 안 박스에 글자를 맞추지 않습니다.

- 생성 이미지는 축하 배경, 무대, 불꽃, 매스몬 장식, 분위기만 맡깁니다. 프롬프트에는 `no text`, `no letters`, `no numbers`, `no UI panels`, `no leaderboard board`, `no cards`, `no table rows`, `no buttons`, `no empty boxes`, `no labels`를 명시합니다.
- 순위판, 내 기록 박스, 행 배경, 스크롤 영역, 버튼 표면, 보이는 글자는 하나의 SVG `viewBox="0 0 1280 800"` 컴포넌트가 그립니다. 배경 이미지와 UI를 같은 SVG 좌표계 안에 두어 Stage가 줄어도 함께 줄어들게 합니다.
- 실제 클릭은 같은 좌표의 투명 HTML `button` hitbox가 맡고, 버튼 안에는 보이는 HTML 텍스트를 넣지 않습니다. 접근성용 `aria-label`만 둡니다.
- 순위판 하단 버튼은 SVG 표면과 HTML hitbox 중심이 실제 브라우저 렌더 기준으로 같아야 하고, 보드·패널·하단 경계선과 겹치면 실패입니다. 사용자가 수치를 주지 않아도 구현자가 `getBoundingClientRect()`로 버튼 바닥과 가장 가까운 경계선을 재고 겹침 0px, 눈에 보이는 여백을 확보합니다.
- 긴 이름과 상태 문구는 렌더러에서 정해진 글자 수로 말줄임합니다. 글자를 맞추려고 viewport 폭 기준 글자 크기를 계속 줄이지 않습니다.
- 목록은 고정 row 슬롯 안에서 안정적으로 보여 주고, 10위까지 같은 SVG 리스트 안에서 wheel/pointer 스크롤로 확인하게 합니다.
- 이 패턴은 동적 UI가 많은 화면을 위한 예외입니다. 결과 라벨, 칭찬, 고정 버튼 장식처럼 유한하고 고정된 시각 요소는 여전히 생성형 결과 이미지/타이틀/버튼 자산으로 처리합니다.
- QA는 1280×800, 1024×768, 사용자가 올린 문제 크기와 비슷한 브라우저 크기에서 캡처하고, SVG `<text>`의 `getBBox()`가 Stage와 의도한 보드 영역 밖으로 나가지 않는지 확인합니다. 버튼이 있으면 캡처와 함께 SVG 버튼 rect, 보이는 라벨, 투명 hitbox의 실제 화면 좌표를 비교하고, 보드 경계선 위에 걸치지 않는지 확인합니다.

## 초3 문제 화면 과밀 금지 (강제)

앞으로 모든 차시의 문제 화면은 "초등학교 3학년 학생이 3초 안에 지금 무엇을 해야 하는지 알 수 있는가?"를 통과해야 합니다. 통과하지 못하면 완성 화면이 아니라 실패 화면으로 보고 다시 줄입니다.

- **한 화면 한 행동**이 기준입니다. 학생이 화면을 보고 `지금은 무엇 하나만 하면 돼?`에 바로 답해야 합니다. 답이 `몫을 고른다`, `남은 별을 고른다`, `검산값을 고른다`처럼 하나로 나오지 않으면 실패입니다.
- **한 화면 한 행동은 한 화면 무사고가 아닙니다.** 학생은 현재 화면에서 수·위치·관계·순서·분해 방법 중 적어도 하나를 수학적으로 판단해야 합니다. 시스템이 정답 분배를 미리 정하고 학생은 채소를 끝날 때까지 누르거나 끌기만 하는 구조는 직접 조작이 아니라 자동 풀이이므로 실패입니다.
- 직접 조작은 학생의 사고를 밖으로 드러내야 합니다. 예: `한 바구니에 몇 묶음을 넣을지 직접 만든다`, `어느 칸에 놓을지 정한다`, `어떤 두 수로 가를지 만든다`. 학생이 숫자를 보지 않아도 반복 동작만으로 끝낼 수 있으면 다시 설계합니다.
- **수학적 판단 수와 물리 입력 수를 따로 셉니다.** 학생이 답을 이미 결정한 뒤 같은 뜻의 물건 놓기·누르기를 세 번 이상 되풀이한다면 새 판단이 생기는 이유를 증명해야 합니다. 이유가 없으면 학생이 답을 먼저 확정하고, 반복 배치는 시스템이 확인 애니메이션으로 보여 줍니다. 학생의 답 없이 시스템이 정답을 만드는 자동 풀이는 금지합니다.
- **버튼에는 누를 이유와 배움이 있어야 합니다.** 학생 풀이 흐름의 모든 선택지·확인·마지막 버튼은 `누르기 전 아직 모르는 것`, `누르며 하는 수학적 판단`, `누른 뒤 새로 보이는 관계나 결과`가 이어져야 합니다. 새 판단도 새 관계 해석도 없으면 버튼을 제거하고 자동 연출로 바꿉니다.
- **이미 결정된 답을 다시 입력시키지 않습니다.** 검산처럼 올바른 관계에서 `몫×나누는 수+나머지=처음 수`가 반드시 성립하고 처음 수가 화면에 이미 보이면, 마지막에 처음 수를 다시 고르거나 입력시키면 실패입니다. 완성값은 자동으로 처음 수 자리와 이어지게 보여 주고, 학생은 결과가 정해지기 전의 수·위치·관계를 판단하거나 그럴듯한 오답을 비교·고칩니다.
- 마지막 행동 버튼은 학생이 만든 상태를 한 번 검증하거나, 완성된 수학 관계를 충분히 본 뒤 아직 알 수 없는 랜덤 보상을 여는 경우에만 둡니다. 보상을 여는 버튼은 새 수학 단계인 척하지 않고 실제로 일어날 변화만 말합니다. 시작·설정·닫기 같은 전역 이동 버튼은 수학 판단 대상에서 제외하지만, 눌렀을 때의 상태 변화는 분명해야 합니다.
- 문제 은행 전체에서 정답 경로의 최소·중앙값·평균·최대 탭·드래그·키 입력 수를 기록합니다. 입력이 기준 차시보다 많다면 늘어난 입력마다 어떤 수학적 판단이 추가되는지 설명할 수 있어야 합니다.
- **각 단계의 존재 이유가 보여야 합니다.** 학생이 `왜 지금 이 계산을 하지?`, `이 값이 마지막 답의 어디에 들어가지?`에 화면 속 수와 물건으로 답할 수 있어야 합니다. 설명 화면이 클릭·드래그 사용법만 알려 주면 실패입니다.
- 확인 버튼을 정답 탐색기로 쓰게 하면 실패입니다. `1개 담기 → 확인 → 2개 담기 → 확인`처럼 작은 수부터 차례로 시험하면 통과할 수 있는 구조는 사고 활동이 아니라 추측 활동입니다. 직접 조작에서는 학생이 물건의 목적지나 관계를 먼저 완성하고, 확인은 완성한 생각을 한 번 검증하는 역할만 맡깁니다.
- 문제 화면에는 학생이 만들 수 있는 그럴듯한 오답 상태가 있어야 하고, 확인 뒤 왜 부족하거나 많은지 현재 물건의 변화로 보여 줍니다. 애니메이션은 학생이 만든 답을 확인하는 역할만 하며 정답을 대신 수행하지 않습니다.
- 문제 화면의 목적은 **현재 한 단계의 수학 행동**을 하게 하는 것입니다. 개념 설명판, 풀이 해설판, 힌트판, 계산 미리보기, 보상 상태판을 한 화면에 동시에 펼치지 않습니다.
- 한 문제 화면에 기본으로 보여도 되는 학습 요소는 4개뿐입니다: **큰 문제**, **현재 단계 조작판**, **한 줄 지시문**, **선택지**. 나머지는 접거나, 완료 뒤에 작은 체크칩으로 줄이거나, 힌트 버튼 뒤로 숨깁니다.
- 설명 문구는 기본 **한 줄**입니다. 문제 화면에서 문장형 해설을 여러 줄 쓰지 않습니다. 예: `먼저 63×3을 골라요`, `7과 곱한 값을 골라요`. 교사용 설명처럼 긴 문장은 금지합니다.
- 원리는 글로 설명하지 말고 **보이는 조작**으로 드러냅니다. 자리값, 0 붙이기, 내림, 나머지, 검산은 블록·칸·스티커·화살표처럼 학생이 바로 볼 수 있는 구조로 보여 줍니다.
- 단계형 문제는 **현재 단계만 크게** 보여 줍니다. 이전 단계는 작은 완료 칩으로 접고, 다음 단계는 `?` 또는 잠금 상태로 둡니다. 여러 단계의 계산판을 옆에 동시에 펼치지 않습니다.
- 단계 정답 뒤에는 반드시 **정답 확인 상태**가 보입니다. 학생이 방금 고른 답이 어디에 들어갔는지 계산판·칸·블록에서 바로 확인하고, 그 확인 화면이 잠깐 머문 뒤 다음 단계로 넘어가야 합니다. 마지막 단계 뒤 보상은 자동 모달보다 학생이 확인 버튼을 눌러 여는 흐름을 우선합니다.
- 선택지는 항상 가장 눈에 잘 띄는 한 덩어리로 둡니다. 선택지와 큰 문제 사이에 장식, 보상판, 긴 설명 패널을 끼워 넣지 않습니다.
- 문제·현재 단계·선택지가 Stage의 대부분을 차지해야 합니다. 보조 정보(진행도, 콤보, 보상 상태, 설정/소리 버튼)는 작게 두고, 풀이 판단을 방해하면 숨깁니다.
- **학습 우선순위는 실제 화면 면적으로 보여야 합니다.** 레이아웃 전에 `1순위 학습 대상`, `2순위 보조 정보`, `3순위 상태·장식`을 선언합니다. 1순위는 콘텐츠 레이어에서 가장 큰 단일 영역이어야 하고, 글자 크기·대비·여백도 가장 강해야 합니다.
- 정보량은 글자 수가 아니라 학생이 연결해서 읽어야 하는 수·라벨·관계·표현의 독립 묶음 수로 봅니다. 정보 묶음이 많은 핵심 패널이 더 단순한 보조 카드보다 좁거나 빽빽하면 실패입니다.
- 검산판처럼 연결된 식·표현이 둘 이상 들어간 핵심 패널은 실제 Stage 폭의 `65%` 이상을 기본값으로 둡니다. 그보다 작으면 글자를 줄이지 말고 장식 폭·중첩 테두리·보조 라벨을 먼저 줄여 패널을 키웁니다. 예외가 필요하면 대안 캡처와 이유를 `PLAN.md`/`REPORT.md`에 남깁니다.
- 브라우저에서 `영역/우선순위/정보 묶음 수/width/height/Stage 폭 비율/Stage 면적 비율/판정`을 실제 rect로 기록합니다. 3초 보기와 흐림/눈가늘임 상태에서도 1순위 학습 대상이 가장 먼저 보이지 않으면 실패입니다.
- 장면 배경은 Stage 전체를 채울 수 있지만 핵심 계산판·조작판·선택지를 좁은 열로 밀어내면 안 됩니다. 사용자가 핵심 영역이 작다고 발견한 화면 크기도 겹침 실패처럼 이름 있는 영구 회귀 테스트로 남깁니다.
- 힌트는 기본으로 열어 두지 않습니다. 학생이 눌렀을 때도 **지금 단계 힌트 1개**만 보여 주고, 전체 풀이 과정을 한 번에 펼치지 않습니다.
- 학생 화면에 `핵심 메커니즘`, `메타 필드`, `부분곱`, `정답 수 게이트` 같은 제작자 용어를 쓰지 않습니다. 수학 용어가 필요하면 교과서식 짧은 말로 바꿉니다.
- 스크린샷을 보고 "문제보다 패널이 많다", "읽어야 할 문장이 많다", "지금 눌러야 할 것이 바로 안 보인다"는 느낌이 들면 무조건 덜어냅니다.

## 기준 차시 비교·증거 최신성 (강제)

새 차시를 만들거나 학습 조작·화면 골격·보상 구조를 크게 바꾼 뒤에는 계약 검사만 하지 않고, 이미 완성된 강한 차시와 실제 화면을 나란히 비교합니다.

- 한 차시를 통째로 복제하지 않습니다. 학습 조작, 오답 피드백, 진행감, 보상, 결과, 검증처럼 강점별 기준을 고릅니다. 기본 비교군은 3학년 2학기 1단원 1~4차시입니다.
- 수학적 판단, 한 화면 한 행동, 그럴듯한 오답, 틀린 이유의 시각화, 마지막 완성값 확인, 텍스트 넘침·요소 겹침 0건, 랜덤 보상, `다음엔 더 멀리`는 다른 장점으로 상쇄할 수 없는 강제 통과 항목입니다.
- 설명 그림의 물건 수·묶음 수·식은 실제 수학 관계와 정확히 맞아야 합니다. 예쁜 그림이라도 학생이 수량 관계를 잘못 읽게 하면 실패입니다.
- 학습 조작을 바꿀 때는 문제 모델, 화면, 설명 이미지, 학생 문구, 테스트, `README.md`, `REPORT.md`, 스크린샷을 같은 변경 묶음으로 갱신합니다. 일부만 새 흐름이면 완료로 보지 않습니다.
- 자동 검사 통과와 실제 화면 확인을 따로 기록합니다. 컴퓨터 화면과 태블릿 가로에서 오답 양방향, 정답 확인, 마지막 확인, 보상, 결과를 직접 봅니다.
- `REPORT.md`의 PASS는 현재 코드에서 해당 명령을 다시 실행해 종료 코드 0을 확인한 항목만 적습니다. 전용 QA 하나라도 실패하면 공통 계약 PASS로 덮지 않으며, 이전 PASS는 날짜를 붙여 과거 기록으로 구분합니다.
- 비교 결과는 `필수 계약`, `기준 차시보다 약한 부분`, `제품 선택`으로 나눕니다. 기준 차시에 있다는 이유만으로 모두 강제하지 않고, 현재 차시가 기준 차시보다 나아진 학습 조작·피드백은 되돌리지 않습니다.
- 생성 원본 PNG나 파일 존재 여부만 보지 않습니다. 실제 `index.html`이 참조하는 WebP를 브라우저가 모두 읽은 뒤 캡처하고, 글자·아이콘·버튼 면·캐릭터가 빈칸 없이 보이는지 확인합니다. 같은 자산이 한 화면 크기에서만 비어 보이면 해당 증거 세트 전체를 다시 캡처합니다.
- 로컬 실행에 필요한 파일이 Git 추적 밖에 있으면 배포 준비 완료로 판정하지 않습니다. 커밋·푸시 전에는 `index.html`과 차시 설정이 참조하는 모든 런타임 자산이 `git ls-files`에 포함되는지 차시 폴더와 `_lessons/` 원본 양쪽에서 확인합니다.
- 현재 흐름과 맞지 않는 예전 스크린샷·전용 QA·문서는 현재 증거와 섞어 두지 않습니다. 삭제하지 말고 `_archive/`로 옮겨 날짜나 버전을 남깁니다.
- 대형 화면 변경 뒤에는 `lesson.json > qa.viewports`의 모든 화면 크기에서 시작·설정·설명·문제 대기·오답/오개념·정답 확인·닫힌/열린 보상·모달 뒤 변화·실제 결과·유한 결과 전 단계를 현재 코드로 다시 캡처합니다. 대표 몇 장만 새로 찍어 이전 화면군과 섞으면 실패입니다.
- `REPORT.md`의 최신 화면 절은 데스크톱 개별 캡처마다 학생이 보는 것, 판단하는 것, 확인되는 수학 관계, 다음 상태로 넘어가는 이유를 설명합니다. 나머지 등록 viewport도 같은 상태를 모두 담은 컨택시트를 포함합니다.
- `node scripts/build-lesson-report-sheets.mjs <lesson-folder>`로 viewport별 컨택시트와 현재 `index.html`·원본 캡처 SHA-256 manifest를 만들고, `node scripts/check-lesson-report-evidence.mjs <lesson-folder>`로 모든 viewport·필수 상태·결과 단계·보고서 연결이 최신인지 검사합니다.
- 차시 폴더의 `BENCHMARK_AUDIT.md` 또는 같은 목적의 문서에 비교 기준, 현재 증거, 판정, 우선순위, 다음 행동을 남깁니다. 판정은 `통과`, `일부 통과`, `부족`, `선택`으로 나눕니다.
- 학생에게 잘못된 수학 관계를 보여 주거나 배움이 일어나지 않는 문제는 `P0`, 현재 증거·오답 피드백·재도전 동기를 크게 떨어뜨리는 문제는 `P1`, 완성도 향상이나 선택 기능은 `P2`로 둡니다.
- 전국 순위처럼 백엔드 계약이 필요하거나 문제 화면을 더 복잡하게 할 수 있는 선택 기능은 구현자가 임의로 넣지 않고 사용자에게 결정받습니다.

## 핵심 설계 철학 (모든 차시 공통)

이 작업실이 만드는 모든 게임은 아래 철학을 따릅니다. 이것은 차시별 소재가 바뀌어도 변하지 않는, 이 자료의 정체성입니다. 새 차시를 만들 때마다 문제 생성·보상 설계·결과 구조가 이 철학에 맞는지 먼저 확인합니다.

### 1. 계산은 "행동의 도구"다

문제는 그 자체로 끝나는 반복 과제가 아니라, 점수·등급·결과 보상으로 이어지는 행동의 도구입니다. 학생은 문제를 맞혀야 다음 보상(상자·연료·부품·바구니 등 차시 소재)을 열 수 있고, 그 결과에 따라 보상이 크게 오르내립니다. 계산은 예측 가능한 영역이지만 보상은 예측 불가능하게 설계해, 학생은 같은 유형의 문제를 풀면서도 매번 다른 결과를 경험합니다.

교육적으로는 반복 연습에 게임의 간헐적 보상을 결합한 구조입니다. 학생은 정답을 맞히는 데서 멈추지 않고, 다음 보상과 최종 결과를 기대하며 더 많은 문제에 자발적으로 접근합니다. 이때 무작위성은 학습을 흐리는 장치가 아니라, 이미 배운 계산 절차를 더 자주 사용하게 만드는 동기 장치로 작동합니다.

계산 행동에는 반드시 학생의 판단이 들어갑니다. 보기 고르기, 수 만들기, 물건 옮기기처럼 겉모양은 달라도 학생이 수학적으로 답을 결정해야 합니다. 시스템이 올바른 답을 자동으로 만들고 학생에게 반복 클릭만 요구하면 계산이 행동의 도구가 아니라 연출의 구경거리가 되므로 허용하지 않습니다.

### 2. 무작위성은 반드시 유지한다

랜덤 보상은 빼지 않습니다. 아이들이 흥미를 가지고 다시 도전하게 만드는 핵심 동력입니다. 단, 무작위성은 "계산을 안 해도 되는 핑계"가 아니라 "이미 한 계산의 결과를 매번 새롭게 만드는 장치"여야 합니다. 보상에는 기대감과 긴장감이 함께 있어야 합니다(잘 풀어도 가끔 손해, 운이 따르면 한 번에 도약).

### 3. 정답이 전부를 보장하지 않는다

- 좋은 것은 얻기 어렵게 만듭니다. 한 판으로 최고 등급에 쉽게 닿으면 반복할 이유가 사라집니다.
- 정답을 많이 맞히는 것은 분명히 유리해야 합니다. 하지만 그것만이 모든 걸 쉽게 얻는 유일한 통로가 되어서는 안 됩니다.
- 못하는 아이도 무언가는 반드시 얻습니다(빈손 금지). 동시에 운이 따르면 평소보다 더 좋은 것을 얻을 수도 있습니다.
- 그래서 잘하는 아이는 실력으로, 아직 서툰 아이는 실력+운으로 위를 노립니다. 누구도 완전히 막히지 않고, 누구도 한 번에 다 갖지 않습니다.

### 4. "다음엔 더 멀리"를 만든다

결과는 차시마다 자체 완결형 등급으로 보여 줍니다. 학생이 "이번엔 여기까지 왔지만 다음엔 더 멀리 갈 수 있다"고 느끼게 만드는 것이 이 자료의 핵심입니다. 한 개의 콘텐츠를 여러 번 반복하게 만드는 재도전 동기가 모든 차시 설계의 최종 점검 기준입니다.

- **숫자 변화만 보여 주지 말고 현재 보상과 다음 목표를 연결합니다.** 보상 화면에서 이번에 얻은 장소·물건·단계가 무엇인지 보여 주고, 결과 화면에서 다음에 노릴 목표를 이어 보여 줍니다. 학생이 `이번엔 여기까지, 다음엔 저기까지`를 바로 알 수 없으면 실패입니다.

### 차시 설계 점검 질문

새 차시를 만들 때 아래에 모두 "그렇다"가 나와야 합니다.

- 문제를 푸는 것이 보상으로 이어지는 "행동의 도구"로 설계되었는가?
- 랜덤 보상이 있고, 기대감과 긴장감이 함께 있는가?
- 최고 보상은 충분히 얻기 어려운가? (한 판으로 쉽게 닿지 않는가)
- 정답이 유리하되, 정답만이 모든 걸 쉽게 주는 유일한 통로는 아닌가?
- 못해도 빈손은 아니고, 운으로 한 단계 더 갈 여지가 있는가?
- 학생이 "다음엔 더 멀리"를 떠올리며 다시 하고 싶어지는가?

## 폴더 규칙

- 새 차시 폴더명은 `학년-학기-단원-차시-영문짧은이름`입니다.
- 각 차시 폴더에는 `index.html`, `README.md`, `REPORT.md`, `screenshots/`를 둡니다.
- 공용으로 재사용할 로고, 캐릭터, 템플릿은 `_shared/` 또는 `_templates/`에 둡니다.
- 오래된 실험본은 삭제하지 말고 필요하면 `_archive/`로 옮깁니다.
- 새 차시를 만들거나 화면을 크게 바꿀 때는 `SERIES_CONTRACT.md`의 화면 계약을 먼저 확인합니다.

## 현재 진행 상황

- 1단원 곱셈: 1~4차시 모두 빌드 완료
  - `3-2-1-1-mathmon-box-run` 매스몬 상자런 (받아올림 없는 세 자리×한 자리)
  - `3-2-1-2-mathmon-rocket-charge` 매스몬 로켓발사 대작전 (받아올림 있는 세 자리×한 자리) — 최신 엔진 기준
  - `3-2-1-3-mathmon-jump-islands` 매스몬 10배 점프섬 ((몇십)×(몇십), (몇십몇)×(몇십))
  - `3-2-1-4-mathmon-fusion` 매스몬 로봇 합체 ((몇)×(몇십몇), (몇십몇)×(몇십몇))
- 2단원 나눗셈: 1~4차시 모두 빌드 완료 + 완성도 폴리시 적용 (문제 화면 과밀 정리, 보상 `requestAnimationFrame` 모션, 매스몬 동행 점검)
  - `3-2-2-1-mathmon-divide-farm` 매스몬 나누기 농장 (내림 없는 (몇십몇)÷(몇))
  - `3-2-2-2-mathmon-elevator` 매스몬 엘리베이터 (내림 있는 (두 자리)÷(한 자리))
  - `3-2-2-3-mathmon-star-pickup` 매스몬 별 줍기 (나머지가 있는 나눗셈)
  - `3-2-2-4-mathmon-check-lock` 매스몬 검산 자물쇠 (곱셈으로 검산하기)
- 3단원 원: 1~4차시 모두 빌드 완료 + 생성형 커버·제목 아트·시작 버튼·결과 등급 자산 연결 완료 (`modal-controls`, `generated-title-overlay`, `generated-button-art`, `generated-assets` 기준). 문제 화면은 원 SVG 도형 선택으로 구현.
  - `3-2-3-1-mathmon-target-hit` 매스몬 표적 맞히기 (원의 중심·반지름·지름 찾기)
  - `3-2-3-2-mathmon-compass-ring` 매스몬 컴퍼스 마법진 (컴퍼스로 원 그리기)
  - `3-2-3-3-mathmon-double-bridge` 매스몬 두 배 다리 (지름 = 반지름 × 2)
  - `3-2-3-4-mathmon-circle-pattern` 매스몬 원 무늬 디자이너 (원으로 규칙·무늬 만들기, ★ 단원 정점)
- 4단원 분수: 1~4차시 모두 빌드 완료 + 생성형 커버·제목 아트·시작 버튼·결과 등급 자산 연결 완료 (`modal-controls`, `generated-title-overlay`, `generated-button-art`, `generated-assets` 기준). 문제 화면은 분수 SVG/HTML 선택으로 구현.
  - `3-2-4-1-mathmon-pizza-fraction` 매스몬 피자 분수 가게 (부분과 전체로 분수 나타내기)
  - `3-2-4-2-mathmon-fraction-scoop` 매스몬 분수만큼 담기 (전체의 분수만큼 구하기, 2단계 선택)
  - `3-2-4-3-mathmon-fraction-sorter` 매스몬 분수 분류 컨베이어 (진분수·가분수·대분수 구분)
  - `3-2-4-4-mathmon-fraction-tug` 매스몬 분수 줄다리기 (분모 같은 분수·단위분수 비교, ★ 단원 정점)
- 5단원 들이와 무게: 1~4차시 모두 빌드 완료 (`modal-controls`, `generated-title-overlay`, `generated-button-art`, `generated-assets` 기준)
  - `3-2-5-1-mathmon-water-fill` 매스몬 물통 채우기 시합 (mL/L 눈금 읽기와 들이 비교)
  - `3-2-5-2-mathmon-drink-order` 매스몬 음료 제조 주문 (들이의 덧셈·뺄셈과 주문 비교)
  - `3-2-5-3-mathmon-scale-balance` 매스몬 저울 균형 (kg/g/t 무게 비교와 균형 맞추기)
  - `3-2-5-4-mathmon-package-weight` 매스몬 택배 무게 맞추기 (무게의 덧셈·뺄셈과 어림)
- 공통 흐름: 문제 풀이 → 단계 선택 → 랜덤 보상 → 차시별 자체 완결형 등급 결과

## 레포지토리 운영 기준

- 이 작업실의 GitHub 레포지토리 하나를 원본이자 배포 기준으로 운영합니다.
- 차시 추가, 문서 수정, 이미지 자산, GitHub Pages 배포 설정은 모두 이 레포지토리 안에서 처리합니다.
- 마무리 커밋이나 푸시를 진행하기 전에는 변경된 차시의 `REPORT.md`를 최신 구현 기준으로 맞춥니다. 화면 흐름, 보상 구조, 자산, 검증 결과가 실제 코드와 어긋나면 커밋 전에 고칩니다.
- Railway, GitHub Pages, 백엔드 배포 오류를 다룰 때도 사용자가 명시하지 않은 배포 방식 전환을 하지 않습니다. `Dockerfile`, `.dockerignore`, `docker-compose.yml`, `nixpacks.toml`, `railway.json`, `Procfile`, 루트 `package.json`, CI/배포 workflow 같은 인프라 진입점은 사용자가 해당 방식이나 파일을 직접 요구한 경우에만 새로 만들거나 바꿉니다.
- Railway `run failed`처럼 배포 오류가 보이면 먼저 기존 배포 표면을 확인합니다. 현재 백엔드 기준은 `scoreboard-api`를 Railway 서비스 Root Directory로 두고 `bun run start`로 실행하는 것입니다. Docker나 다른 런타임으로 우회하지 말고, 로그·Root Directory·Start Command·환경 변수·문서 불일치를 먼저 점검합니다.
- 별도 공개 레포를 만들지 않습니다.

## 시리즈화 원칙

이 작업실은 한 차시짜리 게임이 아니라 "한 시리즈"를 만듭니다. 차시가 24개로 늘어도 학생이 보기에 같은 브랜드의 게임으로 느껴져야 합니다.

### 시리즈 정체성

- 시리즈 통합 마스코트는 `매스몬`입니다. 매스몬은 브랜드 정체성으로 쓰되, 첫 화면에서 보일 때는 배경 위에 따로 붙인 캐릭터가 아니라 `cover-generated.webp` 안에 함께 생성된 동행 캐릭터여야 합니다. 결과에서는 차시별 중심 보상 등급 뒤에 이번 판 보상 카드 1장으로 보여 줍니다(저장형 도감처럼 모아두는 구조는 만들지 않습니다 — 별도 백엔드 없음).
- 매스몬 이미지·로고·등급 색 같은 공용 자산은 `_shared/`에 두고 차시 폴더에서 필요한 배포본만 복사해 씁니다.
- 화면 골격(첫 화면 → 설명 → 문제 → 보상 → 결과)은 모든 차시가 동일하게 따릅니다. 자세한 규칙은 `SERIES_CONTRACT.md`가 단일 기준입니다.
- 차시마다 다른 것은 "문제 내용"과 "그 단원에 어울리는 보상 연출"뿐입니다. 점수·콤보·등급 결과 같은 뼈대는 공유합니다.

### 매스몬 자산 관리 기준

- 매스몬 관련 작업을 시작하면 먼저 `.claude/skills/eduitit-mathmon-assets/SKILL.md`와 `_shared/mathmon/MATHMON_ASSET_CONTRACT.md`를 읽습니다.
- 매스몬 원본의 단일 관리 위치는 `_shared/mathmon/`입니다. Codex와 Claude는 새 매스몬을 만들거나 기존 매스몬을 사용할 때 먼저 `_shared/mathmon/catalog.json`과 `_shared/mathmon/STYLE_GUIDE.md`를 확인합니다.
- 새 매스몬은 곧바로 차시 폴더에만 넣지 않습니다. 먼저 `_shared/mathmon/<pack-id>/`에 `manifest.json`, `png/`, `webp/`, `raw-chromakey/`, `contact-sheets/` 구조로 등록합니다.
- 차시 폴더에는 실행에 필요한 WebP 같은 경량 배포본만 복사합니다. 원본 PNG와 생성 원본은 `_shared/mathmon/`에 둡니다.
- 한 차시는 기본적으로 한 매스몬 팩만 씁니다. 여러 팩을 섞어야 할 때는 해당 차시 `README.md`/`REPORT.md`와 팩 `manifest.json`에 이유를 남깁니다.
- 새 팩을 만들기 전 기존 팩의 contact sheet와 manifest를 보고 실루엣·동물종·소재가 겹치지 않는지 확인합니다.
- 이미지 분위기는 팩 단위로 반드시 일관되어야 합니다. 앞으로 새로 만드는 매스몬은 1차시 `매스몬 상자런`의 기본 10종을 원본 표준으로 삼습니다. 이 10종은 없애는 대상이 아니라 뒤에 만든 매스몬을 맞추는 기준입니다. 기준 팩은 `base-pack`이고, 승인된 활성 팩은 `base-pack`과 `zero-factory-animal-pack`입니다. `circle-pack`, `fraction-pack`, `explorer-pack`, `weight-pack`처럼 기존 실행 화면 보존을 위해 남긴 팩은 `style-review-runtime`으로 보고 새 차시 복제 기준으로 쓰지 않습니다. 0 공장 기존 팩과 V2 팩은 보존만 합니다.
- 매스몬 일관성은 모든 캐릭터를 밋밋하게 귀엽게 만드는 뜻이 아닙니다. 같은 2D 게임 스티커 화풍·외곽선·카메라 거리·파일 규격 안에서 귀여움, 사랑스러움, 멋짐, 강함, 살짝 포악한 야생성, 신비감을 의도적으로 섞습니다. 10마리 팩이 같은 큰 눈·같은 미소·같은 둥근 몸·같은 소품으로 반복되면 실패입니다.
- 차시별 전용 매스몬도 본체는 동물/판타지 생물이어야 합니다. 차시 테마는 소품·의상·배지·포즈로만 표현하고, 톱니바퀴·자석·상자·컨베이어 같은 사물 자체를 매스몬 몸으로 만들지 않습니다.

### RasterStage 화면 규칙

- 모든 새 차시의 첫 화면과 마지막 결과 화면은 이미지 생성으로 만든 대표 장면을 배경으로 사용합니다.
- 모든 RasterStage와 주요 게임 화면은 `1280×800` 기준 `16:10` 안에서 설계합니다. 생성 이미지가 다른 비율이어도 Stage 안에서는 `object-fit: cover/contain`과 HTML 오버레이로 맞추고, Stage 자체 비율을 바꾸지 않습니다.
- 첫 화면 배경 이미지는 글자 없는 대표 장면이어야 합니다. 게임명·한 줄 목표·시작 버튼을 한 이미지에 구워 넣지 않고, `cover-generated.webp` 배경 + `.hero-title-art` 제목 이미지 + HTML 목표 + 생성형 시작 버튼 아트로 나눕니다.
- 첫 화면에 매스몬 동행 캐릭터를 보이게 할 때는 `cover-generated.webp` 생성 단계에서 배경과 함께 한 장면으로 생성합니다. 기존 매스몬 컷아웃 PNG/WebP를 커버 위에 따로 붙이는 방식은 배경과 캐릭터 톤이 어긋나므로 새 차시와 리마스터의 최종 화면으로 쓰지 않습니다.
- 현재 목표 표준은 실제 HTML 버튼 안에 생성형 버튼 아트를 넣는 구조입니다. 새 차시와 이관 완료 차시는 `data-cover-start-standard="generated-button-art"`로 선언합니다. 기존 `.primary-button` 시작 커버는 `compatibility-primary-button`으로 분류해 개별 이관 전까지 유지할 수 있지만, 보이는 CSS 텍스트 버튼과 커버 전체 투명 hitbox 방식은 새 차시에 쓰지 않습니다.
- 첫 화면 제목은 배경 이미지 위에 별도의 타이틀 아트로 얹을 수 있습니다. 이때 **생성 대상은 전체 커버가 아니라 제목 로고만**입니다. 기존 `cover-generated.webp` 같은 대표 장면을 유지하고, GPT Image 등으로 만든 제목 로고를 초록 배경 제거 또는 투명 PNG/WebP로 분리해 `.hero-title-art`로 올립니다.
- 학생에게 보이는 제목은 하나의 그림처럼 보여야 합니다. HTML/CSS로 급조한 밋밋한 큰 글자, 어설픈 SVG 로고, 전체 커버를 새로 합성해 장면 구도를 망가뜨리는 방식, AI가 철자를 틀린 한글 이미지는 모두 실패로 봅니다.
- 제목 로고를 만드는 순간에는 `image_gen`/GPT Image 등 생성형 이미지 도구를 실제로 사용합니다. 로컬 폰트로 직접 래스터화하거나 CSS 효과를 캡처해 만든 파일은 `.hero-title-art`에 연결하지 않습니다.
- 생성형 이미지처럼 보여야 하는 화면·타이틀·보상·결과 자산을 로컬 폰트, Pillow, canvas, SVG, CSS 캡처, 기존 PNG/WebP 겹치기 등 로컬 합성으로 만들지 않습니다. 로컬 합성은 사용자가 먼저 명시적으로 허락한 경우에만 예외로 쓸 수 있습니다.
- 배경 제거, 크롭, 리사이즈, WebP 변환, 용량 최적화처럼 생성형 원본의 의미를 바꾸지 않는 후처리는 허용합니다. 새 문구·버튼·캐릭터·패널을 로컬에서 그려 붙이면 후처리가 아니라 금지된 로컬 합성입니다.
- GPT Image 등 생성형 이미지로 제목을 만들 때는 `standalone title/logo asset`, `exact Korean text`, `no full scene`, `flat chroma-key background` 조건으로 만들고, 한글 철자를 화면 캡처로 검수합니다. 철자가 틀리거나 품질이 맞지 않으면 같은 제목 오버레이 슬롯에서 재생성합니다.
- 결과 화면 생성 이미지는 차시의 중심 보상 장면과 매스몬 동행 캐릭터가 자연스럽게 들어간 한 장면으로 만듭니다. 섬 이름·도착 라벨·칭찬 문구·다시하기 버튼처럼 매 판 똑같은 요소는 개별 HTML/CSS 조각으로 다시 만들지 말고 생성 이미지 안에 포함하는 것을 기본값으로 합니다.
- 결과 화면에서 HTML/CSS 오버레이로 남길 것은 점수·연료·힘처럼 매 판 실제로 달라지고 값 범위가 넓은 정보뿐입니다. 정답 수 `0/10`~`10/10`처럼 경우의 수가 고정된 값은 공용 생성형 숫자 이미지 `_shared/result-count/result-correct-*-generated.webp`를 사용하고, CSS/SVG/HTML 폰트로 크게 그리지 않습니다. 버튼 시각 요소를 이미지 안에 넣은 경우에도 키보드·스크린리더 접근성을 위해 같은 위치의 실제 HTML 버튼 또는 hitbox는 유지할 수 있습니다.
- 모든 차시 결과 화면에서 정답 수를 보일 때는 `<img class="result-correct-art" src="../_shared/result-count/result-correct-N-generated.webp" alt="" aria-hidden="true">` 패턴을 씁니다. JS는 정답 수를 `0..10`으로 제한해 이미지 `src`만 바꾸고, 보이는 `0/10` 텍스트 노드나 SVG `<text>`는 남기지 않습니다. 접근성용 정답 수는 숨김 텍스트나 결과 요약에만 둡니다.
- 결과 화면에 보이는 큰 결과 라벨, 칭찬 문구, 다시 버튼 장식은 CSS 텍스트/카드로 만들지 않습니다. 생성 이미지 위에 반투명 카드, blur 패널, CSS 제목, CSS 본문, CSS로 그린 큰 버튼을 얹으면 하네스 실패입니다.
- 결과 라벨이 4단계처럼 유한하게 바뀌면 각 단계별 생성형 결과 이미지나 독립 타이틀/버튼 자산을 만들고 JS로 이미지 `src`만 바꿉니다. HTML 텍스트는 `visually-hidden` 접근성 값이나 점수·연료·힘처럼 매 판 계산되고 범위가 넓은 최소 정보만 남깁니다.
- 생성형 결과 라벨/버튼 자산 표준을 쓰는 차시는 `<main class="game">`에 `data-result-visual-standard="generated-assets"`를 선언해 공통 하네스가 CSS 결과 카드와 보이는 HTML 결과 문구를 잡게 합니다.
- 결과 화면을 통째 이미지로 요구받거나 `result-final-*-generated.webp`가 도착 상태별 완성 장면일 때는 `<main class="game">`에 `data-result-render-mode="fullscene-score-slot"`도 함께 선언합니다. 이 모드에서 보이는 HTML은 공용 정답 수 이미지 아트(`.result-correct-art`)와 투명 다시하기 hitbox뿐이며, `#finalCorrectText`는 숨김 접근성 값으로만 둡니다.
- `fullscene-score-slot`의 정답 수 이미지는 이미지 안 빈 점수칸에 맞춘 RasterStage 슬롯으로 배치합니다. 생성 이미지마다 빈칸 위치가 다르면 `data-result-island` 같은 상태별 슬롯을 명시합니다. 중앙 정렬 QA는 CSS 좌표값만 보지 말고, 1280×800과 1024×768 스크린샷 픽셀에서 정답 수 이미지 중심과 빈 점수칸 중심을 비교해 확인합니다.
- `fullscene-score-slot` 모드에서는 `.result-stats`, `.result-stat`, `.result-card`, `.result-copy` 같은 CSS 결과 카드와 보이는 `resultTitle`/본문/버튼 장식이 있으면 실패입니다. 점수 박스 라벨도 이미지와 HTML 양쪽에서 보이지 않게 합니다.
- 사용자가 "이미지에는 글자를 넣지 말라"는 계획을 줬더라도 결과 화면의 고정 라벨·칭찬·버튼을 CSS 카드로 대체하지 않습니다. 둘이 충돌하면 구현 전에 계획을 바로잡고, 글자 없는 결과를 택한다면 보이는 CSS 라벨도 함께 제거합니다.
- 결과 화면에 멋진 보상 장면과 정확한 동적 UI가 함께 필요하면 `data-result-render-mode="hybrid-generated-dynamic"`을 씁니다. 생성 이미지는 로봇·섬·행성·무대·빛·감정 같은 결과 세계를 맡고, SVG `viewBox="0 0 1280 800"` 오버레이는 점수·연료·힘처럼 범위가 넓은 짧은 진행값만 맡습니다. 정답 수는 공용 생성 이미지 아트를 우선합니다.
- 혼합형 결과 화면의 생성 프롬프트에는 `no text`, `no letters`, `no numbers`, `no score board`, `no stats cards`, `no table`, `no buttons`, `no UI panels`를 기본으로 넣습니다. 이미지 안에는 텍스트 없는 받침대, 홀로그램 빔, 빈 에너지 장치 같은 장면 소품만 허용합니다.
- 혼합형 SVG 오버레이는 CSS 카드 우회가 아닙니다. 보이는 SVG 텍스트는 1-3개 짧은 값으로 제한하고, 긴 칭찬·고정 결과 라벨·버튼 장식은 생성형 이미지나 독립 생성형 타이틀/버튼 자산으로 처리합니다. 동적 값이 4개 이상 필요하면 결과 화면이 아니라 순위판/대시보드 패턴으로 분리합니다.
- 첫 화면, 보상 모달, 결과 장면에서 매스몬이 필요하면 이미지 생성 단계에서 장면 안에 매스몬을 함께 포함합니다. 완성된 배경 위에 기존 매스몬 PNG/WebP를 따로 붙여 캐릭터처럼 보이게 합성하는 방식은 어색한 결과가 나기 쉬우므로 쓰지 않습니다. 실제 이동 애니메이션이 꼭 필요할 때만 별도의 생성형 스프라이트/프레임을 만들고, 기존 캐릭터 컷아웃을 배경 위에 올리는 임시 합성은 금지합니다.
- 생성 원본 PNG는 작업실에 보관하고, 학생에게 배포하는 `index.html`에서는 WebP 같은 경량 포맷을 우선 사용합니다.
- 문제 화면과 선택지 화면은 생성 이미지보다 읽기 쉬운 HTML/CSS UI를 우선합니다. 장면 이미지는 문제와 선택지를 가리지 않는 배경 또는 보상 연출에만 씁니다.
- `README.md`와 `REPORT.md`에는 사용한 생성 이미지 파일명, 배치 의도, 동적 HTML 오버레이 범위를 기록합니다.

### 차시별로 달라도 되는 것 / 달라지면 안 되는 것

- 달라져도 되는 것: 게임 제목, 문제 소재, 보상 오브젝트(상자·씨앗·저울·물통 등), 배경 테마 색.
- 절대 달라지면 안 되는 것: 브랜드 배지 위치, 단원/배움주제 배지 형식, 첫 화면 3요소(제목·한 줄 목표·시작 버튼), "한 번만 적용되는 중심 보상" 원칙, 차시별 자체 완결형 등급 결과로 끝나는 흐름.

### 제작 워크플로 (차시 빌드 절차)

차시를 실제로 만들거나 크게 바꿀 때의 단계별 공정·재사용 엔진 맵·이미지 패턴·검증·배포 체크리스트는 **`eduitit-mathmon-lesson` 스킬**이 단일 기준입니다. 새 차시는 그 스킬을 따라 빌드하고, 차시별 상세 계획은 각 폴더의 `PLAN.md`에 먼저 적습니다.

새 차시를 만들거나 문제 화면을 다시 설계할 때는 `.claude/skills/eduitit-mathmon-lesson/references/lesson-prompts.md`의 공통 제작 프롬프트와 해당 차시별 프롬프트를 먼저 적용합니다.

1-2-3차시에서 재사용할 Stage, 첫 화면, 설정 버튼/모달, 문제 화면 밀도, 보상 모달, 결과 공개, Humanizer QA, 스크린샷 QA 공통 체크는 `LESSON_COMMONS.md`를 함께 확인합니다.

성취기준 코드 표기·교과서 차시 배열 대조 같은 빌드 시 주의사항도 같은 스킬에 정리되어 있습니다. (아래 로드맵 표의 `[4수0X-XX]` 코드는 안내용이며, 문서에 박기 전 교육부 고시 제2022-33호 원문과 대조합니다.)

## 3학년 2학기 게임 제작 계획 (2022 개정)

2022 개정 수학과는 내용 영역이 `수와 연산 / 변화와 관계 / 도형과 측정 / 자료와 가능성` 4개입니다. 3학년 2학기는 보통 아래 6단원으로 구성되며, 단원마다 게임 4개(총 24개)를 만듭니다.

폴더명 규칙: `3-2-<단원>-<차시>-<영문짧은이름>` · 모든 게임 제목은 `매스몬`으로 시작 · 보상은 차시별 자체 완결형 등급(도감 없음).

### 1단원 곱셈 — 영역: 수와 연산 / 성취기준 `[4수01-05]`

곱하는 수가 한 자리 또는 두 자리인 곱셈의 계산 원리를 이해하고 계산한다.

| 차시 | 배움주제 | 게임명(가안) | 핵심 메커니즘 | 중심 보상 |
| --- | --- | --- | --- | --- |
| 1 | 받아올림 없는 (세 자리)×(한 자리) | 매스몬 상자런(완성) | 10문제 풀이 후 상자 등급 보상 | 상자→점수→매스몬 |
| 2 | 받아올림 있는 (세 자리)×(한 자리) | 매스몬 로켓발사 대작전 | 받아올림 있는 곱셈을 풀어 로켓의 연료 넣기 | 연료량 측정→매스몬 |
| 3 | (몇십)×(몇십), (몇십몇)×(몇십) | 매스몬 10배 점프섬 | 0 가리기→먼저 곱하기→0 붙이기 | 섬 도착지 |
| 4 | (몇)×(몇십몇), (몇십몇)×(몇십몇) | 매스몬 로봇 합체 | 부분곱 2칸을 더해 큰 곱 완성 | 합체 등급 |

### 2단원 나눗셈 — 영역: 수와 연산 / 성취기준 `[4수01-06]`

나누는 수가 한 자리(또는 두 자리)인 나눗셈을 계산하고, 몫과 나머지의 의미를 안다.

| 차시 | 배움주제 | 게임명(가안) | 핵심 메커니즘 | 중심 보상 |
| --- | --- | --- | --- | --- |
| 1 | 내림 없는 (몇십몇)÷(몇) | 매스몬 나누기 농장 | 십·일의 자리를 차례로 나눠 바구니에 담기 | 수확 등급 |
| 2 | 내림 있는 (두 자리)÷(한 자리) | 매스몬 엘리베이터 | 십의 자리부터 층을 내려 나누기 | 도착 층 등급 |
| 3 | 나머지가 있는 나눗셈 | 매스몬 별 줍기 | 몫만큼 나누고 남는 나머지를 줍기 | 별 등급 |
| 4 | 곱셈으로 검산하기 | 매스몬 검산 자물쇠 | 몫×나누는수+나머지로 자물쇠 풀기 | 금고 등급 |

### 3단원 원 — 영역: 도형과 측정 / 성취기준 `[4수02-XX] (원·컴퍼스)`

원의 중심·반지름·지름을 알고, 컴퍼스로 원을 그리며, 지름과 반지름의 관계를 이해한다.

| 차시 | 배움주제 | 게임명(가안) | 핵심 메커니즘 | 중심 보상 |
| --- | --- | --- | --- | --- |
| 1 | 원의 중심·반지름·지름 찾기 | 매스몬 표적 맞히기 | 그림에서 중심/반지름/지름 골라 맞히기 | 명중 등급 |
| 2 | 컴퍼스로 원 그리기 | 매스몬 컴퍼스 마법진 | 반지름 길이를 맞춰 원을 그려 통과 | 마법진 등급 |
| 3 | 지름=반지름×2 관계 | 매스몬 두 배 다리 | 반지름↔지름 변환으로 칸 건너기 | 다리 등급 |
| 4 | 원으로 규칙·무늬 만들기 | 매스몬 원 무늬 디자이너 | 같은 반지름 원을 규칙대로 배치 | 무늬 등급 |

### 4단원 분수 — 영역: 수와 연산 / 성취기준 `[4수01-09], [4수01-10]`

등분할로 분수를 이해하고, 진분수·가분수·대분수를 알며, 분모가 같은 분수와 단위분수의 크기를 비교한다.

| 차시 | 배움주제 | 게임명(가안) | 핵심 메커니즘 | 중심 보상 |
| --- | --- | --- | --- | --- |
| 1 | 부분과 전체로 분수 나타내기 | 매스몬 피자 분수 가게 | 색칠된 조각을 분수로 답하기 | 피자 등급 |
| 2 | 전체의 분수만큼 구하기 | 매스몬 분수만큼 담기 | 전체에서 분수만큼 골라 담기 | 담은 양 등급 |
| 3 | 진분수·가분수·대분수 구분 | 매스몬 분수 분류 컨베이어 | 분수를 세 통으로 분류 | 분류 등급 |
| 4 | 분모 같은 분수·단위분수 비교 | 매스몬 분수 줄다리기 | 두 분수 크기 비교로 승부 | 승점 등급 |

### 5단원 들이와 무게 — 영역: 도형과 측정 / 성취기준 `[4수02-XX] (양의 측정)`

들이(L, mL)와 무게(kg, g, t)를 비교·측정·어림하고, 들이와 무게의 덧셈·뺄셈을 한다.

| 차시 | 배움주제 | 게임명(가안) | 핵심 메커니즘 | 중심 보상 |
| --- | --- | --- | --- | --- |
| 1 | 들이 비교와 L, mL | 매스몬 물통 채우기 시합 | 눈금을 읽고 알맞은 들이 고르기 | 물통 등급 |
| 2 | 들이의 덧셈·뺄셈과 어림 | 매스몬 음료 제조 주문 | L+mL 받아올림으로 주문 맞추기 | 주문 등급 |
| 3 | 무게 비교와 kg, g, t | 매스몬 저울 균형 | 양쪽 무게를 맞춰 저울 수평 | 균형 등급 |
| 4 | 무게의 덧셈·뺄셈과 어림 | 매스몬 택배 무게 맞추기 | kg+g 계산으로 무게 한도 맞추기 | 배송 등급 |

### 6단원 자료의 정리(그림그래프) — 영역: 자료와 가능성 / 성취기준 `[4수04-XX] (자료 정리·그래프)`

자료를 표와 그림그래프로 정리·표현하고, 그래프를 읽어 해석한다.

| 차시 | 배움주제 | 게임명(가안) | 핵심 메커니즘 | 중심 보상 |
| --- | --- | --- | --- | --- |
| 1 | 자료를 표로 정리하기 | 매스몬 인구조사 | 자료를 세어 표 칸 채우기 | 완성 표 등급 |
| 2 | 그림그래프 읽기 | 매스몬 그림 단위 해독 | 그림 1개의 단위로 값 읽어 답하기 | 해독 등급 |
| 3 | 그림그래프로 나타내기 | 매스몬 그림 도장 찍기 | 큰 그림·작은 그림 수를 맞춰 찍기 | 그래프 등급 |
| 4 | 그래프 해석·비교 | 매스몬 데이터 탐정 | 그래프 보고 가장 큰/작은 값 추리 | 사건 해결 등급 |

## 배포 (GitHub Pages)

학생용 정적 HTML 게임은 이 레포지토리 하나의 GitHub Pages로 공유합니다(별도 공개 레포 금지). 필요한 게임 폴더만 `actions/upload-pages-artifact`와 `actions/deploy-pages`로 배포하고, 공유 전 `curl -I -L <url>`로 실제 URL이 `HTTP 200`인지 확인합니다. Pages 실패 시 `gh run view <run-id> --log-failed`로 원인을 먼저 확인합니다. checkout 단계에서 submodule 오류가 나면 `.gitmodules`와 `git ls-files -s`로 깨진 `160000` gitlink를 확인하고, 필요 없는 임시 gitlink는 `git rm <path>`로 제거합니다.
