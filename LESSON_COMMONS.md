# 매스몬 차시 공통 빌더 체크리스트

이 문서는 1-2-3차시에서 굳어진 공통 제작 습관을 새 차시에 바로 적용하기 위한 실무 체크리스트입니다. 큰 원칙과 로드맵은 `AGENTS.md`/`CLAUDE.md`, 화면 계약은 `SERIES_CONTRACT.md`, 제작 절차는 `.claude/skills/eduitit-mathmon-lesson/SKILL.md`를 기준으로 둡니다.

## 1. Stage와 첫 화면

- 모든 차시는 `16:10` Stage와 기준 크기 `1280x800`을 지킵니다.
- `index.html`의 `<main class="game">`에는 `data-stage-ratio="16:10"`과 `data-stage-size="1280x800"`을 둡니다.
- `.stage-shell`이 contain 폭과 `aspect-ratio: 16 / 10`을 담당하고, 각 `.screen`은 Stage 안을 채웁니다.
- 화면을 크게 바꾼 뒤에는 루트에서 `node scripts/check-stage-ratio.mjs`를 실행합니다.
- 첫 화면은 `generated-title-overlay` 표준을 씁니다. 글자 없는 `cover-generated.webp`를 배경으로 깔고, 생성형 이미지 제목 자산을 `.hero-title-art`로 얹습니다.
- 첫 화면에 매스몬을 보이게 할 때는 `cover-generated.webp` 생성 프롬프트에 처음부터 함께 넣습니다. 기존 매스몬 PNG/WebP를 `.cover-mathmon` 같은 별도 이미지로 커버 위에 붙이는 방식은 새 차시와 리마스터에서 쓰지 않습니다.
- 제목 자산은 GPT Image/imagegen 같은 생성형 이미지 산출물이어야 합니다. HTML/CSS/SVG/로컬 폰트로 만든 글자 이미지는 제목 아트로 쓰지 않습니다.
- 한 줄 목표는 실제 HTML 오버레이로 둡니다. 새 차시와 이관 완료 차시는 `data-cover-start-standard="generated-button-art" data-cover-start-asset="shared-canonical-v1"`를 붙이고, 공용 `../_shared/mathmon/cover-start-button/start-button-generated.webp`만 실제 HTML 버튼 안에 넣습니다.
- 시작 버튼은 `mathmon-cover-start-button-v1` 공용 생성 자산을 반복 사용합니다. 새 차시나 큰 커버 수정에서 버튼을 새로 생성·복제·색 변경·재가공하지 않습니다. 1280×800 Stage에서는 `360×152px`, 작은 화면에서는 최소 `300×127px`, `aspect-ratio: 1611 / 680`을 고정합니다. 버튼 아트와 HTML hitbox는 같은 실제 브라우저 경계여야 하며, 커버 전체 투명 hitbox는 새 차시에 쓰지 않습니다.
- 기존 `generated-title-overlay` 차시의 `.primary-button` 시작은 개별 이관 전까지 `compatibility-primary-button`으로 보존할 수 있습니다. 이 호환 상태는 새 차시 기준이 아니며, 이관할 때는 생성형 버튼 자산과 실제 `.cover-start-button` 구조로 바꿉니다.

## 2. 상단 슬롯과 설정 버튼

- 새 차시와 큰 수정 차시는 `<main class="game" data-settings-standard="modal-controls">`를 선언합니다.
- 오른쪽 위 전역 버튼은 `.stage-shell` 안의 상단 오른쪽 보조 슬롯에 둡니다.
- `.stage-shell`에는 `--sound-button-size`, `--sound-gap`, `--sound-reserve`를 두고, `.top-row`와 `.hud`가 같은 예비 공간을 비웁니다.
- 새 표준 버튼은 원형 SVG 톱니바퀴 `.settings-toggle`입니다. 화면에 `설정`이나 `소리` 글자를 직접 보이지 않게 하고, `aria-label="설정 열기"`로 이름을 줍니다.
- 설정 모달은 `.stage-shell` 안에 `role="dialog" aria-modal="true" aria-labelledby="settingsTitle"`로 두고, `배경 소리`, `효과 소리`, `방법 다시 보기`, `처음부터`, `닫기`만 짧게 보입니다.
- `배경 소리`와 `효과 소리`는 분리합니다. 공통 저장 키는 `mathmon-audio-bgm-enabled`, `mathmon-audio-sfx-enabled`입니다.
- `방법 다시 보기`는 설명 화면을 복습 모드로 열고, 버튼 문구를 `계속하기`로 바꾼 뒤 기존 화면으로 돌아옵니다. `처음부터`는 확인 상태를 거친 뒤 첫 화면으로 돌아갑니다.
- 아직 이관하지 않은 차시는 레거시 `.sound-toggle`을 보존할 수 있지만, 새 복제 기준으로 삼지 않습니다.
- `.top-row`는 `top`, `left`, `right`, `height`, `gap`을 명시합니다. 화면별 `transform`, active-screen별 위치 보정, viewport fixed 배치는 쓰지 않습니다.
- 배지는 `flex: 0 0 auto`, `width: fit-content`, `max-width: max-content`를 기본으로 하며, 불필요한 `min-width`로 빈 칸을 만들지 않습니다.

## 3. 문제 화면 3초 규칙

- 문제 화면은 초3 학생이 `3초` 안에 지금 할 일을 알 수 있어야 합니다.
- `3초 안에 할 일을 안다`와 `생각하지 않아도 된다`를 혼동하지 않습니다. 학생은 수·위치·관계·순서·분해 방법 중 하나를 판단해 답을 직접 만들어야 합니다.
- 시스템이 정답을 자동으로 배분하고 학생은 끝날 때까지 반복 클릭만 하는 직접 조작은 실패입니다. 숫자를 가리고도 완료할 수 있으면 다시 설계합니다.
- `1부터 넣고 확인`, `2를 넣고 확인`처럼 검사 버튼을 반복해 정답을 찾을 수 있으면 실패입니다. 학생이 목적지·관계·수를 먼저 완성하고 확인은 마지막 검증만 맡게 합니다.
- 직접 조작에는 그럴듯한 오답 상태와 확인 행동을 둡니다. 애니메이션은 학생이 만든 답을 검증하며, 정답을 대신 수행하지 않습니다.
- 수학적 판단 횟수와 물리 입력 횟수를 따로 셉니다. 답을 이미 정한 뒤 같은 뜻의 물건 놓기를 세 번 이상 반복한다면 새 판단이 생기는 이유를 적고, 이유가 없으면 시스템의 확인 애니메이션으로 줄입니다.
- 학생이 답을 먼저 정한 뒤 시스템이 그 답대로 물건을 차곡차곡 보여 주는 것은 확인입니다. 학생의 답을 받기 전에 시스템이 정답을 나누는 것은 자동 풀이이므로 금지합니다.
- 문제 은행 전체에서 정답 경로의 최소·중앙값·평균·최대 탭·드래그·키 입력 수를 기록합니다. 입력이 기준 차시보다 많다면 늘어난 입력마다 어떤 새 수학 판단이 필요한지 확인합니다.
- 설명 화면은 조작법뿐 아니라 `왜 이 단계를 하는지`를 보여 줍니다. 학생이 단계별 값이 최종 답의 어디에 들어가는지 말할 수 없으면 설명을 다시 만듭니다.
- 기본으로 펼쳐 둘 학습 요소는 큰 문제, 현재 단계 조작판, 한 줄 지시문, 선택지뿐입니다.
- 이전 단계는 작은 완료 칩으로 접고, 다음 단계는 `?`나 잠금 상태로 둡니다.
- 정답을 고른 뒤에는 선택지 색만 바꾸고 바로 넘기지 않습니다. 계산판·칸·블록이 정답값으로 바뀌고, 짧은 확인 문구가 잠깐 보인 뒤 다음 단계로 갑니다.
- 마지막 단계 뒤에는 보상 모달이 뜨기 전에 완성식이나 완성값을 먼저 보여 줍니다. 자동으로 모달을 덮기보다 학생이 `바람 보기`, `상자 열기`처럼 차시 소재에 맞는 버튼을 눌러 보상으로 넘어가는 흐름을 우선합니다. 모달이 정답 확인을 가리면 실패입니다.
- 힌트는 기본으로 닫아 두고, 열어도 지금 단계 힌트 1개만 보입니다.
- 원리는 긴 설명보다 블록, 칸, 스티커, 화살표처럼 바로 보이는 조작으로 보여 줍니다.
- 선택지와 큰 문제 사이에 장식, 보상판, 긴 설명을 끼우지 않습니다.
- 일반 플레이에서는 `시작`과 `다시하기`를 누를 때마다 새 run seed로 문제은행을 다시 섞습니다. 직전 판의 seed를 그대로 재사용하지 않습니다.
- 주소에 예전 `?seed=61`이 남아 있어도 학생 플레이를 고정하지 않습니다. 고정 seed는 `qa`, `qaProblem`, `audioSmoke`처럼 QA 표식이 함께 있을 때만 허용합니다.
- 공용 엔진 차시는 `createRunSeed()`에서 `crypto.getRandomValues()`를 우선 사용하고, 연속 두 판의 seed가 같으면 즉시 다른 값으로 보정합니다.
- 전체 차시 랜덤성 회귀는 루트에서 `node scripts/check-run-randomness.mjs`로 확인합니다.

## 4. 거리형 게임의 전체 목표 지도

- 섬, 행성, 층, 다리처럼 멀리 가는 게임은 전체 목표 지도를 작게라도 계속 보여 줍니다.
- 지도는 현재 위치, 다음 목표, 마지막 목표를 한눈에 알게 해야 합니다.
- 지도는 보조 정보입니다. 큰 문제, 현재 단계, 선택지를 밀어내면 크기를 줄이거나 접습니다.
- 낮은 결과도 도착한 곳으로 보여 줍니다. 빈손이나 실패처럼 보이는 결과 화면은 쓰지 않습니다.

## 5. 보상과 확률

- 랜덤 보상은 유지합니다. 같은 계산을 해도 결과가 달라져야 다시 해 볼 이유가 생깁니다.
- 정답을 많이 맞히면 유리해야 하지만, 정답만으로 최고 보상이 쉽게 보장되면 안 됩니다.
- 최고 보상은 어렵게 둡니다. 한 판으로 자주 닿으면 재도전 동기가 약해집니다.
- 틀려도 무언가는 얻습니다. 낮은 결과에도 보이는 변화와 다시 할 길을 남깁니다.
- 한 차시의 중심 보상은 하나만 둡니다. 별도 코인, 랭킹, 수집장을 동시에 앞세우지 않습니다.
- **숫자 변화만 보여 주지 말고 현재 보상과 다음 목표를 연결합니다.** 보상 화면은 이번에 얻은 장소·물건·단계를 보여 주고, 결과 화면은 다음에 노릴 목표를 이어 보여 줍니다.
- `empty` 사건은 누적값을 0으로 만들지 않고 이번 변화만 0으로 둡니다. 오답 손해는 흔한 정답 보상의 최소 증가량보다 커지지 않게 제한합니다.

## 6. 보상 Stage

- 새 차시와 큰 보상 수정은 `마지막 정답 확인 → 닫힌 보상 → 사건 공개 → 현재 단계·다음 목표 → 다음 문제/결과`의 `stage-reveal` 흐름을 기본으로 합니다.
- 닫힌 보상과 열린 보상은 같은 `16:10` Stage 안에서 전환합니다. 작은 중앙 모달은 기존 차시 호환용으로만 둡니다.
- 보상 루트에는 `data-reward-stage-story="true"`를 두고 공통 QA가 닫힌·열린 상태를 실제로 확인하게 합니다.
- 버튼을 제외한 보이는 텍스트는 큰 결과 라벨 또는 변화량 1개로 줄입니다.
- 제목, 본문, 버튼이 같은 뜻을 반복하면 실패입니다. 버튼은 `다음`, `보기`, `다시`처럼 다음 행동만 말합니다.
- 이벤트 차이는 긴 설명보다 이미지, 아이콘, 변화량, 짧은 모션으로 구분합니다.
- 보상 Stage에 매스몬이 보이면 생성 이미지 안에 처음부터 함께 들어가야 합니다. 배경을 먼저 만들고 기존 매스몬 PNG/WebP를 위에 얹는 합성은 쓰지 않습니다.
- 폴짝 뛰는 실제 이동이 꼭 필요하면 기존 캐릭터 컷아웃을 움직이지 말고, 생성형 스프라이트 프레임이나 짧은 영상처럼 그 모션 전용 자산을 따로 만듭니다.
- 효과가 문제, 선택지, 설정/소리 버튼, 배지, 다음 조작부를 가리면 줄입니다.
- 설정·상태 이미지·확률·공정성·검증의 상세 기준은 `.claude/skills/eduitit-mathmon-lesson/SKILL.md`의 「기본 Stage-Reveal 보상 계약」을 따릅니다.

## 7. 결과 화면과 도착 전 재기

- 결과 화면은 최종 점수보다 학생이 얻은 장소나 보상을 먼저 크게 보여 줍니다.
- 결과 공개 전에는 짧게 도착 전 재기, 길 스캔, 게이지 채우기 같은 기대 연출을 둘 수 있습니다.
- 결과 공개 뒤에는 한 줄 칭찬, 다시 하기 버튼, 간단한 요약만 남깁니다.
- 결과 배경은 생성형 bitmap 장면 1장을 기본으로 하고, 섬 이름·도착 라벨·칭찬 문구·다시 하기 버튼처럼 매 판 똑같은 요소는 이미지 안에 포함합니다.
- HTML/CSS 오버레이는 정답 수·점수처럼 매 판 달라지는 값만 맡깁니다.
- 결과 등급처럼 4단계 안에서만 바뀌는 라벨은 "매 판 달라지는 텍스트"가 아닙니다. 단계별 생성 이미지나 독립 타이틀/버튼 자산으로 바꾸고, HTML은 숨김 접근성 텍스트와 실제 점수만 남깁니다.
- 생성 이미지 위에 큰 반투명 카드, blur 패널, CSS 제목/본문, CSS로 그린 큰 다시 버튼을 올리면 실패입니다. 이 표준을 쓰는 차시는 `<main class="game">`에 `data-result-visual-standard="generated-assets"`를 붙여 공통 하네스 검사를 받습니다.
- 결과 화면을 통째 이미지로 요구받은 차시는 `data-result-render-mode="fullscene-score-slot"`를 함께 붙입니다. 이 모드의 보이는 HTML은 동적 점수 숫자 1개와 투명 다시하기 hitbox만 허용하며, 점수 박스 라벨은 이미지와 HTML 양쪽에서 보이지 않아야 합니다. 점수 중앙 정렬은 CSS 좌표 일치가 아니라 스크린샷 픽셀에서 숫자 글자 중심과 빈 점수칸 중심을 비교해 확인합니다.
- 결과 화면에 생성형 보상 장면과 정확한 동적 UI가 함께 필요하면 `data-result-render-mode="hybrid-generated-dynamic"`을 씁니다. 생성 이미지는 장면과 감정을 맡고, SVG 오버레이는 정답 수·결과명·짧은 진행값·버튼처럼 정확히 맞아야 하는 값만 맡습니다. 보이는 값은 1-3개로 줄이고, 4개 이상이면 결과 화면이 아니라 순위판/대시보드로 분리합니다.
- 3학년 2학기 1~6단원은 랭킹·전국 순위 문구, 진입 버튼, 순위 화면, 점수 제출·조회 요청을 만들거나 노출하지 않습니다. 기존 코드가 남아 있어도 `scoreboard.enabled=false`를 기본으로 하고 학생 흐름과 네트워크 요청에서 닿을 수 없게 합니다.

## 8. 시각 자산 기준

- 첫 화면, 설명용 대표 장면, 보상 장면, 결과 장면은 생성형 bitmap 이미지를 기본 시각 자산으로 삼습니다.
- CSS와 HTML은 읽기 쉬운 문제, 선택지, 버튼, 배지, 오버레이, 상태 변화, 모션에 씁니다.
- 문제 풀이에 필요한 수식과 선택지는 이미지 안에 굽지 않습니다.
- 생성 이미지 위에 별도 캐릭터 파일을 붙여 장면을 만든 것처럼 보이게 하지 않습니다. 첫 화면을 포함해 캐릭터가 장면의 일부여야 하면 프롬프트 단계에서 함께 생성합니다.
- 생성 원본 PNG와 배포용 WebP를 구분해 보관하고, 문서에 파일명과 오버레이 범위를 남깁니다.

## 9. 겹침/넘침 고정 규칙

- 버튼, 카드, 배지, 선택지 타일, 보상 모달, 결과 카드 안의 글자와 숫자는 상자 밖으로 나가면 실패입니다.
- 잘림, 삐져나옴, 줄 사이 겹침, 숫자나 기호가 칸 밖으로 밀리는 상태도 실패입니다.
- 사용자가 발견한 실패 화면의 실제 viewport·DPR·Stage rect는 해당 차시의 영구 회귀 항목으로 남깁니다. 이후 모든 큰 수정에서 같은 화면 크기와 같은 상태를 다시 검사합니다.
- 자동 검사는 텍스트·버튼뿐 아니라 SVG/Canvas 표면, 카드, 지시문, 피드백, 선택지/숫자판의 실제 rect를 재고 형제 영역 교차 0px를 확인합니다.
- z-index로 가리는 임시 처리는 금지합니다. grid/flex track, contain, overflow 경계와 명시적인 간격으로 구조를 분리합니다.
- 문제, 선택지, 설정/소리 버튼, 브랜드/단원 배지, 진행 상태, 보상 버튼, 다음 단계 조작부는 서로 덮으면 안 됩니다.
- 고정 포맷 UI에는 `aspect-ratio`, 명시적인 grid/flex track, `min/max-width`, `min/max-height`를 둡니다.
- 긴 문구는 먼저 학생 말로 짧게 줄입니다. 그래도 필요하면 `overflow-wrap`, 적절한 `line-height`, `max-width`, `fit-content`로 상자 안에서 자연스럽게 줄바꿈합니다.
- 간격과 컨테이너 크기에는 필요한 경우 `clamp()`를 쓰되, viewport-width 기반 글자 크기 축소로 억지로 맞추지 않습니다.
- hover, focus, 정답, 오답, disabled, 보상, 결과 상태에서도 선택지와 버튼 크기가 흔들리지 않아야 합니다.

## 10. Humanizer QA

- 학생에게 보이는 한국어를 만들거나 크게 바꾼 뒤에는 `/Users/yubyeongju/.agents/skills/humanizer/SKILL.md` 기준으로 점검합니다.
- 첫 화면 목표, 설명 단계, 문제 지시문, 선택지, 힌트, 피드백, 보상 모달, 결과 칭찬, 다운로드 카드 문구를 봅니다.
- 한 문장에는 행동 하나만 담습니다. 초3 학생이 소리 내어 읽을 때 숨이 차면 줄입니다.
- 제작자에게 편한 말보다 화면 속 물건과 행동으로 말합니다. 예: `먼저 63x3을 골라요`, `어디까지 갔는지 볼까요?`
- 문구를 줄인 뒤에도 실제 화면 캡처로 줄바꿈, 버튼 폭, 카드 안 텍스트를 다시 봅니다.

## 11. 스크린샷 QA 목록

- 컴퓨터 화면과 태블릿 가로 화면을 모두 확인합니다.
- 기본 캡처: 첫 화면, 설명 화면, 문제 1단계, 문제 2단계, 힌트/오답 상태, 보상 모달, 결과 공개 전, 결과 화면.
- 각 캡처에서 텍스트 겹침/넘침, 클릭 영역 충돌, 설정/소리 버튼과 배지 충돌, 선택지·보상 버튼 크기 흔들림을 확인합니다.
- 결과 화면 캡처에서는 보이는 CSS 결과 카드, CSS 제목/본문, CSS 버튼 장식이 남아 있지 않은지도 확인합니다. `fullscene-score-slot` 모드라면 `6/10` 같은 점수 숫자 외 보이는 HTML 텍스트가 없어야 합니다.
- `REPORT.md`에는 확인한 화면 크기, 화면 상태, 텍스트 겹침/넘침 QA 결과를 남깁니다.

## 12. 교과서식 계산·찰나 화면 체크

- [ ] 이번 차시의 계산 순서와 공간 배치를 실제 교과서 풀이와 대조했습니다.
- [ ] 세로 계산의 일·십·백 자리, 나눗셈의 몫·곱한 값·빼는 선·남은 수·내려오는 수가 같은 자리 열에 맞습니다.
- [ ] 계산판의 자리 숫자와 학생이 고르는 실제 값을 구별했습니다. 예: 몫 칸 `1`, 선택지·확인 문구 `몫 10`.
- [ ] `7십`, `2십` 같은 낯선 표기 대신 `70`, `20`처럼 학생이 평소 쓰는 수로 말합니다.
- [ ] 한 개의 예시뿐 아니라 문제 은행 전체의 지시문·선택지·오답·정답 문구를 검사했습니다.
- [ ] 문제 화면 왼쪽 위에 `에듀잇티 수학 게임`, 같은 상단 줄에 단원 배지가 보입니다.
- [ ] 큰 문제의 숫자와 `÷`·`×` 사이 간격, 나눗셈 괄호선과 몫 칸 간격, 글자 기준선이 자연스럽습니다.
- [ ] 오답 상태를 따로 캡처했고, 틀린 이유가 현재 계산판이나 물건으로 보입니다.
- [ ] 각 단계 정답 확인을 따로 캡처했고, 고른 답이 계산판·칸·물건에 들어간 뒤 짧은 확인 문구가 보입니다.
- [ ] 지시문과 피드백은 같은 자리를 번갈아 써도 동시에 겹치지 않으며, `?` 칸·숫자·화살표를 가리지 않습니다.
- [ ] 다음 단계로 넘어가는 찰나와 마지막 완성값을 따로 확인했고, 보상 모달이 먼저 덮지 않습니다.
- [ ] 현재 코드보다 오래된 스크린샷은 현재 증거에서 빼고 `_archive/`로 구분했습니다.
- [ ] `1280×800`, `1024×768`, 사용자가 문제를 발견한 창 크기와 비슷한 화면에서 같은 상태를 확인했습니다.
- [ ] 실제 렌더에서 핵심 숫자·지시문·선택지·작은 라벨의 글자 위계가 유지되고, 터치 영역은 최소 `42×42px`입니다.
- [ ] `REPORT.md`에 화면 크기, 상태별 캡처 파일, 남은 P0/P1 문제를 적었습니다.

## 13. 기준 차시 비교 QA

- 새 차시와 큰 수정 차시는 기본적으로 3학년 2학기 1단원 1~4차시를 강점별로 비교합니다.
- 1차시에서는 보상 손익과 오답 손해, 2차시에서는 계산 구조와 진행감, 3차시에서는 현재 위치와 다음 목표, 4차시에서는 단계 확인과 검증 완결성을 봅니다.
- 수학적 판단, 오답 이유, 정답값 반영, 마지막 완성식, 랜덤 보상, 재도전 동기는 모두 통과해야 합니다.
- 설명 그림의 물건 수와 묶음 수가 실제 식과 맞는지 따로 셉니다. 그림이 멋져도 수량 관계가 틀리면 다시 만듭니다.
- 자동 검사와 실제 화면 확인을 나눠 기록합니다. 오답 양방향과 마지막 확인 상태는 우연한 흐름에 맡기지 않고 각각 재현합니다.
- 수학적 판단 횟수와 물리 입력 횟수를 따로 적고, 문제 은행의 최소·중앙값·평균·최대 입력 수를 계산합니다.
- `REPORT.md`의 PASS는 현재 코드에서 명령을 다시 실행해 종료 코드 0을 확인한 항목만 적습니다. 전용 QA 실패를 공통 계약 PASS로 덮지 않습니다.
- 현재 구현과 맞지 않는 예전 스크린샷·전용 QA·문서는 `_archive/`로 옮기고 현재 증거와 섞지 않습니다.
- 결과는 차시 폴더의 `BENCHMARK_AUDIT.md`에 `통과/일부 통과/부족/선택`, `P0/P1/P2`, 다음 행동으로 남깁니다.
