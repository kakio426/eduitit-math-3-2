# 매스몬 별 줍기 구현 보고서

현재 증거 실행일: 2026-07-22
동행 매스몬 팩: `base-pack`

## 구현 결과

문제 화면의 원천 구조를 `빈 계산 막대 + 작은 현재→다음 제목`에서 `상시 별자리판 + 실제 별 조작판`으로 교체했습니다.

- 왼쪽 `300×656px` 슬롯에는 여섯 단계 유니콘 별자리판이 항상 보입니다.
- 오른쪽에는 처음 수만큼 별이 가운데 맞춘 10~11열 격자로 보입니다. 20~60개 문제에서는 별을 더 크게 보여 줍니다.
- 몫 보기는 계산 결과를 숨기고 `6 / 7 / 8`과 `묶음`만 보여 줍니다.
- 사용자 제보 문제 `50÷4`에서는 별 50개를 10열×5행으로 놓고, SVG 기준 x `222~798`, y `92~276`, 글자 크기 `28px`로 고정했습니다.
- 답을 고른 뒤에만 별이 묶음 캡슐로 바뀝니다. 16묶음 이하는 큰 4열 배치, 그보다 많으면 최대 8열×4행 배치를 씁니다.
- `50÷4`의 12묶음은 `126×58px` 캡슐을 4열×3행으로 확대하고, `4×12=48`은 `34px`로 키웠습니다.
- 나머지 대기 화면에는 `2개`를 미리 쓰지 않습니다. 별 두 개만 보여 주고, 학생이 선택한 뒤에만 개수를 표시합니다.
- 작은 몫은 더 만들 수 있는 한 묶음, 큰 몫은 부족한 별자리를 현재 판에서 보여 줍니다.
- 나머지 단계에서는 이전 묶음을 접고 남은 별을 확대합니다.
- 마지막 식을 확인한 뒤 학생이 `별병 열기`를 눌러 보상으로 갑니다.

이 흐름으로 별·별병·유니콘자리는 배경 장식이 아니라 `나누기 → 나머지 → 별빛 → 결과`를 잇는 하나의 세계가 됐습니다.

## 수학·입력 계약

- 모든 생성 문제는 `처음 수 = 나누는 수×몫+나머지`를 만족합니다.
- 나머지는 항상 0보다 크고 나누는 수보다 작습니다.
- 몫 선택지는 `q-1`, `q`, `q+1`이고, 학생이 고른 뒤에만 곱과 차이를 계산판에 보여 줍니다.
- 최대 문제 밀도는 99개 흩어진 별과 32개 캡슐이며, 고정 계산판 안에 들어갑니다.
- 정답 경로의 수학 입력은 문제당 2회입니다.
- 몫 정답 확인은 1.2초 머물고, 마지막 완성식은 수동 보상 열기 전까지 유지됩니다.

모델 QA는 200개 시드×10문제를 검사해 수식, 나머지 범위, 선택지 곱, 부족 수, 오답 관계, 8×4 캡슐 경계를 확인했습니다.

## 계산과 보상의 연결

- 처음에 맞힌 경우 실제 나머지 개수만큼 별이 왼쪽 별병으로 이동합니다.
- 기존 `remainderBonus`와 이동 별 개수는 같은 `problem.remainder`를 씁니다.
- 랜덤 사건은 기존 확률과 문턱을 유지합니다.
- 보상 공개 뒤 실제 별빛 변화량 하나만 보여 줍니다.
- 보상이 반영되면 왼쪽 별자리 이미지가 즉시 새 단계로 바뀝니다.
- `prefers-reduced-motion`에서는 이동 애니메이션을 없애고 별병과 별자리 밝기 변화만 남깁니다.

## 상태 이미지 세트 QA

### 문제 화면

- 6장 모두 `600×1312px`
- 브라우저 표시 슬롯은 1280×800 Stage 기준 `300×656px`
- `naturalWidth/naturalHeight`, `object-fit: cover`, 표시 위치와 비율을 자동 검사했습니다.
- 컨택시트 `play-constellation-contact-sheet.png`에서 유니콘몬, 별병, 별자리 기준선, 상하 잘림을 전수 확인했습니다.

### 결과 화면

- 장면 6장 모두 `1280×800px`
- 생성형 제목 6장과 결과 장면 6장을 각 결과 ID에 연결했습니다.
- 컨택시트: `result-unicorn-contact-sheet.png`, `result-title-unicorn-contact-sheet.png`
- 브라우저에서 여섯 결과를 강제로 재현해 장면과 제목을 각각 캡처했습니다.

### 설명 화면

- `tutorial-page-1-generated.webp`, `tutorial-page-2-generated.webp`는 모두 `1280×800px`입니다.
- 첫 장은 `6×7=42`, 7묶음, 남은 별 5개가 정확히 맞습니다.
- 둘째 장은 남은 별, 별병, 유니콘자리 여섯 단계를 한 방향으로 보여 줍니다.

## Humanizer 학생 문구 QA

첫 화면 목표, 설명, 문제 지시, 선택지, 오답, 정답 확인, 완성식, 보상, 결과 이름을 다시 읽었습니다.

- `별 47개를 6개씩 묶으면 몇 묶음까지 만들 수 있을까요?`
- `6개씩 7묶음, 42개예요.`
- `한 묶음을 더 만들 수 있어요.`
- `별 1개가 모자라요.`
- `묶고 남은 별을 세어 봐요.`
- `6개면 한 묶음이에요.`

한 문장에는 행동 하나만 남겼습니다. `핵심`, `등급`, `게이트`, `오브젝트` 같은 제작자 말은 학생 화면에 없습니다. 설명 대체 문구의 피동형도 `남은 별을 별병에 담으면 유니콘자리가 빛나요.`로 고쳤습니다. 의미·수치·격식은 그대로 유지했습니다.

## 텍스트 넘침·요소 겹침 QA

- 화면 크기: `1280×800`, `1024×768`, `1780×1112`, `1082×897`(DPR 2)
- 확인 상태: 표지, 설정, 설명 1·2, 문제 대기, 20개·99개 별 경계, 32캡슐 경계, 몫 너무 작음·너무 큼·정답, 나머지 대기·오답·정답, 완성식, 보상 닫힘·열림, 여섯 결과, 순위판
- 결과: 텍스트 overflow 0건, 깨진 이미지 0건, 별자리판과 계산판 교차 0px
- 정렬: 몫 확인의 캡슐 줄 중심 x=`350`, 나머지 계산판의 캡슐 줄 중심 x=`300`, 오른쪽 제목·별 줄 중심 x=`729`를 실제 SVG 좌표로 검사했습니다. 대기 상태의 개수 텍스트 0개와 선택 뒤 개수 표시도 함께 확인했습니다.
- 선택 카드: 높이 112px 고정, 묶음 수 48px·단위 24px(1024px 이하에서는 42px/21px), 터치 영역 42×42px 이상
- 상단 배지·문제 수·단원 배지는 같은 기준선이며 설정 버튼 예약 공간을 침범하지 않습니다.

## 현재 자동 검증

2026-07-22 현재 코드에서 아래 명령을 다시 실행했습니다.

- `node scripts/qa-lesson-model.mjs 3-2-2-3-mathmon-star-pickup` → `QA_LESSON_MODEL: PASS`
- `node scripts/qa-lesson2-star-pickup.mjs 3223` → `QA_LESSON_FLOW: PASS`
- `node scripts/check-stage-ratio.mjs` → `Stage ratio contract OK (21 lesson packages, 16:10 / 1280x800)`
- `node scripts/check-lesson-contract.mjs 3-2-2-3-mathmon-star-pickup` → `CHECK_LESSON_CONTRACT: PASS`
- `node scripts/check-lesson-visual-contract.mjs 3-2-2-3-mathmon-star-pickup` → `LESSON_VISUAL_CONTRACT: PASS (1)`

자동 검사와 별도로 현재 PNG를 눈으로 확인했습니다. 기준 증거는 `screenshots/engine-flow-*`이며, 이전 판정은 `_archive/pre-unicorn-redesign-2026-07-14/`와 섞지 않습니다.
