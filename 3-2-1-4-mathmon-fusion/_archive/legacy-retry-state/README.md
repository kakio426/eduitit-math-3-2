# Legacy retry state

2026-07-10에 현재 실행 경로에서 분리한 예전 실패 결과 자산입니다.

합체 힘은 0점 아래로 내려가지 않고, 현재 등급표는 0점부터 소형 로봇을 보여 줍니다. 따라서 예전 `retry` 결과 분기는 도달하지 않았습니다. 기록 보존을 위해 자산과 캡처를 삭제하지 않고 이 폴더로 옮겼습니다.

현재 실행 기준은 상위 폴더의 `result-small-generated.webp`부터 `result-legend-generated.webp`까지 6단계입니다.
