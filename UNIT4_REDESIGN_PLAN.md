# 4단원 분수 게임 고도화 계획

## 진행 기준

- 대상: `3-2-4-1`부터 `3-2-4-4`
- 복제 기준: Mathmon Engine v1 + 3단원 이미지형 문제 무대
- 화면: 생성 이미지 세계 + 정확한 SVG 분수 모델 + 접근성 HTML 버튼
- 문제 화면: 큰 질문, 현재 분수 조작판, 한 줄 지시·피드백, 선택지
- 숨김: 문제 중 점수판, 결과 등급, 긴 설명, 드래그 전용 자리, 다음 단계 예고

## 기존 화면 진단

네 차시 모두 분수 생성 규칙과 대표 오개념은 갖추고 있습니다. 하지만 검은 CSS 패널, 하단 점수·등급 장치, 카드와 놓을 자리가 분수 그림보다 먼저 보입니다. 드래그와 탭을 동시에 안내해 지금 할 행동도 길어집니다. 문제 무대는 밝은 생성 이미지로 바꾸고 수학 판단은 SVG로 남깁니다.

## 차시별 한 행동

| 차시 | 핵심 지식 | 문제 화면의 한 행동 | 대표 오개념 |
| --- | --- | --- | --- |
| 4-1 피자 분수 가게 | 색칠된 조각 수/전체 조각 수 | 피자와 같은 분수 카드를 고른다 | 분자·분모 뒤바꿈, 색칠 안 된 조각 세기 |
| 4-2 분수만큼 담기 | 전체÷분모×분자 | 현재 단계의 묶음 수를 고른다 | 분모를 한 묶음 수로 사용, 한 묶음만 답함 |
| 4-3 분수 분류 컨베이어 | 진분수·가분수·대분수 | 분수의 이름을 고른다 | 가분수와 대분수 혼동, 분자=분모 오판 |
| 4-4 분수 줄다리기 | 분모 같은 분수·단위분수 비교 | 더 길게 채워진 분수를 고른다 | 단위분수에서 분모가 큰 쪽을 선택 |

## 공통 시각 구조

- 차시별 `problem-stage-generated.webp`를 1280×800으로 생성합니다.
- 생성 이미지는 공간·빛·테마 소품만 담당합니다.
- SVG는 조각 수, 묶음, 분수 막대, 분자·분모 관계만 담당합니다.
- 색은 선택 결과의 보조 신호로만 쓰고 조각 수·위치·길이·라벨을 함께 보여 줍니다.
- 오답 뒤에는 선택한 관계와 이유 한 줄만 보입니다.
- 정답 뒤에는 완성된 분수나 식을 먼저 보여 준 뒤 학생이 보상 버튼을 누릅니다.

## 자산 계약

| 파일 | 용도 | 크기 |
| --- | --- | --- |
| `problem-stage-source.png` | imagegen 생성 원본 | 생성 원본 크기 |
| `problem-stage-generated.png` | 16:10 작업 원본 | 1280×800 |
| `problem-stage-generated.webp` | 학생용 문제 배경 | 1280×800 |

문제 배경에는 글자, 숫자, UI 카드, 버튼, 분수 정답 그림을 넣지 않습니다. 기존 커버·제목·시작 버튼·결과 등급 이미지는 유지합니다.

## 검증

- 각 단계 정답은 정확히 1개
- 모든 핵심 오답에 `misconceptionId`와 짧은 피드백 존재
- 현재 단계 외 계산판·점수판·등급 미리보기 없음
- 정답 뒤 SVG가 실제 정답 상태로 바뀐 뒤 보상 버튼 노출
- 1280×800, 1024×768 전체 흐름에서 넘침·겹침·이미지 누락 0건
- 차시별 소스 계약 테스트, `check-lesson-contract`, `check-stage-ratio` 통과

## 범위 밖

- 1·2·3·5·6단원 변경
- 새 매스몬 팩 제작
- 점수판 API·백엔드 변경
- 배포·푸시

## 구현 완료 기록 (2026-07-10)

- 4-1: 이미지형 피자 가게 + 색칠된 조각/전체 조각 표지 + 분수 4선택
- 4-2: 이미지형 과일 작업장 + `전체 ÷ 분모`와 `한 묶음 × 분자` 순차 선택
- 4-3: 이미지형 분류 작업장 + 분수 표기/막대 모형 + 이름과 관계 3선택
- 4-4: 이미지형 야외 경기장 + 같은 전체 길이의 분수 막대 2선택
- 네 차시 모두 Mathmon Engine v1 소스, 대표 오개념 테스트, 정답 확인 상태, 학생 확인 버튼을 연결했습니다.
- 브라우저 QA 기준은 1280×800과 1024×768이며, 각 차시 `screenshots/engine-flow-*`에 결과를 보관합니다.
