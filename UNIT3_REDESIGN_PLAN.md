# 3단원 원 게임 고도화 계획

## 진행 기준

- 대상: `3-2-3-1`부터 `3-2-3-4`
- 골드 스탠더드: `3-2-3-1-mathmon-target-hit`
- 공통 구조: Mathmon Engine v1 + 차시별 SVG 원 조작판
- 화면: 생성 이미지 세계 + 정확한 SVG 수학 관계 + 접근성 HTML hitbox
- 기본 문제 화면: 큰 질문, 현재 원 조작판, 한 줄 지시/피드백, 선택지
- 제외: 문제 중 점수 미터, 등급 트랙, 긴 설명판, 다음 단계 예고

## 진행 상황

- 3-1 표적 맞히기: 엔진 소스 이관, 문제 배경 생성, 독립 미니 원 선택지, 정답 확인 화면, 데스크톱·태블릿 QA 완료
- 3-2 컴퍼스 마법진: 엔진 소스 이관, 문제 배경 생성, 컴퍼스 벌림 선택지, `=`·`≠` 확인 화면, 데스크톱·태블릿 QA 완료
- 3-3 두 배 다리: 엔진 소스 이관, 문제 배경 생성, 원 안의 두 반지름·다리 길이 선택, `=`·`≠` 확인 화면, 데스크톱·태블릿 QA 완료
- 3-4 원 무늬 디자이너: 엔진 소스 이관, 문제 배경 생성, 독립 미니 무늬 선택지, 가로·대각선 규칙, 데스크톱·태블릿 QA 완료

## 완료 상태

3단원 1~4차시 모두 이미지 중심 문제 무대, 한 화면 한 행동, 색에 의존하지 않는 SVG 수학 관계, 오답 이유 1개, 정답 확인 뒤 보상 열기 흐름으로 이관했습니다.

## 기존 화면 진단

네 차시 모두 문제 생성과 대표 오답은 갖추고 있지만, 검은 CSS 패널과 왼쪽 점수 장치가 원 도형보다 먼저 보입니다. 3-1은 한 원에 후보 선과 글자가 겹쳐 중심·반지름·지름의 관계를 읽기 어렵습니다. 3-2~3-4도 같은 화면 골격을 복제해 이미지 자산이 커버와 결과에만 머물러 있습니다.

## 차시별 한 행동

| 차시 | 핵심 지식 | 문제 화면의 한 행동 | 대표 오개념 |
| --- | --- | --- | --- |
| 3-1 표적 맞히기 | 중심·반지름·지름의 위치 관계 | 묻는 관계가 그려진 원을 고른다 | 테두리 점=중심, 지름=반지름, 중심을 지나지 않는 현=지름 |
| 3-2 컴퍼스 마법진 | 컴퍼스 벌림=반지름 | 반지름만큼 벌린 컴퍼스를 고른다 | 지름만큼 벌림 |
| 3-3 두 배 다리 | 지름=반지름×2 | 두 배 또는 반의 길이를 고른다 | 주어진 수를 그대로 사용 |
| 3-4 원 무늬 디자이너 | 같은 크기·같은 간격의 원 무늬 | 규칙에 맞는 다음 원 자리를 고른다 | 다른 간격, 다른 줄, 다른 크기 |

## 3-1 골드 스탠더드

### 수학 설계

- 한 판 10문제: 중심 3~4개, 반지름 3~4개, 지름 3~4개
- 선택지는 4개의 독립된 미니 원 그림으로 구성합니다. 후보를 한 원 위에 겹쳐 그리지 않습니다.
- 중심 문제: 한가운데 점 1개, 테두리 점, 치우친 안쪽 점, 바깥 점
- 반지름 문제: 중심→테두리 선분, 지름, 중심을 지나지 않는 현, 테두리에 닿지 않는 선분
- 지름 문제: 중심을 지나는 테두리→테두리 선분, 반지름, 중심을 지나지 않는 현, 테두리에 닿지 않는 선분
- 정답 뒤 선택한 미니 원을 큰 원으로 확대하고 한 줄 관계를 표시한 뒤 `점수 보기`로 보상에 들어갑니다.

### 시각 구조

- 배경: `problem-stage-generated.webp`, 밝은 판타지 표적 훈련장
- 생성 이미지는 공간·빛·목재·표적 발사 장치만 담당합니다.
- SVG는 원, 중심점, 선분과 정답 확인 관계만 담당합니다.
- 선택지 의미는 색으로 구별하지 않고 점과 선의 시작·끝 위치로 구별합니다.
- 정답/오답 색은 선택 뒤 상태를 돕는 보조 신호로만 사용합니다.

### 보상

- 중심 보상은 `표적 점수` 하나만 유지합니다.
- 정답 문제는 증가·감소·대량 증가·정중앙·0·무지개 중 하나가 나옵니다.
- 오답을 거친 문제는 점수 감소만 적용합니다.
- 문제 화면에서는 현재 점수와 결과 등급을 숨깁니다.

## 자산 계약

| 파일 | 용도 | 크기 |
| --- | --- | --- |
| `problem-stage-source.png` | imagegen 생성 원본 | 생성 원본 크기 |
| `problem-stage-generated.png` | 16:10 작업 원본 | 1280×800 |
| `problem-stage-generated.webp` | 학생용 문제 배경 | 1280×800 |

기존 커버·제목·시작 버튼·결과 등급 이미지는 유지합니다. 문제 배경에는 글자, 숫자, UI 카드, 버튼, 점수판, 원 정답 그림을 넣지 않습니다.

## 검증

- 각 문제 정답은 정확히 1개
- 모든 오답에 `misconceptionId`와 짧은 피드백 존재
- 중심·반지름·지름이 색 없이 기하 관계만으로 구별됨
- 문제 화면에 보상 진행판과 결과 미리보기 없음
- 정답 뒤 원 그림이 실제 정답 관계로 바뀐 뒤 보상 버튼 노출
- 1280×800, 1024×768 전체 흐름에서 넘침·겹침·이미지 누락 0건
- `node scripts/check-lesson-contract.mjs`
- `node scripts/check-stage-ratio.mjs`

## 범위 밖

- 1·2·4·5단원 변경
- 새 매스몬 팩 제작
- 점수판 API 변경
- 배포·푸시
