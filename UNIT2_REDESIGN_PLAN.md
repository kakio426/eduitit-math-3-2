# 구현 계획: 2단원 나눗셈 게임 고도화와 Mathmon Engine v1.1 이관

## 진행 현황 (2026-07-10)

- 2-1 `매스몬 나누기 농장`부터 2-4 `매스몬 검산 자물쇠`까지 네 차시 엔진 이관 완료
- 공통 엔진의 의미 있는 선택지, 전체 시도 기록, poster-two-step 설명, modal-art 보상, hybrid-generated-dynamic 결과 계약 반영 완료
- 네 차시 모두 생성 이미지 전체 장면 + 동적 SVG 수학판 + 접근성 HTML 선택지 구조 적용
- 2-2 첫 단계는 `십의 자리 몫`과 `나머지`를 선택지에 직접 표기하고, 계산판에서 `나머지(남은 십)`의 관계를 보여 줌
- 데스크톱 `1280×800`, 태블릿 가로 `1024×768` 전체 흐름 QA 완료

## 목표

- 초등학교 3학년 학생이 나눗셈의 절차를 외우는 데서 끝나지 않고, `똑같이 나누기`, `남은 십 내리기`, `나머지의 범위`, `곱셈으로 되돌려 보기`를 화면에서 직접 확인하게 한다.
- 2단원 네 차시의 수학 조작판은 보존하면서 화면 흐름, 설정, 보상, 결과, 접근성, QA는 `_engine/v1` 공통 코드가 맡게 한다.
- 소스는 공통 엔진과 차시 플러그인으로 나누되, 학생에게 배포하는 결과는 지금처럼 차시별 독립 실행 `index.html` 한 개를 유지한다.
- 먼저 2차시 `매스몬 엘리베이터`를 골드 스탠더드로 이관하고 검증한 뒤 3차시, 4차시, 1차시 순서로 확장한다.

## 시각 구현 원칙: CSS는 배치만, 이미지는 세계와 물성을

- 학생이 보는 장면, 조명, 공간, 재질, 기계, 농장, 별, 금고 같은 시각적 물성은 생성형 bitmap 이미지가 맡는다.
- CSS gradient, 복잡한 box-shadow, pseudo-element를 여러 겹 쌓아 그림처럼 보이게 만드는 방식은 새 골드 스탠더드에서 사용하지 않는다.
- 수학식, 현재 숫자, 정확한 칸 정렬, 막대 길이, 묶음 수처럼 문제마다 바뀌는 학습 정보는 SVG 또는 짧은 HTML로 그린다.
- HTML 버튼은 정확한 클릭 영역과 접근성을 맡는다. 보이는 버튼 표면을 이미지로 만들 때도 HTML은 같은 위치의 hitbox만 맡는다.
- CSS는 `1280×800` 좌표 안에서 이미지, SVG, hitbox를 배치하고 상태를 전환하는 역할로 제한한다.
- 문제 화면용 생성 이미지는 글자·숫자·수식·선택지를 포함하지 않는다. 학생이 읽고 판단해야 하는 값은 항상 선명한 동적 레이어로 둔다.
- 먼저 2-2 엘리베이터 문제 화면의 이미지 중심 시안을 승인받고, 승인된 시안에서 배경/조작대/버튼 표면 자산 계약을 뽑은 뒤 코드에 연결한다.

역할 분담:

| 레이어 | 담당 | 예시 |
| --- | --- | --- |
| 생성 이미지 | 세계, 분위기, 물성, 고정 장치 | 엘리베이터 내부, 층 통로, 금속 계산대, 빛, 그림자 |
| SVG | 수학적 관계와 정확한 변화 | 세로셈 칸, 남은 십 이동, 별 묶음, 검산 막대 |
| HTML | 짧은 지시와 접근성 hitbox | `내린 수를 골라요`, 선택 버튼, 설정 버튼 |
| CSS | 배치와 상태 전환 | 좌표, 크기, 숨김, focus, reduced motion |

## 문제 화면 정보량 계약

문제를 푸는 동안 학생에게 기본으로 보이는 학습 요소는 아래 네 개뿐이다.

1. 큰 문제
2. 현재 수학 조작판
3. 한 줄 지시 또는 한 줄 오답 증명
4. 지금 눌러야 하는 선택지

강제 규칙:

- 지시문과 오답 피드백은 같은 한 줄 슬롯을 교대로 사용한다.
- 보상 진행도, 결과 등급, 닉네임, 전체 목표 지도, 긴 단계 설명은 문제 화면에서 숨긴다.
- 이전 단계는 작은 완료 표시 하나로만 남기고 다음 단계는 `?`로 둔다.
- 정답을 고르면 선택지보다 조작판의 변화가 먼저 보인다.
- 정답 확인이 끝나기 전에는 보상 이미지와 보상 모달을 보여 주지 않는다.
- 장면 안에서 학생이 눌러야 할 것이 두 종류 이상 동시에 강조되면 실패다.

차시별로 문제 화면에서 치울 요소:

- 2-1 농장: 왼쪽의 큰 수확 바구니는 문제 중 숨기고 정답 뒤 보상 장면에서 크게 보여 준다.
- 2-2 엘리베이터: 엘리베이터는 현재 단계를 알려 주는 작은 동행 장치만 남기고, 큰 문제와 세로셈판을 중앙 주인공으로 둔다.
- 2-3 별 줍기: 왼쪽 별빛 진행·별 이름 입력·결과 미리보기 패널은 문제 중 모두 숨긴다. 별은 오답 증명과 정답 확인에 필요한 개수만 보여 준다.
- 2-4 검산 자물쇠: 왼쪽의 큰 금고 장면은 문제 중 숨기고, 곱 막대와 나머지 막대를 화면 중앙에 크게 둔다. 금고는 검산 완료 뒤 열린다.

## 현재 문제

### 기술 구조

- 2단원 네 `index.html`은 합계 약 1만 6천 줄이며, 화면 전환, 설정, 오디오, 보상, 결과, 접근성 코드가 각 차시에 중복되어 있다.
- `_engine/v1`과 `_lessons` 기반의 소스 분리 작업은 이미 시작되어 있다. 새 구조를 다시 만드는 대신 현재 v1을 확장한다.
- 현재 공통 런타임의 선택지는 단순 문자열 버튼을 전제로 한다. 다음 2단원 UI를 그대로 표현할 수 없다.
  - 1차시: 묶음과 낱개를 칸에 넣고 `확인`하는 배치형 조작
  - 2차시: `몫`과 `남은 십`을 한 카드에서 함께 고르는 복합 선택지
  - 3차시: 고른 몫이 너무 큰지·작은지를 막대와 남은 별로 증명하는 오답 피드백
  - 4차시: `몫×나누는 수`와 `+나머지`를 막대 길이로 복원하는 검산판
- 현재 런타임은 정답 뒤 650ms가 지나면 자동으로 다음 단계로 이동한다. 차시별 정답 증명을 충분히 보여 주거나 학생이 직접 다음 단계로 넘어가는 흐름을 만들기 어렵다.
- 현재 풀이 기록은 단계별 첫 선택 하나만 저장한다. 첫 오답 뒤 어떤 답으로 고쳐 풀었는지, 같은 오개념을 반복했는지 남지 않는다.
- 공통 런타임은 `applyReward()`가 상태를 직접 바꾸는 방식과 새 상태를 반환하는 방식을 혼용한다. 현재 일부 모델은 새 객체를 반환하지만 런타임이 반환값을 사용하지 않는다. 계약 테스트를 추가한 뒤 한 방식으로 통일해야 한다.
- `_engine/v1/README.md`가 요구하는 `inline-panel` 보상과 `hybrid-generated-dynamic` 결과 모드는 현재 스키마 열거형에 완전히 반영되어 있지 않다.

### 교육·UX 구조

- 네 차시 모두 문제 생성과 대표 오답 검증은 잘 갖춰져 있다. 고도화의 핵심은 내용을 새로 늘리는 것이 아니라, 오답이 왜 틀렸는지를 수학 조작판에서 더 분명하게 보여 주는 것이다.
- 1차시 문제 화면은 실제로 묶음을 칸에 넣는 조작이 강점이지만, 별도의 확인·다시 버튼과 테마 영역이 공통 엔진의 단순 4지선다 구조와 다르다.
- 2차시는 큰 식, 현재 세로셈 칸, 한 줄 지시, 복합 선택지가 가장 명확하게 정리되어 있어 공통 기준으로 삼기 좋다.
- 3차시는 `너무 많은 몫`과 `너무 적은 몫`을 시각적으로 증명하는 피드백이 가장 교육적이다. 다만 왼쪽 별빛·이름 영역이 문제 풀이보다 크게 보여 공통 와이어프레임에서는 축소 또는 접기가 필요하다.
- 4차시는 검산 관계를 막대 길이로 보여 주는 장점이 있지만 상단 단계 탭, 좌측 금고 장면, 중앙 막대판이 동시에 경쟁한다. 현재 단계 하나만 강조하는 밀도 조정이 필요하다.

## 교육 브리프

- 학습자: 초등학교 3학년, 한국어 사용자
- 운영자: 학생이 직접 실행하며, 이번 범위에서는 교사용 대시보드를 만들지 않는다.
- 기기: 컴퓨터와 태블릿 가로, 기준 Stage `1280×800`, 확인 화면 `1024×768`
- 피드백 말투: 짧고 직접적이며 비난하지 않는다. 오답의 이유는 문장보다 움직이는 묶음·칸·막대로 보여 준다.
- 수집할 데이터: 문제 ID, 단계 ID, 시도 순서, 선택지 ID, 오개념 ID, 정오 여부, 단계별 시간
- 수집하지 않을 데이터: 학생 실명, 학교, 학급, 연락처. 서버가 없을 때도 모든 게임 흐름은 끝까지 동작해야 한다.

## 차시별 학습 설계

| 차시 | 핵심 수학 지식 | 학생의 한 행동 | 반드시 잡을 오개념 | 정답 확인 장면 |
| --- | --- | --- | --- | --- |
| 2-1 나누기 농장 | 십과 일을 각각 똑같이 나누고 몫을 합침 | 현재 묶음 또는 낱개를 각 칸에 넣는다 | 묶음 수와 한 칸의 양 혼동, 십과 일을 합치지 않음 | 모든 칸에 같은 양이 들어가고 `55÷5=11`이 완성됨 |
| 2-2 엘리베이터 | 십의 자리에서 남은 십을 일의 자리로 내려 계산 | 현재 세로셈 칸에 들어갈 값을 고른다 | 남은 십 누락, 일의 자리 숫자만 나눔 | 남은 십이 아래 칸으로 이동하고 두 자리 내린 수가 완성됨 |
| 2-3 별 줍기 | 나머지는 나누는 수보다 작아야 함 | 가능한 가장 큰 몫 또는 남은 별을 고른다 | 몫이 너무 큼, 몫이 너무 작아 더 나눌 수 있음, 나머지 범위 위반 | `나누는 수×몫` 막대와 남은 별을 합쳐 원래 수를 복원함 |
| 2-4 검산 자물쇠 | `나누는 수×몫+나머지=나누어지는 수` | 곱한 값 또는 나머지를 더한 값을 고른다 | 나머지 더하기 누락, 곱과 나머지를 잘못 결합 | 두 막대가 원래 수의 끝선에 정확히 닿아 자물쇠가 열림 |

## 골드 스탠더드

첫 이관 대상은 `3-2-2-2-mathmon-elevator`로 한다.

선정 이유:

1. 십의 자리 나누기, 내리기, 일의 자리 나누기의 서로 다른 세 단계가 있어 단계 계약을 검증할 수 있다.
2. `몫 + 남은 십` 복합 선택지가 있어 단순 문자열이 아닌 선택지 렌더러를 시험할 수 있다.
3. 남은 십 누락이라는 대표 오개념이 명확하다.
4. 정답 확인 뒤 학생이 `엘리베이터 움직이기`를 누르는 흐름과 자동 QA가 이미 있다.
5. 모달 보상과 하이브리드 결과 화면까지 포함해 공통 엔진의 전체 흐름을 한 차시에서 검증할 수 있다.

이관 순서:

1. 2-2 엘리베이터: 공통 단계·선택지·정답 증명 계약 확립
2. 2-3 별 줍기: 오개념별 시각 피드백과 여러 번의 시도 기록 확장
3. 2-4 검산 자물쇠: 막대형 조작판과 단원 종합형 단계 확장
4. 2-1 나누기 농장: 배치·확인·다시 조작을 지원하는 복합 입력 확장

## 범위

- target_app:
  - `_engine/v1/`
  - `_lessons/3-2-2-*/`
  - `3-2-2-1-mathmon-divide-farm/`
  - `3-2-2-2-mathmon-elevator/`
  - `3-2-2-3-mathmon-star-pickup/`
  - `3-2-2-4-mathmon-check-lock/`
  - 2단원 QA 스크립트와 공통 엔진 QA
- do_not_touch_apps:
  - 1단원, 3단원, 4단원, 5단원 실행 화면
  - `scoreboard-api`
  - Railway, GitHub Pages, CI와 배포 설정
- 이번 단계에서 안 건드릴 범위:
  - 새 이미지 생성과 매스몬 팩 교체
  - 스마트폰 세로 대응
  - 교사용 대시보드
  - 실제 배포와 푸시

## 남길 것 / 바꿀 것

### 유지

- 차시별 문제 후보와 수학 항등 검증
- 한 판 10문제
- 랜덤 보상, 오답 문제의 불리한 보상, 정답 수 게이트, 빈손 없는 결과
- 차시별 중심 보상 하나
- 현재 생성형 커버·설명·보상·결과 자산
- 기존 차시 폴더와 공개 URL
- 기존 2단원 전용 QA의 수학 검증과 주요 스크린샷 상태

### 제거

- 각 `index.html`에 반복된 화면 전환, 설정, 오디오, 결과, 공통 보상 코드
- 단순 문자열만 받을 수 있는 공통 선택지 가정
- 모든 단계에 고정된 650ms 자동 이동
- 첫 오답 하나만 남기는 풀이 기록
- 수학 행동과 관계없는 큰 테마 패널

### 통합

- Stage, 화면 레지스트리, 설정, 오디오, 포커스, 보상, 결과, 접근성은 엔진으로 통합
- 문제 생성, 오개념, 수학 조작판, 단계별 증명은 차시 플러그인으로 통합
- 기존 네 QA의 공통 브라우저 조작과 기하 검사는 공통 QA 유틸리티로 통합

## Mathmon Engine v1.1 계약

### 1. 문제와 선택지 데이터

선택지를 원시 문자열 대신 의미가 있는 객체로 표준화한다.

```js
{
  id: "omit-carried-ten",
  value: 2,
  label: "2",
  misconceptionId: "DIV2_OMIT_REMAINING_TEN",
  feedbackKind: "show-missing-ten",
  ariaLabel: "내린 수 2"
}
```

단계에는 다음 정보를 둔다.

```js
{
  id: "carry-down",
  action: "내린 수를 고른다",
  instruction: "내린 수를 골라요.",
  answerChoiceId: "down-12",
  choices: [],
  advance: { mode: "student", label: "다음" },
  feedback: { correct: "12가 되었어요." }
}
```

### 2. 조작판 플러그인

공통 엔진이 수학판 내부 DOM을 직접 가정하지 않게 한다.

```js
window.MathmonLessonView = {
  mount(context),
  renderProblem(context),
  renderStep(context),
  renderChoices(context),
  renderAttempt(context),
  revealCorrect(context),
  renderComplete(context),
  resetProblem(context),
  destroy()
};
```

- 엔진은 상태와 접근성, 공통 버튼 동작을 맡는다.
- 플러그인은 묶음판, 세로셈판, 별 막대, 검산 막대를 그린다.
- 단순 4지선다 차시는 기본 `renderChoices`를 사용하고, 복합 선택·배치형 차시만 재정의한다.

### 3. 단계 상태 머신

```text
ready
  -> attempting
  -> wrong-evidence
  -> attempting
  -> correct-evidence
  -> awaiting-next 또는 timed-next
  -> next-step
  -> problem-complete
  -> awaiting-reward
```

- 오답 뒤에는 선택지를 계속 고를 수 있다.
- 정답 뒤에는 최소한 한 번의 수학적 변화가 조작판에 보여야 한다.
- `advance.mode`는 `student`, `timed`, `immediate` 중 하나로 둔다.
- 마지막 단계는 항상 `problem-complete -> awaiting-reward`를 거치며 보상이 정답 확인을 덮지 않게 한다.

### 4. 오개념과 시도 기록

```js
stepRecord.attempts = [
  {
    choiceId,
    misconceptionId,
    correct,
    elapsedMs,
    attemptIndex
  }
];
```

- 모든 오답 선택지는 `misconceptionId`를 가져야 한다.
- 같은 오개념을 두 번 고르면 두 번째에는 더 구체적인 시각 증명을 보여 줄 수 있다.
- 결과 등급 계산은 기존처럼 첫 시도 정답 여부를 사용하되, 학습 피드백은 전체 시도 기록을 사용한다.

### 5. 보상 계약

- `applyReward`는 상태를 직접 변경하거나 새 상태를 반환하는 방식 중 하나로 통일한다.
- 권장 방식은 `const patch = applyReward(snapshot, event)`처럼 순수한 결과를 반환하고 엔진이 명시적으로 상태에 반영하는 것이다.
- `modal-art`, `stage-full`, `inline-panel`을 스키마와 런타임에서 같은 이름으로 지원한다.

### 6. 결과 계약

- `hybrid-generated-dynamic`, `fullscene-score-slot`, `simple-generated`를 README, 스키마, 런타임에서 같은 이름으로 사용한다.
- 2단원 기존 결과 화면은 이관 중 새로 디자인하지 않고 현재 렌더 모드를 그대로 보존한다.

## 공통 문제 화면 와이어프레임

기준 좌표는 `1280×800` Stage다.

```text
┌──────────────────────────────────────────────────────────────┐
│ 24  브랜드/문제 수                         단원/설정   24    │  y 24~88
├──────────────┬───────────────────────────────────────────────┤
│              │                 큰 문제                       │  y 104~190
│ 테마 동행    ├───────────────────────────────────────────────┤
│ 최대 180px   │                                               │
│              │           현재 수학 조작판                    │  y 200~500
│ 문제 중에는  │                                               │
│ 조용히 유지  ├───────────────────────────────────────────────┤
│              │ 한 줄 지시 또는 현재 오답 증명 한 줄          │  y 510~570
│              ├───────────────────────────────────────────────┤
│              │                 선택지                        │  y 580~752
└──────────────┴───────────────────────────────────────────────┘
```

규칙:

- 테마 동행 영역은 폭 180px 이하의 보조 슬롯이다. 수학적 의미가 없으면 숨긴다. 2-3과 2-4의 기본 문제 상태에서는 사용하지 않는다.
- 큰 문제와 현재 조작판은 오른쪽 주 영역의 70% 이상을 사용한다.
- 지시문과 오답 증명은 같은 슬롯을 교대로 사용해 문장 패널이 늘어나지 않게 한다.
- 선택지는 단계마다 한 덩어리로 유지한다.
- 이전 단계는 작은 완료 칩, 다음 단계는 `?`로만 표시한다.
- 정답 뒤에는 선택지 영역을 줄이고 완성식과 다음 행동 버튼을 보여 준다.
- 최소 터치 영역은 `56×56px`, 주요 선택지는 높이 `88px` 이상을 목표로 한다.
- 이 골격은 CSS 카드 여러 개로 만들지 않는다. 승인된 `problem-stage-generated.webp` 장면 위에 하나의 수학 SVG와 최소 HTML hitbox를 올린다.

## 구현 단계

### 0. 작업 기준 고정

1. 현재 수정 중인 파일을 덮어쓰지 않도록 `git status`와 관련 diff를 먼저 확인한다.
2. 기존 2단원 QA 스크립트와 스크린샷은 기준선으로만 사용하고, 골드 스탠더드 승인 전에는 자동 갱신하지 않는다.
3. `_engine/v1`의 현재 변경이 정리된 시점에 짧은 `codex/unit2-engine-v1-1` 브랜치에서 구현한다.

### 1. 공통 계약을 테스트로 확장

1. `_engine/v1/schemas/lesson.schema.json`에 선택지 객체, 단계 전이, workbench hook, 오개념, 보상·결과 모드를 정의한다.
2. `scripts/check-lesson-contract.mjs`에 다음 실패 조건을 추가한다.
   - 답이 아닌 선택지에 `misconceptionId`가 없음
   - 마지막 단계가 정답 확인 없이 보상으로 연결됨
   - README·스키마·런타임의 모드 이름 불일치
   - `applyReward` 반환 계약 불일치
3. 공통 런타임 상태 전이 단위 테스트를 추가한다.

### 2. 공통 런타임 v1.1 구현

1. `_engine/v1/runtime/core.js`에서 선택지 객체와 사용자 진행/시간 진행을 모두 지원한다.
2. 전체 시도 기록과 오개념 ID를 저장한다.
3. `MathmonLessonView` hook을 호출해 차시별 조작판이 정답·오답 증명을 직접 렌더하게 한다.
4. `applyReward` 계약과 결과 모드 이름을 통일한다.
5. 기존 v1 파일로 만든 차시가 깨지지 않도록 원시 문자열 선택지를 임시 호환한다.

### 3. 2-2 엘리베이터 골드 스탠더드 이관

1. 현재 문제 화면을 참고하되 정보량을 줄인 이미지 중심 시안을 먼저 만든다.
2. 시안 승인 뒤 `problem-stage-generated.webp`, 필요한 고정 조작대 이미지, 상태별 버튼 표면 이미지의 자산 계약을 작성한다.
3. `_lessons/3-2-2-2-mathmon-elevator/`에 `lesson.json`, `model.js`, `view.js`, `lesson.css`를 만든다.
4. 기존 문제 후보, 세 단계 계산, 대표 오답, 보상 확률, 결과 기준을 그대로 옮긴다.
5. 1단계 복합 선택지와 2단계 내림 증명을 SVG와 view hook으로 구현한다.
6. 기존 `scripts/qa-lesson2-elevator.mjs`의 수학·화면 계약을 새 빌드에도 그대로 통과시킨다.
7. 기존 `index.html`과 새 빌드의 주요 상태를 나란히 캡처해 기능·레이아웃 회귀를 확인한다.
8. 골드 스탠더드 승인 전에는 기존 실행 파일을 덮어쓰지 않고 별도 임시 출력으로 비교한다.

### 4. 나머지 차시 이관

1. 2-3 별 줍기: `too-many`, `too-small`, `good` 증명과 반복 오답 기록을 이관한다.
2. 2-4 검산 자물쇠: 곱 막대와 나머지 막대가 원래 수 끝선에 닿는 상태를 이관한다.
3. 2-1 나누기 농장: 배치 선택, 확인, 다시, 칸별 동일 분배를 custom workbench 입력으로 이관한다.
4. 각 차시는 기존 전용 QA 통과 뒤에만 다음 차시로 넘어간다.

### 5. 문서와 최종 검증

1. 각 차시 `PLAN.md`, `README.md`, `REPORT.md`를 실제 새 엔진 기준으로 갱신한다.
2. `_engine/v1/README.md`에 2단원 플러그인 예제와 새 차시 추가 절차를 적는다.
3. Humanizer 학생 문구 QA를 거친다.
4. 모든 검증이 끝난 뒤에만 기존 차시 `index.html`을 새 빌드 결과로 교체한다.

## 검증

### 수학 검증

- 각 차시를 고정 seed 200회 이상 생성하고 한 판 안 중복, 범위, 항등식을 검사한다.
- 2-1: 십과 일이 각각 나누어떨어지고 조합한 몫이 원래 식과 일치한다.
- 2-2: 십의 자리 내림이 반드시 있고 최종 나머지는 0이다.
- 2-3: `0 < 나머지 < 나누는 수`, `나누는 수×몫+나머지=나누어지는 수`가 항상 성립한다.
- 2-4: `나누는 수×몫+나머지=나누어지는 수`가 항상 성립하고 나머지 누락 오답이 항상 포함된다.
- 모든 오답 선택지가 오개념 ID와 연결되는지 검사한다.

### 흐름 검증

- 첫 화면 -> 설명 1 -> 설명 2 -> 각 단계 -> 오답 -> 오개념 증명 -> 정답 -> 정답 확인 -> 보상 -> 결과를 완주한다.
- 마지막 정답 직후 보상 모달이 열리지 않는지 확인한다.
- 설정의 배경 소리, 효과 소리, 방법 다시 보기, 처음부터, 닫기를 확인한다.
- 오프라인에서도 게임을 끝까지 진행할 수 있어야 한다.

### 화면 검증

- `1280×800`, `1024×768`, 현재 인앱 브라우저와 비슷한 크기에서 캡처한다.
- 문제 1단계, 대표 오답, 정답 증명, 다음 단계, 마지막 확인, 보상, 결과를 확인한다.
- 텍스트 넘침, 요소 겹침, Stage 밖 요소, 클릭 영역 충돌은 0개여야 한다.
- 문제와 선택지가 테마 장면보다 먼저 보여야 한다.

### 명령 검증

```bash
node scripts/check-lesson-contract.mjs
node scripts/check-stage-ratio.mjs
node scripts/qa-lesson-flow.mjs 3-2-2-2-mathmon-elevator
node scripts/qa-lesson2-elevator.mjs
node scripts/qa-lesson2-star-pickup.mjs
node scripts/qa-lesson2-check-lock.mjs
node scripts/qa-lesson2-divide-farm.mjs
```

## 위험

- 현재 작업 트리에 다른 차시와 엔진 수정이 진행 중임 -> 구현 전에 변경 소유 범위를 다시 확인하고, 기존 수정이 정리되기 전에는 공용 파일을 편집하지 않는다.
- 공통 엔진이 수학 조작판까지 통제해 차시가 획일화될 수 있음 -> 엔진은 상태와 shell만 맡고 수학판은 lesson view hook이 소유한다.
- 한 번에 네 차시를 옮기며 회귀가 커질 수 있음 -> 2-2 한 차시 승인 후 2-3, 2-4, 2-1 순서로 이동한다.
- 오개념 피드백이 긴 설명문으로 변할 수 있음 -> 한 줄과 한 가지 시각 증명만 사용한다.
- 새 엔진 이관 중 기존 공개 게임이 깨질 수 있음 -> 별도 임시 출력 비교와 기존 QA 통과 뒤에만 실행 파일을 교체한다.
- 보상 리팩터링이 기존 확률을 바꿀 수 있음 -> 기존 이벤트 가중치와 결과 게이트를 스냅샷 테스트로 고정한다.

## 첫 구현 단위의 완료 조건

2-2 엘리베이터 골드 스탠더드는 아래를 모두 만족해야 완료다.

- 기존 수학 후보와 대표 오답 검사가 모두 통과한다.
- 세 단계가 새 엔진의 상태 머신으로 동작한다.
- 복합 선택지가 문자열 편법 없이 의미 있는 객체로 렌더된다.
- 남은 십 누락 오답을 고르면 내림 관계가 조작판에서 바로 보인다.
- 모든 시도가 오개념 ID와 함께 기록된다.
- 마지막 계산식 확인 뒤 학생이 직접 보상으로 이동한다.
- 기존 보상 확률과 결과 등급이 유지된다.
- 데스크톱과 태블릿 가로에서 넘침·겹침이 없다.
- 새 빌드가 독립 실행 가능한 단일 `index.html`로 나온다.
- 기존 전용 QA와 공통 엔진 QA가 모두 통과한다.
